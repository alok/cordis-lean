# CORDIS paper and upstream map

<!-- markdownlint-disable MD013 MD029 -->

> **Current-development addendum.** This document records the reviewed `0.1.0`
> mapping at DeepSeek Harness `47f9438`. The active `0.2` contract and refreshed
> Harness pin `99f6f02` live in [`V0_2_SPEC.md`](V0_2_SPEC.md). The new
> `Cordis.GenericHarness`, `Cordis.GenericSessionHarness`, `Cordis.Session`,
> `Cordis.SessionValidation`, and `Cordis.SessionTheoremBridge`
> implement a generic phase-indexed runner, exact-call policy denial,
> visibility-indexed rich events, proof-producing surface-intent validation,
> certified replacement, request reconstruction, and a proof that generic rich
> sessions (exercised by both the counter and dependent-choice catalogs) project
> exactly to their structural runner logs. This
> does not retroactively turn the tables below into a full paper or
> TypeScript-equivalence claim.
>
> `Cordis.Coeffect` directly mechanizes the finite local content of paper
> Definitions 22–26. `Cordis.UnifiedContext` directly models Definitions 27–31
> and exposes only finite unfoldings of Definition 32's negatively recursive
> equation. `Cordis.ContextualEquivalence` mechanizes the finite-context clause
> of Definition 33 and proves satisfaction/notification invariance.
> `Cordis.OperationalEquivalence` covers Definition 34 and generator-level
> Lemma 35 while exposing a formal paired-inverse gap; `Cordis.QuotientEffect`
> and `Cordis.CoeffectQuotient` cover Definitions 36–37 and the finite
> composition core of Lemma 38. `Cordis.Transformation` covers Definition 17,
> Lemma 18, and full Definition 19; `Cordis.OperationIndependence` adds full
> total Definition 39, finite partial distinct-key Theorem 40, and Definition 41. `Cordis.Removal` proves finite exact Theorem 20/Corollary 21, while
> `Cordis.MediatedIndependence` reifies branches and separates the observational
> Theorem 42 statement from exact representative coherence.
> `Cordis.MediatedTheorem` refutes that old partial-domain closure and proves the
> corrected exact/observational finite whole-run interchange for arbitrary trees;
> `Cordis.PartialTransformation` promotes it to full finite partial/Kleisli transformation
> monoids. `Cordis.TotalQuotientIndependence` adds an explicit totality-certified quotient
> specialization whose total words are linked back to those closures; the paper's unrestricted
> total/quotient setting remains separate. `Cordis.DomainTotalQuotientIndependence` then keeps
> partiality explicit while certifying totality and domain preservation on a named invariant
> domain, transporting the same finite observational theorem without global totalization.
> `Cordis.GlobalLifecycle` now checks
> the seven lifecycle rule names as eight phase-indexed constructors, while the
> `Cordis.GlobalCalculus` combines those with orchestration in one ten-name,
> exact-endpoint, empty-origin trace relation. Full global metatheory remains absent.
>
> `Cordis.Schedule` proves arbitrary finite semantic permutation of certified
> commuting pure effects, while `Cordis.RichStream` models the current Harness
> block stream. `Cordis.RuntimeRefinement` is a real but deliberately partial
> cross-language boundary: it decodes supported current-Harness `StreamChunk`
> JSON-AST shapes into an intrinsic rich-stream witness and rejects unsupported
> shapes. `Cordis.SessionRefinement` separately validates a source-shaped,
> stateful turn/step/tool session subset, restricted request headers, route context,
> whole-list todo snapshots, empty seed markers, and text-only
> assistant chunks, plus text/reasoning/image and complete assistant tool-call surface blocks, into
> local Session and Protocol witnesses. Surface IDs and provider metadata are retained
> in the refinement state; provider call IDs are allocated once and reused by later
> call/result events. Neither module is a behavioral-
> equivalence or completeness theorem. `Cordis.SessionTheoremBridge` gives the
> stable `ValidatedAppend.applies` and `ValidatedLog.replays` names for the
> already proof-carrying append and finite-log endpoint certificates.
> `Cordis.GlobalDynamics` is paper-side rather than a TypeScript refinement: it
> gives opaque iterator/undo codes an external proof-carrying semantics.

This document maps the Lean proof kernel and local reference layers to the pinned CORDIS paper
and to the runtime systems that motivated them. It is a claim ledger, not a claim that the Lean
code is a line-by-line mechanization or a drop-in implementation of either JavaScript system.

## Claim vocabulary

The labels below have deliberately narrow meanings.

| Label               | Meaning                                                                                                                      |
| ------------------- | ---------------------------------------------------------------------------------------------------------------------------- |
| **Proved**          | A proposition or proof field is checked by Lean's kernel. The claim is only the displayed Lean type.                         |
| **Checked**         | A constructor, index, or executable validator rejects some bad values. No refinement theorem to an upstream runtime follows. |
| **Trusted**         | Correctness depends on data or behavior supplied outside the proved Lean term.                                               |
| **Not implemented** | The cited paper or runtime claim has no theorem in the mapped modules.                                                       |

“Corresponds to,” “models,” and “is a narrow analogue of” do not mean “proves the
paper theorem.” In particular, a theorem about one local transition is not identified
with a paper theorem quantified over an interleaved, multi-fiber execution.

## Pinned evidence set

All external links below include a full commit hash.

| Source            | Pin                                                        | Evidence used here                                                                          |
| ----------------- | ---------------------------------------------------------- | ------------------------------------------------------------------------------------------- |
| CORDIS paper      | [`948a07b369c62adb3b12e102458be5c18dfb69b9`][paper-tree]   | The 88-page draft [_A Programming Paradigm for Spatiotemporal Composability_][paper-pdf]    |
| Standalone CORDIS | [`8cc9e33fab69e2d0476d126baaf2acb24e6a6ab4`][cordis-tree]  | `Context`, `Fiber`, registry, resolution, isolation, interception, and lifecycle source     |
| DeepSeek Harness  | [`47f943859bef60e4160492346772ded9b24f765a`][harness-tree] | Tool runtime, ordered tool scheduler, session log, and optional session invariant companion |

The Harness snapshot source-vendors its production framework. Its
[vendor manifest][harness-vendor] pins:

- `@deepseek-ai/cordis` `4.0.0-rc.7` and the loader `1.0.0-rc.5` to CORDIS
  [`56b3d4f725681cf4556c1a8695a709cc3b6eed74`][harness-cordis-pin]; and
- the include, group, timer, HMR, and console-logger plugins to DeepSeek's CORDIS fork
  [`abb0a307cb1d3b0947f455d590cf5ba922d4caa4`][harness-plugins-pin].

The same manifest records local modifications to the vendored sources. Therefore the
actual Harness behavior is the code under `vendor/` at the Harness pin, not an inferred
combination of either CORDIS commit with the Harness code. The newer standalone CORDIS
pin is architectural evidence only; compatibility between the three CORDIS snapshots is
not assumed.

## Declaration map

### `Cordis.EffectContext`

Local source: [`Cordis/EffectContext.lean`](../Cordis/EffectContext.lean).

| Lean declaration                                                                                                           | Status and exact Lean guarantee                                                                                                                                                                                                           | Paper correspondence                                                  | Boundary                                                                                                                                      |
| -------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------- |
| `twistedComp`, `twistedIdentity`                                                                                           | **Proved:** forward maps compose in application order while candidate inverses compose in the opposite order; identity and associativity are extensional.                                                                                 | Definition 1 and the twisted composition monoid `𝔗Γ`.                 | Function-level context maps only; no quotient or external-effect interpretation.                                                              |
| `EffectContext`, `track`, `recover`, `track_projection`, `track_comp`, `recover_track`                                     | **Proved:** `∂Γ = Γ × (Γ → Γ)` carries the current context and inverse accumulator; tracking projects to the forward map, is a twisted-monoid homomorphism, and preserves the recovery result under the stated one-sided inverse premise. | Definitions 2–3, 6 and Theorems 4–5, 7.                               | The inverse law is supplied per pair/application; no global invertibility is inferred.                                                        |
| `EffectFunction`, `WitnessedEffect`, `effectComp`, `WitnessedEffect.effectComp`                                            | **Proved:** state-indexed effects return their selected inverse, effect composition has unit/associativity, and witnessed effects form a submonoid.                                                                                       | Definitions 8–9 and Theorems 10–11.                                   | This is exact representative equality, not the later observational quotient or independence theorem.                                          |
| `effectLift`, `effectLift_comp`, `effectLift_projection`, `effectLift_inverse_value`, `effectLift_isWitnessed_iff`         | **Proved:** the next effect-context level preserves composition/projections; the lifted inverse has the exact accumulator formula and preserves recovery, with a precise uniform-inverse criterion for full witnessing.                   | Definition 12 and Theorems 13–15.                                     | The finite lift is exact, but arbitrary interleaving and Definition 19 remain separate claims.                                                |
| `Theorems.applyEffect`, `Theorems.inverseAt`, `Theorems.Run`, `Theorems.Run.recovery_eq`, `Theorems.Run.reverseContext_eq` | **Proved:** an indexed finite sequence of witnessed lifted effects preserves its recovery target, and applying the selected inverses in reverse order recovers the initial raw context exactly.                                           | Theorem 16's finite reverse-order application and soundness fragment. | This is an indexed finite run, not a theorem about arbitrary interleavings, quotient independence, or the later transformation-monoid claims. |

### `Cordis.Effect`

Local source: [`Cordis/Effect.lean`](../Cordis/Effect.lean).

| Lean declaration                                                    | Status and exact Lean guarantee                                                                        | Paper correspondence                                                | Boundary                                                                                                                      |
| ------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------- |
| `Applied`, `Effect`                                                 | **Proved by construction:** each application stores `undo_after : undo after = before`.                | §3.1.2, Definition 8, witnessed effect functions.                   | This is a state-indexed, one-sided inverse. It says nothing about `undo` away from the particular `after`.                    |
| `Effect.seq`, `seq_recovers`                                        | **Proved:** two witnessed effects compose and the second inverse runs before the first.                | Definition 9 and Theorem 11(1).                                     | Lean's `seq first second` runs `first` then `second`; it corresponds to the paper's `second ⋄ first` order convention.        |
| `identity_seq`, `seq_identity`, `seq_assoc`                         | **Proved:** the Lean operation has extensional unit and associativity laws.                            | The monoid part of Theorem 10(1).                                   | Theorem 10(2), the homomorphism from transformation pairs, is not formalized.                                                 |
| `UndoAccumulator.push`, `UndoStack.push`, `UndoStack.recover_after` | **Proved:** an indexed LIFO sequence recovers its exact initial state from its indexed terminal state. | The reverse-order recovery conclusion of Theorem 16.                | This does not include the paper's nested effect context, soundness invariant, or arbitrary interleaving.                      |
| `Observational.Applied`, `Observational.Effect.seq`, `seq_recovers` | **Proved:** every inverse respects a supplied `Setoid`, and composition recovers modulo it.            | Definitions 36–37 and the composition/recovery portion of Lemma 38. | The supplied `Setoid` is arbitrary. The coeffect-derived relation of Definition 33 and the full Lemma 38 are not constructed. |

The standalone CORDIS runtime accepts disposer callbacks, promises, and iterators and
collects their disposers; the pinned source is in [`fiber.ts`][cordis-fiber]. The Harness
vendor adds documented re-entrant-disposal and lifecycle hardening in its
[`vendor/cordis/src/fiber.ts`][harness-vendor-fiber]. Those runtimes trust plugin authors
to return correct inverses. Lean instead makes recovery evidence part of `Applied`, but
does not model JavaScript promises, cleanup failures, or re-entrant disposal.

`Effect.seq` equality is not paper independence. The paper explicitly distinguishes
commutativity of a composite from Definition 19's requirements on every forward map,
every yielded inverse, and inverse-yield stability.

### `Cordis.Batch`

Local source: [`Cordis/Batch.lean`](../Cordis/Batch.lean).

| Lean declaration                                                                               | Status and exact Lean guarantee                                                                                                                                                            | Paper correspondence                                                                                                 | Boundary                                                                                                                                                                                       |
| ---------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `Effect.IndependentAt`, `Effect.Independent`                                                   | **Proved by construction:** at one predecessor, or at every predecessor, a certificate states that the two orders have the same successor and pointwise-equal composed recovery functions. | A local sufficient condition related to Definition 19's effect independence.                                         | It does not construct the paper's transformation monoids or prove pairwise commutation and inverse-yield stability for every operation generated during an execution.                          |
| `TwoBatch.IndependentAt`                                                                       | **Proved by construction:** the two reversible effects satisfy the local certificate and each pure result is stable when evaluated after the other effect.                                 | A two-operation, result-sensitive analogue of the hypotheses used for Theorem 20.                                    | The batch has exactly two pure calls at one indexed start state. The result-stability fields are assumptions supplied in the certificate.                                                      |
| `CertifiedTwoBatch.execute_after_eq`, `execute_undo_eq`, `execute_applied_eq`                  | **Proved:** the model and swapped orders have the same modeled successor and the same complete recovery function, hence the same proof-carrying `Applied` value.                           | A finite two-order instance of the order-invariance intuition behind Theorem 20 and Corollary 21.                    | It does not prove arbitrary removal from an interleaved context or arbitrary permutation of an unbounded sequence.                                                                             |
| `execute_outputs_in_model_order`, `execute_eq`, `execute_order_irrelevant`, `execute_recovers` | **Proved:** either of the two permitted semantic orders returns the same model-ordered result pair and proof-carrying outcome, and that outcome recovers the indexed predecessor.          | A project-specific pure batching theorem; there is no numbered paper theorem about heterogeneous tool-result tuples. | `execute` is a pure function. It starts no tasks, performs no `IO`, and establishes no external-effect independence, data-race freedom, scheduling, cancellation, or parallel execution claim. |

The pinned Harness scheduler may overlap calls while committing results in model order, with
exclusive barriers and abort draining in [`tool-calls.ts`][harness-tool-scheduler]. The Lean
two-call theorem proves only the equality of two explicitly certified pure evaluations; it is
not a refinement theorem for that scheduler.

### `Cordis.Api`

Local source: [`Cordis/Api.lean`](../Cordis/Api.lean).

| Lean declaration                                      | Status and exact Lean guarantee                                                                                                            | Paper correspondence                                                                                    | Boundary                                                                                                                  |
| ----------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------- |
| `Signature`                                           | **Checked intrinsically:** an operation chooses its request type and the response type for that exact request.                             | Definition 24's typed operation argument/outcome families; §5.1.4's proposed compile-time mediation.    | Request-indexed responses are a Lean strengthening, not a numbered theorem in the paper.                                  |
| `Provider`, `Registry`                                | **Checked intrinsically:** a key can store only a provider for that key; a handle returns only the indexed response type.                  | §3.2.1, Definition 22's dependent partial function.                                                     | `Except String` is a pure model. It does not establish behavior of an external implementation.                            |
| `Needs`                                               | **Checked as a proposition:** capability membership is `sig.Op -> Prop`.                                                                   | §3.2.2, Definition 25's coeffect specification.                                                         | No finite-set representation or decidability is supplied here.                                                            |
| `Binding`, `View`, `provider_present`                 | **Proved:** resolving a declared operation returns a provider with equality evidence that it is present in the specific registry value.    | Definition 44's committed view, Definition 46's provider-valued target view, and §5.1.4 context access. | It does not compute a dynamic target, notify dependents, or prove an episode-level coherence theorem.                     |
| `SomeCall`, `AuthorizedCall`, `Reply`, `View.execute` | **Checked intrinsically:** a response remains indexed by the resolved operation and exact request; authorization is carried as `needs op`. | Harness-facing strengthening of typed coeffect access.                                                  | Name lookup, decoding, and authority decisions occur outside this module.                                                 |
| `Component`                                           | **Checked:** a component receives only a restricted `View`.                                                                                | Part of Definition 43's dependency-facing component interface.                                          | It omits Definition 43's declared provision and witnessed effect function, so it is not the paper's full component tuple. |

The standalone runtime enforces declarations dynamically through its proxy resolver in
[`reflect.ts`][cordis-reflect]: the resolver follows committed fiber stores and rejects
inactive or undeclared access. Lean moves the declared-operation check into the type of
`View.resolve`. No theorem proves that a JavaScript proxy access and a Lean `View.call`
select the same provider.

The pinned standalone `Context` derives isolation and interception scopes through inherited
tables in [`context.ts`][cordis-context]. The mapped Lean API has no corresponding realm or
metadata transformation.

### `Cordis.Registry`

Local source: [`Cordis/Registry.lean`](../Cordis/Registry.lean).

| Lean declaration                                            | Status and exact Lean guarantee                                                                                                      | Paper correspondence                                                            | Boundary                                                                                                                                                                            |
| ----------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `setAt`, `install`, `withdraw`, `setAt_same`, `setAt_other` | **Proved/checked:** a dependent update changes only the selected key and retains its indexed provider type.                          | Definitions 22–23, dependent table update and restriction.                      | Paper `set` requires absence and its inverse removes the new key. Lean permits replacement and records the overwritten `Option`, so the operations are not definitionally the same. |
| `setAt_restore`, `setEffect`, `setEffect_recovers`          | **Proved:** restoring the captured prior binding exactly recovers the whole registry.                                                | Definition 23 viewed as a witnessed effect and Definition 8's recovery witness. | The proof is about a pure functional registry, not mutable runtime aliases or external resources.                                                                                   |
| `setAt_commute`, `setEffect_commute`                        | **Proved:** fixed updates at distinct keys commute; equality of `Effect.seq` includes both successor and composed recovery function. | A concrete distinct-key commutation instance related to Theorem 40.             | It does not define transformation monoids, prove yielded-inverse stability for arbitrary operations, account for outcomes, or establish Theorem 40 in full.                         |
| `Satisfies`, `satisfiesNone`, `satisfiesOne`                | **Proved by construction:** satisfaction is a resolver yielding a present provider for each declared operation.                      | Definition 25's satisfaction predicate and Definition 46's provider resolution. | There is no activating/deactivating/neutral notification classifier from Definition 26.                                                                                             |

CORDIS's concrete provider registration, notification, and withdrawal live in
[`reflect.ts`][cordis-reflect]. A disposer deletes the public binding, notifies and awaits
affected fibers, then removes self-access. Lean's registry contains no notification or wait;
it proves only pure table facts.

### `Cordis.Tool`

Local source: [`Cordis/Tool.lean`](../Cordis/Tool.lean).

| Lean declaration                                | Status and exact Lean guarantee                                                                                                                         | Paper/upstream correspondence                                                                                                                                                          | Boundary                                                                                                                                                      |
| ----------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `ToolSpec`                                      | **Checked as a type:** input-dependent output/failure, precondition, postcondition, required capabilities, and an emission label are declared together. | Paper §1.2.2 motivates agent harnesses; Definition 24 supplies typed operations. Harness `ToolDefinition` has JSON schemas and an async body in [`tools/src/index.ts`][harness-tools]. | This contract shape is project-specific; the paper has no `ToolSpec` theorem.                                                                                 |
| `ToolSpec.Invocation`                           | **Proved by construction:** a value exists only with `precondition` and `authorized` proofs.                                                            | A static strengthening of Harness pre-dispatch policy and capability filtering.                                                                                                        | The propositions and granted-capability predicate are supplied by the caller.                                                                                 |
| `CertifiedOutcome`, `VerifiedTool`              | **Proved by construction:** a pure implementation must return a result, successor model, and the declared postcondition proof.                          | A proof-carrying counterpart to Harness's validated canonical tool outcome.                                                                                                            | No external I/O backend is verified. A bridge that constructs the outcome remains trusted unless separately proved.                                           |
| `ToolCatalog.signature`, `ToolCatalog.provider` | **Checked intrinsically:** a heterogeneous tool catalog becomes a dependent API without losing the request/result dependency.                           | Harness tool registry and schema projection.                                                                                                                                           | No theorem relates Lean tool names or schemas to a deployed Harness registry.                                                                                 |
| `Decision.tighten` and its algebraic theorems   | **Proved:** deny is absorbing, allow is the identity, and combination is commutative, associative, and idempotent.                                      | Harness `PreToolDecision = allow \| deny \| ask` and its monotonic guards in [`tools/src/index.ts`][harness-tools-policy].                                                             | Harness policy is an ordered async waterfall with reasons and approval routing. `tighten` is a small pure policy algebra, not a model of that whole pipeline. |
| `EmissionClass`                                 | **Checked only as data:** one of five labels is attached to a tool.                                                                                     | Paper §6.1's system-boundary discussion.                                                                                                                                               | Nothing in this module enforces idempotence, compensation, reversibility, or an OS sandbox from the label.                                                    |

The pinned Harness tool runtime additionally has scoped shadowing and restrictions,
fail-closed concurrency classification, cooperative cancellation, output-schema validation,
and pre/around/post execution stages. See its [registry view and scheduler-facing
classification][harness-tools-view] and [execution pipeline][harness-tools-execute]. None
of those runtime behaviors follows from `ToolSpec` or `Decision.tighten`.

### `Cordis.ToolWire`

Local source: [`Cordis/ToolWire.lean`](../Cordis/ToolWire.lean).

| Lean declaration                                                                                   | Status and exact Lean guarantee                                                                                                                                                                                                                          | Upstream correspondence                                                                                                                     | Boundary                                                                                                                                                                   |
| -------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `RawCall`, `AdmissionError`                                                                        | **Checked data:** the local untrusted call contains a textual name and an already-parsed `Lean.Json` arguments AST; failures distinguish unknown, undeclared, malformed, and contract-rejected calls.                                                    | Pinned Harness `tool/call` records a name, call ID, and the model's raw argument string in [`session/src/types.ts`][harness-session-types]. | Harness starts from a raw string; Lean starts after parsing. The error types and payloads are not equivalent.                                                              |
| `ToolWire.resolve`, `resolve_name`, `inputCodec`, `outputCodec`, `failureCodec`                    | **Proved/checked interface:** every catalog tool's declared name resolves to that tool, and codecs retain the selected operation and input indices.                                                                                                      | Harness registry name resolution and input/output schemas.                                                                                  | The integrator supplies the resolver and codecs. `resolve_name` covers catalog-generated names, not uniqueness of arbitrary aliases or agreement with a deployed registry. |
| `AdmissionEvidence`, `certifyAdmission`, `validate`, `validate_declared`                           | **Checked and proof-producing:** successful validation resolves a declared tool, decodes its exact input, and obtains its precondition and required-capability evidence before returning an `AuthorizedCall`; the returned call carries `needs call.op`. | A pure, fail-closed counterpart to Harness pre-dispatch admission and capability filtering.                                                 | `certifyAdmission` is supplied by the integrator. The module does not prove that its propositions reflect authenticated identity, live capabilities, or the outside world. |
| `resultCodec`, `decode_encoded_result`, `encodeCertifiedResult`, `decode_encoded_certified_result` | **Proved:** failure and success payloads retain the request-selected failure/output types, and every encoded result or certified outcome decodes to that exact dependent result.                                                                         | A typed local counterpart to Harness output validation and model-facing result materialization.                                             | These are one-way `Lean.Json` AST round trips. They do not cover parsing, rendering, schema semantics, transport, or equality with Harness result messages.                |

### `Cordis.Policy`

Local source: [`Cordis/Policy.lean`](../Cordis/Policy.lean).

| Lean declaration                                                                                                                 | Status and exact Lean guarantee                                                                                                                                                               | Upstream correspondence                                                                                                                   | Boundary                                                                                                                                                                      |
| -------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `LeasePool`, `issue`, `consume`, `consumed_absent`, `cannot_consume_twice`                                                       | **Proved/checked:** the represented list of available call IDs is duplicate-free; successful consumption removes the exact ID, so that returned pool cannot consume it again without reissue. | A project-specific pure lease layer adjacent to Harness call dispatch.                                                                    | Lean values are duplicable. The same pre-consumption pool can be copied and consumed independently, so this is not a linear capability or global atomic exactly-once theorem. |
| `SubjectPolicyResult`, `SubjectPolicyState`, `SubjectPolicyTransition`                                                           | **Checked intrinsically:** one exact subject is retained through proposal, decision, allowed dispatch, and subject-indexed completion or rejection; only `.allow` has a dispatch constructor. | Harness retains the exact tool call through its ordered pre/around/post policy pipeline in [`tools/src/index.ts`][harness-tools-execute]. | This finite state machine does not model approval routing, asynchronous waterfalls, monotonic guard enforcement, post-policy rewriting, or cancellation.                      |
| `SubjectPolicyTransition.phase_strict`, `dispatched_lease_absent`, `denied_cannot_dispatch`                                      | **Proved:** every transition advances phase; dispatch removes the exact ID's lease; a denied state has no dispatch edge.                                                                      | A proof-carrying local policy lifecycle, not a CORDIS paper theorem.                                                                      | These are constructor properties of the supplied subject, decision, result families, and pool.                                                                                |
| `SubjectPolicyTrace.dispatchCount_le_one`, `dispatchCount_to_completed`, `cannot_dispatch_twice`, `denied_dispatchCount_eq_zero` | **Proved:** one explicitly threaded trace has at most one dispatch edge; proposal-to-completed has exactly one; a trace starting denied has none.                                             | A local path property related to Harness's requirement that an admitted call enter dispatch once.                                         | The quantification is over one trace value. It does not exclude duplicated Lean states, separate workers, retries, crashes, or re-execution by an external runtime.           |

The earlier ID-only `PolicyState` and `PolicyTransition` remain a smaller compatibility model.
The exact-subject trace is the stronger claim surface; even there, “at most once” must always be
qualified as “within one explicitly threaded trace.”

### `Cordis.Protocol`

Local source: [`Cordis/Protocol.lean`](../Cordis/Protocol.lean).

| Lean declaration                                                                                  | Status and exact Lean guarantee                                                                                                                                                                 | Upstream correspondence                                                                                   | Boundary                                                                                                                                                                                                              |
| ------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `SessionState`, `Event`, `Trace`                                                                  | **Checked intrinsically:** adjacent states must match; tool calls require a fresh identifier; results require pending membership; a step can end only with no pending calls.                    | Harness turn, step, call, and result event vocabulary in [`session/src/types.ts`][harness-session-types]. | This is a deliberately smaller and stricter protocol, not the full merge-extensible Harness event map.                                                                                                                |
| `SessionState.WellFormed`, `Event.preservesWellFormed`, `Trace.preservesWellFormed`               | **Proved:** pending identifiers remain duplicate-free over typed events and traces.                                                                                                             | Related to the optional Harness session invariant companion.                                              | Harness uses a `Set` and does not reject a repeated `tool/call` identifier at insertion.                                                                                                                              |
| `Event.noOrphanResult`                                                                            | **Proved:** a typed result event carries predecessor-membership evidence.                                                                                                                       | Harness normally requires a same-step pending call before a result.                                       | Harness has explicit exceptions for repair-generated not-started results and surface replacements.                                                                                                                    |
| `applyRaw`, `replayRaw`                                                                           | **Checked executable boundary:** the six runtime events are validated for phase, number, duplicate, orphan, and pending-call errors.                                                            | Harness optional relational validator in [`session/src/invariant.ts`][harness-session-invariant].         | These functions return erased runtime state. Use `validateEvent` or `validateTrace` when a dependent witness is required.                                                                                             |
| `ValidatedEvent`, `validateEvent`, `ValidatedTrace`, `validateTrace`, `validateRuntimeTrace`      | **Checked and proof-producing:** successful validation returns an intrinsic `Event` or `Trace`, its exact indexed finish state, and a proof that erasure equals the supplied raw event or list. | A constructive validator for the local six-event subset.                                                  | It does not accept or reconstruct the full merge-extensible Harness event map, and no translation or equivalence theorem connects it to persisted Harness sessions.                                                   |
| `applyRaw_eraseEvent`, `ValidatedEvent.applies`, `replayRaw_eraseTrace`, `ValidatedTrace.replays` | **Proved:** typed events/traces and successfully reconstructed raw events/traces are accepted by the erased validator at their indexed endpoints.                                               | An internal refinement between Lean's intrinsic and erased protocol representations.                      | No theorem states equivalence to Harness validation, surface rules, sequence metadata, or durability. The source does not state a converse equating every successful `replayRaw` result with `validateTrace` success. |
| `Trace.erase_append`                                                                              | **Proved:** typed trace composition erases to list concatenation.                                                                                                                               | Append-only log composition.                                                                              | It says nothing about persistence, sequence numbers, timestamps, surface metadata, or crash durability.                                                                                                               |

#### Exact differences from the pinned Harness protocol

| Topic                       | Lean protocol                                                       | Pinned Harness                                                                                                                                    |
| --------------------------- | ------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| Initial numbers             | A caller chooses `.ready n`; `turnStart n` starts step counter `0`. | A fresh invariant trace expects turn `1` and step `1` ([initialization][harness-session-invariant]).                                              |
| Duplicate call ID           | Rejected and impossible in typed `Event.toolCall`.                  | `pendingCalls.add(callId)` does not reject an existing ID.                                                                                        |
| Step end with pending calls | Rejected by `pendingCallsRemain`; typed `stepEnd` requires `[]`.    | The invariant companion clears `pendingCalls` at `step/end`.                                                                                      |
| Orphan result               | Always rejected by the six-event validator.                         | Normally rejected, with exceptions for a synthetic repair result and non-append surface replacement.                                              |
| Other events                | No other event can occur in a typed trace.                          | User, assistant, request, seed, todo, and plugin-merged events share the log.                                                                     |
| Always-on checks            | `applyRaw` performs the listed relational checks whenever called.   | `Session.append` always checks lossless JSON and surface rules, but relational turn/step checks require loading the optional invariant companion. |

Harness `Session.append` snapshots and freezes JSON before appending, assigns sequence and
time fields, validates surface operations, and publishes observers; see
[`session/src/index.ts`][harness-session-append]. Those checks are outside the Lean protocol.

Harness dispatch may overlap calls but commits results and result contexts in model order,
uses exclusive barriers, drains started calls on abort, and emits synthetic pairs for skipped
calls. That behavior is in [`agent-loop/src/tool-calls.ts`][harness-tool-scheduler].
`Cordis.Protocol` proves no concurrency, ordering, cancellation, or exactly-once durability
property for that scheduler.

### `Cordis.Stream`

Local source: [`Cordis/Stream.lean`](../Cordis/Stream.lean).

| Lean declaration                                                                                            | Status and exact Lean guarantee                                                                                                                                                                                                | Upstream correspondence                                                                                                                         | Boundary                                                                                                                                                                              |
| ----------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `State`, `Chunk`, `Trace`, `noChunkAfterFinished`                                                           | **Checked intrinsically/proved:** an open text stream carries its exact accumulator and remaining chunk budget; a text chunk consumes one unit; finish preserves the accumulator; no typed chunk starts from a finished state. | Pinned Harness adapters expose a richer `StreamChunk` union and promise a terminal finish in [`llm/src/types.ts`][harness-llm-stream].          | Lean models only text fragments plus an unlabelled finish. It omits block indexes, reasoning, tool-call deltas, usage, finish reasons, replay state, and adapter error normalization. |
| `applyRaw`, `replayRaw`, `applyRaw_budgetExhausted`, `applyRaw_text_after_finish`, `applyRaw_double_finish` | **Checked/proved for the local runtime mirror:** over-budget text, text after finish, and a second finish are rejected.                                                                                                        | Harness stores raw `assistant/chunk` events in the session vocabulary and checks their turn/step placement in its optional invariant companion. | The Lean validator receives already-decoded `String` fragments and has no turn/step, byte decoding, provider, network, cancellation, or persistence semantics.                        |
| `applyRaw_eraseChunk`, `replayRaw_eraseTrace`                                                               | **Proved:** erasing a typed local chunk or trace is accepted at its indexed erased endpoint.                                                                                                                                   | An internal typed-to-runtime refinement, not a paper theorem.                                                                                   | Unlike `Cordis.Protocol.validateTrace`, the stream module does not reconstruct an existential typed trace from an arbitrary accepted raw chunk list.                                  |
| `assemble`, `textTrace`, `completeTrace`, `replay_textTrace`, `replay_completeTrace`                        | **Proved:** the deterministic trace for a finite list of strings consumes exactly that list's length as budget and finishes with its left-to-right concatenation.                                                              | A small text-only reference behavior adjacent to Harness stream assembly.                                                                       | It is not a correctness proof for Harness's block assembler, raw chunk persistence, tool-argument assembly, or provider adapters.                                                     |

### `Cordis.Lifecycle`

Local source: [`Cordis/Lifecycle.lean`](../Cordis/Lifecycle.lean).

| Lean declaration                                                     | Status and exact Lean guarantee                                                                                                                                                                                                                                                                                    | Paper correspondence                                                                  | Boundary                                                                                                                                                                                                         |
| -------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `State`                                                              | **Checked intrinsically:** inactive retains a current modeled value and outcome; reloading, active, and unloading carry an origin, current model, indexed undo stack, and committed view; reloading also carries an abstract iterator. The `unload` endpoint records the recovered `origin` as the inactive model. | §4.3, Definition 49; §5.1.3.                                                          | The paper gives the iterator recursive witnessed semantics, and its global context also carries retirement, fiber tables, dependency/provision sets, and an error calculus. Lean abstracts or omits those parts. |
| `CommittedView`, `Consumer`, `Withdrawable`                          | **Checked/propositional:** the guard quantifies over the supplied consumer list and rejects an installed consumer whose supplied view resolves to the provider.                                                                                                                                                    | Definitions 44 and 50 and the `L-Unload` guard.                                       | Completeness and freshness of the consumer snapshot, truth of `installed : Bool`, and provider existence are assumptions of the input data.                                                                      |
| `Transition.begin`, `iterate`, `finish`, `divert`, `leave`, `unload` | **Checked intrinsically:** only the encoded phase changes can be constructed; iteration pushes an `Effect`; unloading requires a `Withdrawable` proof.                                                                                                                                                             | The local shape of the §4.3 lifecycle rules.                                          | `begin` does not require target satisfaction, `iterate` is synchronous and accepts an arbitrary next iterator, and the trace fixes one consumer snapshot. It is not the paper calculus.                          |
| `Transition.unload_recovers`                                         | **Proved:** an `unload` constructor ends at `.inactive origin outcome`, and its indexed stack recovers the activation's exact `origin` from the unloading `current`.                                                                                                                                               | A local, non-interleaved analogue of Theorem 61 and Corollary 62.                     | Paper recovery quantifies over pairwise-independent interleaved steps and preserves foreign effects up to observational equivalence. None of those hypotheses or conclusions appears here.                       |
| `Transition.unload_rejects_relied`                                   | **Proved:** given membership, installed, and reliance evidence in the supplied list, an unload transition yields `False`.                                                                                                                                                                                          | Definition 50 and the premise of `L-Unload`; it supports the intuition of Theorem 63. | Theorem 63's episode nesting, provision availability, and stable binding values are not proved.                                                                                                                  |
| `Transition.active_successor_keeps_view`                             | **Proved:** the only active successor is unloading with exactly the same committed view and undo stack.                                                                                                                                                                                                            | A narrow structural fragment of Definition 44 and Theorem 64.                         | Theorem 64 covers one committed resolution throughout every iteration and its finish/divert dichotomy. This theorem covers only the `active -> unloading` edge.                                                  |
| lifecycle `Trace`, `Trace.append`                                    | **Checked intrinsically:** adjacent lifecycle endpoints match.                                                                                                                                                                                                                                                     | §4.4 indexed step sequences.                                                          | It has no global registry, orchestration, fairness, progress, or confluence theorem.                                                                                                                             |

The standalone CORDIS implementation commits a provider snapshot during reload and reads
through that snapshot in its proxy resolver; see [`fiber.ts`][cordis-fiber] and
[`reflect.ts`][cordis-reflect]. Its provider removal notifies and waits for affected fibers
before deleting the provider's self-view. The Harness-vendored fiber contains additional
lifecycle hardening documented in the [vendor manifest][harness-vendor]. The Lean
lifecycle does not prove equivalence to either asynchronous implementation.

### `Cordis.Harness` and `RecordChain`

Local source: [`Cordis/Harness.lean`](../Cordis/Harness.lean).

This namespace is the repository's deterministic Lean reference runner. “Harness” below does
not mean that the pinned TypeScript DeepSeek Harness has been imported or verified.

| Lean declaration                                                                                                                 | Status and exact Lean guarantee                                                                                                                                                                                                                                                                                                                                                                                    | Upstream correspondence                                                                                                        | Boundary                                                                                                                                                                                                                                                                  |
| -------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `CallEvidence`                                                                                                                   | **Proved by construction:** a record either retains the exact failed `validateRaw` equation with unchanged model/leases, or retains the admitted dependent call, validation equation, lease issue, exact provider completion, execution equation, and an exact-subject policy trace ending in that call's completion.                                                                                              | A proof-carrying local counterpart to admission, policy, execution, and result settlement in the pinned Harness tool pipeline. | The model is the pure counter example. The constructor does not verify external I/O, TypeScript execution, cancellation, approval, or persistence.                                                                                                                        |
| `CallEvidence.encodedResult`, `CallRecord.encodedResult`                                                                         | **Checked by dependent elimination:** an encoded result exists only for successful provider execution and is produced with the selected call's operation, request, and certified outcome. `ToolWire.decode_encoded_certified_result` supplies the AST round-trip theorem.                                                                                                                                          | Related to Harness output validation and model-facing tool-result materialization.                                             | Admission rejection and provider-level failure have no encoded dependent provider result. The JSON AST is not a Harness `ToolResultMessage` or durable wire payload.                                                                                                      |
| `CallBoundary`, `RuntimeEvent.callBoundary?`, `callBoundaries`, `recordBoundaries`                                               | **Checked executable projection:** `callBoundaries` retains the ordered tool-call and tool-result IDs from a local runtime log, erasing turn/step coordinates; `recordBoundaries` emits exactly one adjacent call/result pair for every record ID.                                                                                                                                                                 | A deliberately small index of the pinned Harness's call/result event order.                                                    | The projection does not include payloads, sequence metadata, timestamps, surfaces, or any event outside the six-variant local protocol.                                                                                                                                   |
| `RecordChain`                                                                                                                    | **Proved by construction:** the six-index relation threads initial model, next numeric call ID, ordered records, final model, final lease pool, and ordered call/result boundaries. Every appended record has the next ID, starts at the preceding modeled successor and lease pool, and contributes exactly its call/result pair.                                                                                 | The pinned scheduler commits results in model order.                                                                           | It is a sequential pure history relation, not a proof about concurrent completion, external side effects, durable transactions, or the TypeScript scheduler.                                                                                                              |
| `RecordChain.length_eq_nextCall`, `RecordChain.ids_eq_range`, `RecordChain.boundaries_eq_records`, `RecordChain.leases_threaded` | **Proved:** a certified history has one record per allocated ID; IDs are exactly `0, ..., nextCall - 1`; its boundary index equals `recordBoundaries records`; and record lease endpoints thread from `LeasePool.empty` to the indexed final pool.                                                                                                                                                                 | Local ordered call accounting and lease continuity.                                                                            | These theorems do not prove durable uniqueness, atomic storage, or global exactly-once behavior across processes, duplicated values, retries, forks, or crashes.                                                                                                          |
| `RunnerState.history`, `RunnerState.callBoundaries_eq_records`, `RunnerState.leases_threaded`                                    | **Proved by the stored index and public consequences:** `history` is a `RecordChain initialModel nextCall records model leases (callBoundaries log)`. Thus the log's call/result-ID projection equals the records' exact ordered pairs, and lease endpoints thread from the empty pool to `RunnerState.leases`.                                                                                                    | A local proof-carrying correspondence between the reference runner's event projection and audit records.                       | This does not translate full Harness events, prove payload equivalence, or establish persistence, external-effect agreement, or crash recovery.                                                                                                                           |
| `RunnerState.replayProof`, `replayRaw_append`, private `emitNonBoundary`                                                         | **Proved by a stored field and supporting theorem:** every returned runner state replays its full local log from `.ready 0` to its stored protocol state. Structural turn/step events are appended only through an internal helper requiring proof that they are not call boundaries.                                                                                                                              | Append/replay discipline adjacent to Harness's session log.                                                                    | There is no public arbitrary event-emission function. The log still has no Harness sequence numbers, timestamps, surfaces, storage flush, or crash guarantee.                                                                                                             |
| `RunnerState.dispatch`, private `settle`, `dispatchAll`, `runTurn`, `runTurns`                                                   | **Checked/proved deterministic path:** admitted records carry explicit allow/dispatch/settle `SubjectPolicyTrace` values. On successful dispatch, the private settlement helper returns one new state containing the call event, matching result event, appended record, successor model, successor lease pool, next ID, and extended certificates; no intermediate returned `RunnerState` contains only the call. | A small executable reference path shaped like Harness turn/step/call/result bracketing and model-ordered commit.               | “Atomic” here describes construction of one pure Lean value. Rejected admissions have no admitted-subject trace. The runner starts no tasks, performs no tool `IO`, overlaps no calls, and provides no database, process, crash, or DeepSeek Harness atomicity guarantee. |
| `certifiedTwoCallTrace`, `certifiedTwoCallTrace_replays`                                                                         | **Proved:** one static two-call trace erases and replays from `.ready 0` to `.ready 1`.                                                                                                                                                                                                                                                                                                                            | Executable illustration of the local intrinsic protocol.                                                                       | One fixed trace is not coverage of arbitrary Harness sessions.                                                                                                                                                                                                            |

Together, `history` and `replayProof` rule out divergence among the local protocol endpoint,
modeled counter history, allocated IDs, record lease chain, final lease pool, and the
call/result-ID projection of the in-memory log. This is a joint invariant of constructible
`RunnerState` values. It is not an invariant of persisted Harness sessions or external tool
effects, and it does not turn pure lease values into linear, process-global capabilities.

### `Cordis.GenericSessionHarness`

Local sources: [`Cordis/GenericSessionHarness.lean`](../Cordis/GenericSessionHarness.lean) and
[`Cordis/Examples/DependentChoiceSession.lean`](../Cordis/Examples/DependentChoiceSession.lean).

`GenericSessionHarness.RunnerState` factors the rich-session wrapper over an arbitrary
`GenericHarness.Config`. Its request header, user/assistant surface messages, tool-call/result
events, and turn/step boundaries are appended to one `Session`, while
`protocolProjection_eq_log` and `protocolProjection_replays` tie that rich value to the
generic runner's structural log. The dependent-choice fixture is a second, non-counter
instantiation: its `Bool` request selects a `Nat` or `String` result, the revision call is
admitted, the label call is rejected by policy, and both records remain in a reconstructible
model request.

This is a finite pure in-memory bridge. It does not prove transport, persistence, external
tool execution, scheduling, crash recovery, or equivalence with the deployed TypeScript
Harness.

### `Cordis.Codec`

Local source: [`Cordis/Codec.lean`](../Cordis/Codec.lean).

| Lean declaration                                | Status and exact Lean guarantee                                                                             | Upstream correspondence                                                              | Boundary                                                                                                                                                                          |
| ----------------------------------------------- | ----------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `DecodeError`, `PathSegment`, `JsonKind`        | **Checked data:** decoding failures identify a type/length error and an AST path.                           | Harness receives raw tool arguments and validates schemas at runtime.                | The error vocabulary does not model all JSON Schema failures or Harness error payloads.                                                                                           |
| `Codec`, `decode_encode`                        | **Proved by field:** `decode (encode value) = .ok value` at the in-memory `Lean.Json` AST boundary.         | A proof-carrying replacement for a subset of Harness's schema/validation convention. | There is no numbered CORDIS paper theorem about JSON codecs. The theorem is one-way and does not prove schema soundness, completeness, parser correctness, or wire compatibility. |
| `unit`, `bool`, `string`, `nat`, `prod`, `list` | **Proved:** every constructed codec discharges the same AST round trip, including recursive products/lists. | Common tool-wire data shapes.                                                        | `nat` accepts the exact canonical nonnegative exponent-zero AST form used by its encoder. Object, optional, sum, arbitrary number, and named-field codecs are absent.             |
| `schema` field                                  | **Trusted descriptive metadata.**                                                                           | Harness exposes parameter/output JSON Schema to model and runtime.                   | No Lean proposition connects `schema` to `encode` or `decode`, and an external producer is not proved to follow it.                                                               |

Harness model calls carry the raw argument string unchanged in the session event and parse it
later for dispatch. Its tool registry snapshots lossless JSON, validates input/output schemas,
and materializes failures. `Codec` begins after parsing, at `Lean.Json`; it is neither a parser
nor a proof about Harness's TypeScript schema engine.

### Current-development coeffect and unified-context layer

Local sources: [`Cordis/Coeffect.lean`](../Cordis/Coeffect.lean),
[`Cordis/UnifiedContext.lean`](../Cordis/UnifiedContext.lean), and
[`Cordis/ContextualEquivalence.lean`](../Cordis/ContextualEquivalence.lean),
[`Cordis/OperationalEquivalence.lean`](../Cordis/OperationalEquivalence.lean),
[`Cordis/QuotientEffect.lean`](../Cordis/QuotientEffect.lean), and
[`Cordis/CoeffectQuotient.lean`](../Cordis/CoeffectQuotient.lean).

| Lean declaration                                                                                                        | Status and exact Lean guarantee                                                                                                                                                                                 | Paper correspondence                        | Boundary                                                                                                       |
| ----------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------- | -------------------------------------------------------------------------------------------------------------- |
| `Coeffect.Context`, `Present`, `Absent`, `get`, `setEffect`, `removeEffect`                                             | **Checked/proved:** key selection fixes the stored value type; witnessed insertion/removal has a concrete local inverse and exact recovery.                                                                     | Definitions 22–23.                          | Finite pure dependent maps; no external registry, persistence, or alias identity.                              |
| `CoeffectAt`, `CoeffectAt.lift`, `Spec`, `Satisfies`, `notify`                                                          | **Checked/proved:** key-local operations carry typed inputs/outcomes and equivalence-respect laws; finite satisfaction and three-way notifications are exact.                                                   | Definitions 24–26.                          | The operation record and decidability assumptions are supplied; no global component reacts automatically.      |
| `UnifiedContext.InPlace`, `Derived`                                                                                     | **Checked/proved:** in-place values carry a witnessed inverse; a derived child retains its unchanged parent as an index and discards back to it.                                                                | Definition 27.                              | Pure Lean values do not establish imperative alias identity.                                                   |
| `IsolatedContext`, `resolve`, `isolate`, isolation `setEffect`                                                          | **Checked/proved:** routing selects a realm-indexed value type, distinct own realms are intrinsic, derived overrides inherit the store, and resolved set recovers.                                              | Definitions 28–29.                          | Finite routing/store model; it does not prove runtime tenant isolation or confinement.                         |
| `MetadataAlgebra`, `InterceptionContext`, `getDeclared`, `intercept`, interception `setEffect`                          | **Checked/proved:** each key has its own metadata monoid/provider type; declared/context metadata order, distinct-key commutation, and recovery are exact.                                                      | Definitions 30–31.                          | Monoid semantics and right-bias behavior are integrator-supplied; no middleware/runtime execution is verified. |
| `Approximation`, `Layer.record`, `Layer.liftCoeffect`, `pushApproximation`                                              | **Checked/proved:** any selected finite unfolding contains state, accumulator, and coeffects; effects record LIFO and coeffect transitions lift and recover.                                                    | Finite unfoldings of Definition 32.         | The fixed point has a negative occurrence in `Gamma -> Gamma`; no equirecursive fixed point is claimed.        |
| `Coeffect.Observational.Related`, `related_iff`, `contextSetoid`, `satisfies_iff_of_related`, `notify_eq_of_related`    | **Proved:** relation is exactly equal presence domain plus pointwise key relations; satisfaction and notification descend to the finite-context quotient.                                                       | Finite-context part of Definition 33.       | Operation-test and quotient-effect results are separate rows; global component state is absent.                |
| `OperationalEquivalence.observe`, `Indistinguishable`, `indistinguishable_admissible`, `contained_in_indistinguishable` | **Checked/proved:** finite typed words execute partial operations and concrete seeded inverses, retain heterogeneous outcomes, form an equivalence, and define the largest fixed-generator-admissible relation. | Definition 34 and generator-level Lemma 35. | The paired comparison of different inverses does not follow from same-word tests.                              |
| `PairedInverseCoherent`, `withOperationalEquivalence`, `PairedGap.pairedInverseCoherent_fails`                          | **Proved:** a full `CoeffectAt` over test indistinguishability needs an extra paired-inverse premise; a finite model proves that premise is not derivable in general.                                           | Boundary between Definitions 24 and 34–35.  | This is a discovered formal gap/choice, not a proof that the paper is inconsistent under every interpretation. |
| `Observational.Quotient.Respects`, `MapRelated`, `Admissible`, `Program.run_admissible`, `Program.recovers`             | **Proved:** quotient-respecting maps/effects compose; every certified finite word retains a respecting accumulated inverse and recovers its start up to the relation.                                           | Definitions 36–37 and finite Lemma 38 core. | It does not restate every Section 3.1 theorem or build the transformation monoids used later.                  |
| `Coeffect.Quotient.lift_results_related`, `liftApplied`                                                                 | **Proved:** related local-operation applications lift to related whole-context successors and inverse maps with equal typed outcomes.                                                                           | Definition 24 lift read through 33 and 37.  | Generator-level bridge only; no operation-monoid independence or Theorem 40/42.                                |

### Current-development rich stream and TypeScript refinement

Local sources: [`Cordis/RichStream.lean`](../Cordis/RichStream.lean),
[`Cordis/RuntimeRefinement.lean`](../Cordis/RuntimeRefinement.lean),
[`Cordis/RuntimeFailureRefinement.lean`](../Cordis/RuntimeFailureRefinement.lean),
[`Cordis/RuntimeOutcomeRefinement.lean`](../Cordis/RuntimeOutcomeRefinement.lean),
[`Cordis/RuntimeOutcomeSession.lean`](../Cordis/RuntimeOutcomeSession.lean),
[`Cordis/SessionRefinement.lean`](../Cordis/SessionRefinement.lean), and
[`Cordis/HarnessPersistenceRefinement.lean`](../Cordis/HarnessPersistenceRefinement.lean), and
[`Cordis/DeepSeekApi.lean`](../Cordis/DeepSeekApi.lean),
[`Cordis/DeepSeekRequestMode.lean`](../Cordis/DeepSeekRequestMode.lean),
[`Cordis/LoaderHMR.lean`](../Cordis/LoaderHMR.lean),
[`Cordis/DeepSeekCurlTransport.lean`](../Cordis/DeepSeekCurlTransport.lean),
[`Cordis/DeepSeekCurlStream.lean`](../Cordis/DeepSeekCurlStream.lean),
[`Cordis/DeepSeekCurlSession.lean`](../Cordis/DeepSeekCurlSession.lean),
[`Cordis/DeepSeekCurlIncremental.lean`](../Cordis/DeepSeekCurlIncremental.lean),
[`Cordis/DeepSeekStream.lean`](../Cordis/DeepSeekStream.lean), and
[`Cordis/DeepSeekRichStream.lean`](../Cordis/DeepSeekRichStream.lean), and
[`Cordis/DeepSeekRichToolStream.lean`](../Cordis/DeepSeekRichToolStream.lean), and
[`Cordis/DeepSeekRichMixedStream.lean`](../Cordis/DeepSeekRichMixedStream.lean), and
[`Cordis/DeepSeekRichMultiStream.lean`](../Cordis/DeepSeekRichMultiStream.lean), and
[`Cordis/DeepSeekSessionBridge.lean`](../Cordis/DeepSeekSessionBridge.lean), and
[`Cordis/DeepSeekSessionRunner.lean`](../Cordis/DeepSeekSessionRunner.lean), and
[`Cordis/DeepSeekApiSession.lean`](../Cordis/DeepSeekApiSession.lean).

| Lean declaration                                                                                                                                                                                        | Status and exact Lean guarantee                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Current Harness correspondence                                                                                                                                                    | Boundary                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `RichStream.Event`, `Trace`, `ValidatedTrace`, `AlignedMetadata`                                                                                                                                        | **Checked/proved:** typed/raw streams preserve block-kind deltas, exact assembly, first-seen ordering, terminal usage discipline, and metadata length.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Current block-stream and assembler vocabulary at Harness `99f6f02`.                                                                                                               | The local semantic contract is stricter; images, tool-result blocks, transport, and replay pruning are outside `RichStream`.                                                                                                                                                                                                                                                                                                                                                 |
| `RuntimeRefinement.decodeChunk`, `decodeChunks`, `SafeNat`, `WireUsage.toLocal`                                                                                                                         | **Checked executable boundary:** supported current fields/tags decode path-sensitively; IDs/raw arguments are unchanged; unsafe integers fail closed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Current [`StreamChunk` and usage fields][harness-current-stream-types].                                                                                                           | Starts from `Lean.Json`, not bytes. Missing optional usage counts become zero at one named local normalization.                                                                                                                                                                                                                                                                                                                                                              |
| `RuntimeRefinement.ValidatedJsonTrace`, `validateJsonTrace`, `replay_eq`                                                                                                                                | **Checked/proved:** success retains exact decoded chunks and a real intrinsic `ValidatedTrace` whose erased replay reaches its indexed endpoint.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Supported-subset JSON-AST-to-semantics refinement for Harness `99f6f02`.                                                                                                          | Opaque replay state, image/tool-result blocks, and upstream `LlmFailure` shapes are rejected. No completeness or whole-runtime equivalence follows.                                                                                                                                                                                                                                                                                                                          |
| `LoaderHMR.reconcile`, `classify`, `detectStale`, `transactionalReload`                                                                                                                                 | **Checked/proved:** Definition 74 entries reconcile by stable ID; Algorithm 8 reaches an accepted/declined fixed point with unresolved cycles declined; Algorithm 9 respects declined dependency boundaries; Algorithm 10 stages reload and restores the exact prior state on import failure.                                                                                                                                                                                                                                                                                                                                                                                     | Loader entry/config/HMR vocabulary from the pinned CORDIS/Harness implementation.                                                                                                 | No dynamic-import execution, filesystem watching, real fiber disposal, OS cache mutation, or deployed loader/HMR equivalence.                                                                                                                                                                                                                                                                                                                                                |
| `RuntimeFailureRefinement.ValidatedFailureTrace`, `validateFailureTrace`, `decoded_exact`                                                                                                               | **Checked/proved:** a final normalized `error` or `aborted` finish retains the exact ordinary decoded prefix and every `LlmFailure` field, including optional status, retry delay, and request id; terminal-after-failure and malformed metadata are rejected.                                                                                                                                                                                                                                                                                                                                                                                                                    | Current Harness in-band `StreamChunk` failure-finish union at `99f6f02`.                                                                                                          | A separate failure certificate, not a `RichStream.ValidatedTrace`: no open-block reconstruction, retry/cancellation policy, provider authenticity, or normal rich/session projection.                                                                                                                                                                                                                                                                                        |
| `RuntimeOutcomeRefinement.ValidatedOutcome`, `validateOutcome`                                                                                                                                          | **Checked/proved:** successful and normalized failure inputs dispatch into one dependent outcome without erasing either certificate; inputs rejected by both retain the successful-language and failure-language errors together.                                                                                                                                                                                                                                                                                                                                                                                                                                                 | The supported success/failure union at the current Harness `StreamChunk` boundary.                                                                                                | Dispatcher only: no retry/cancellation policy, open-block reconstruction, session-message append, provider authenticity, or whole-runtime equivalence.                                                                                                                                                                                                                                                                                                                       |
| `RuntimeOutcomeSession.validateAndDispatch`, `DispatchResult`, `appendJson`                                                                                                                             | **Checked/proved:** successful validated JSON is finished and appended through the existing pure session runner; normalized failure retains its exact terminal certificate and unchanged runner.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Policy-free local session boundary after the current-Harness outcome dispatcher.                                                                                                  | No fabricated failure message, retry/cancellation choice, source-event synthesis, persistence, provider authenticity, or whole-runtime equivalence.                                                                                                                                                                                                                                                                                                                          |
| `SessionRefinement.RefinedEvent`, `ValidatedSequence`, `ValidatedJsonLog.projection_exact`                                                                                                              | **Checked/proved:** supported source-shaped events retain wire seq/time, derive step/turn state and fresh call IDs from the accepted prefix, carry exact rich append/replacement witnesses, admit selected request headers, route context, whole-list todo snapshots, empty seed markers, and text/reasoning index-zero assistant chunks, and admit text/reasoning/image blocks plus complete assistant tool-call blocks whose IDs/provider metadata remain in `State.wireSurface`; later call/result events reuse the allocated local ID, while runtime events additionally carry intrinsic-protocol witnesses whose complete projections agree.                                 | Current Harness [`SessionEvent` envelope and event map][harness-current-session-types] at `99f6f02`.                                                                              | Unsupported event kinds/fields, unsupported header configuration, unknown todo statuses, nonempty seed payloads, malformed replacement ranges or incomplete source coverage, multimodal tool-result blocks, unsupported chunk kinds, replay state, and non-equivalent payloads fail closed; reasoning and image surface blocks remain wire-visible and are omitted from the smaller local projection. No complete event-union, persistence, or behavioral-equivalence claim. |
| `SessionRefinement.SurfaceCodec.decode_encode`, `decodeWireEventsText_of_encoded`, `decodeWireEventsBytes_of_encoded`, `decode_sanitized_toolResultEventWithMetadata`                                   | **Checked/proved/exercised:** canonical encoding is the exact reverse boundary for text-only `user/message`, text/reasoning/tagged-raw-image/complete-tool-call `assistant/message`, and singleton-text `tool/result` payloads; safe integers, assistant usage, `sourceEventSeqs`, `surfaceOp`, provider/source call IDs, raw tool-call arguments, `isError`, and a metadata-aware raw `error`/`meta` emitter are preserved through AST, JSONL text, and UTF-8 byte composition, with executable assistant/user/tool/image/metadata fixtures and invalid-UTF-8 rejection.                                                                                                         | Canonical local source-shaped AST/text/byte codec over the existing semantic decoder; tagged image objects and tool metadata are preserved as opaque raw JSON until sanitization. | Image schema semantics, provider/tool metadata semantics, unsupported surface operations, unsupported event variants, logger serialization identity, and deployed Harness compatibility remain fail-closed or external.                                                                                                                                                                                                                                                      |
| `SessionOpaqueMetadata.validateLogRetainingMetadata`, `metadata_example_projection`, `metadataOf_toolResultEventWithMetadata`                                                                           | **Checked/proved:** only provider/tool-owned `tool/result.data.error` and `tool/result.data.meta` are removed before the existing validator; their exact source JSON is retained in order, and the sanitized Session/Protocol projection is certified. The reverse codec emits those values without assigning them local semantics, while `sanitize_toolResultEventWithMetadata` proves that the existing tool-result decode is recovered.                                                                                                                                                                                                                                        | Current tool-result metadata fields are retained as opaque raw JSON.                                                                                                              | No provider/tool schema semantics, replay/resume behavior, complete event-union coverage, persistence, or whole-session equivalence.                                                                                                                                                                                                                                                                                                                                         |
| `TextRefinement.parseJsonLinesBytes`, `validateStreamBytes`, `validateSessionBytes`, `validateFailureBytes`                                                                                             | **Checked/proved at the adapter boundary:** UTF-8 JSONL text becomes exact AST lines, then the existing stream/session/failure validators return dependent certificates retaining the parsed source.                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Text/JSONL ingress before the supported Harness refinements.                                                                                                                      | Lean's JSON parser/printer are explicit library boundaries; no deployed JSONL schema, logger framing, timestamp truth, transport, or whole-runtime equivalence is claimed.                                                                                                                                                                                                                                                                                                   |
| `HarnessPersistenceRefinement.validatePersistedJson`, `ValidatedPersistedJson.projection_exact`                                                                                                         | **Checked/proved:** a session header and storage rows are split exactly; verbatim events and the three pinned packed-row tags expand losslessly at the AST level, safe sequence/time reconstruction is checked, and successful expansion composes with the stateful session projection theorem.                                                                                                                                                                                                                                                                                                                                                                                   | Harness JSONL [`format.ts`][harness-jsonl-format] and [`chunk-rows.ts`][harness-jsonl-chunks] at `99f6f02`.                                                                       | Logical JSON-AST subset only. Compression, UTF-8/file framing, path/index metadata, torn-tail repair, and complete persistence/session equivalence are not claimed.                                                                                                                                                                                                                                                                                                          |
| `HarnessPersistenceIO.readValidated`, `replaceRows`, `appendValidatedRow`, `ReadCertificate`                                                                                                            | **Checked/exercised:** UTF-8 bytes from memory or filesystem backends retain exact bytes/text/rows and the logical persistence/session certificate; replacement can be revalidated, and append-only rows are accepted only after the existing document validates.                                                                                                                                                                                                                                                                                                                                                                                                                 | Executable adapter above the logical Harness JSONL refinement.                                                                                                                    | Host acknowledgement remains separate from semantics: no canonical deployed renderer, fsync, stable media, locking, torn-tail repair, crash durability, or complete storage-union compatibility is proved.                                                                                                                                                                                                                                                                   |
| `HarnessPersistenceBytes.validatePersistedBytes`, `ValidatedPersistedBytes.projection_exact`                                                                                                            | **Checked/proved:** a pure `ByteArray` witness retains source bytes, decoded text, parsed JSONL rows, packed-row expansion, and the composed Session/Protocol projection; executable fixtures cover accepted and fail-closed byte cases.                                                                                                                                                                                                                                                                                                                                                                                                                                          | Byte-level composition of the pinned Harness JSONL persistence subset.                                                                                                            | Lean UTF-8/JSON parsing remains a library boundary; no deployed renderer, compression, filesystem, torn-tail repair, crash durability, or complete storage-union compatibility is claimed.                                                                                                                                                                                                                                                                                   |
| `DeepSeekApi.ChatRequest`, `buildRequest`, `ValidatedResponse`, `validateResponse`, `execute`                                                                                                           | **Checked/proved at the API boundary:** typed messages/tools/options construct an exact POST plan; successful response JSON retains parse and decode equalities; transport, non-2xx, malformed JSON, and API error paths remain distinct.                                                                                                                                                                                                                                                                                                                                                                                                                                         | DeepSeek's OpenAI-compatible `/chat/completions` vocabulary, including tool calls, reasoning, finish reasons, and usage.                                                          | This is a small non-streaming subset from `Lean.Json`; no live HTTP, credential validity, complete provider schema, streaming, or local `ToolSpec` validation is claimed.                                                                                                                                                                                                                                                                                                    |
| `DeepSeekApiBytes.ByteRequestPlan`, `ValidatedResponseBytes`, `validateResponseBytes`, `execute`                                                                                                        | **Checked/proved/exercised at the byte boundary:** request plans retain exact UTF-8 body bytes; successful responses retain raw bytes, decoded text, parse/decode certificates, and typed responses; invalid UTF-8 and non-2xx bodies remain distinct.                                                                                                                                                                                                                                                                                                                                                                                                                            | Byte-backed adapter immediately above the typed DeepSeek API seam.                                                                                                                | Injected byte transport plus Lean's UTF-8/JSON behavior only; no network reachability, credential validity, provider obedience, byte-level deployed compatibility, or Harness equivalence is claimed.                                                                                                                                                                                                                                                                        |
| `DeepSeekApiErrorEnvelope.ValidatedApiError`, `validateApiError`, `validateApiError_decoded`                                                                                                            | **Checked/proved:** a non-success body accepted as an OpenAI-compatible `{error: ...}` envelope retains exact JSON parse/decode equations and the dependent `ApiErrorBody`; successful bodies and malformed envelopes fail closed.                                                                                                                                                                                                                                                                                                                                                                                                                                                | Typed provider-error body normalization adjacent to the existing DeepSeek API codec.                                                                                              | No provider-error authenticity, retry/backoff safety, credential/network validity, or deployment equivalence is claimed.                                                                                                                                                                                                                                                                                                                                                     |
| `DeepSeekToolSchema.ValidatedParameters`, `validateParameters`, `ValidatedArguments`, `validateArguments`, `validateToolDefinition`, `validateRequestSource`, `CertifiedRequestSource.buildRequestPlan` | **Checked/proved/exercised bounded tool admission:** object parameter schemas retain exact source JSON; primitive property types, optional descriptions, required-name uniqueness/presence, optional `additionalProperties`, nonempty tool names, and duplicate tool names are checked before a dependent certified request builder is exposed. A parsed argument object can then carry duplicate-free fields, required-name, primitive-kind, and restrictive unknown-field certificates.                                                                                                                                                                                         | A narrow typed counterpart to the function-tool schema surface carried by DeepSeek `ChatRequest`.                                                                                 | Nested schemas, arrays/unions/constraints/defaults/enums, provider extensions, complete JSON-Schema semantics, external provider validation, and model obedience remain outside.                                                                                                                                                                                                                                                                                             |
| `DeepSeekToolAdmission.CertifiedFunctionCall`, `validateFunctionCall`                                                                                                                                   | **Checked/proved/exercised call bridge:** a raw provider function call is accepted only when its name equals a certified tool declaration, and the result retains the parsed `ValidatedArguments` certificate.                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | A narrow bridge from provider `FunctionCall` wire data to the local bounded schema certificate.                                                                                   | No call-ID authenticity, provider obedience, generic capability lookup/execution, or deployed Harness equivalence is claimed.                                                                                                                                                                                                                                                                                                                                                |
| `DeepSeekGenericBridge.SchemaToolBinding`, `validateAndAdmit`                                                                                                                                           | **Checked/proved/exercised composition:** an explicit provider-tool/generic-catalog name binding feeds the exact parsed provider AST into `GenericHarness.Config.validate`; success retains the provider certificate, generic validation equality, and existentially indexed local call/request type.                                                                                                                                                                                                                                                                                                                                                                             | A pre-execution bridge between bounded provider schema admission and the dependent generic harness.                                                                               | No provider-schema/`ToolSpec` semantic equivalence, call-ID authenticity, provider execution, external effects, or deployed Harness equivalence is claimed.                                                                                                                                                                                                                                                                                                                  |
| `DeepSeekSchemaExecution.executeCertifiedCall`                                                                                                                                                          | **Checked/proved/exercised dispatch:** consumes only the combined provider/generic certificate, evaluates the existing dependent policy, and invokes the committed generic `View` only for `.allow`, retaining typed policy/reply/execution evidence.                                                                                                                                                                                                                                                                                                                                                                                                                             | A schema-aware pure dispatch adapter over the generic harness.                                                                                                                    | No provider obedience, call-ID authenticity, live process/external-tool effects, raw-path equivalence, or deployed Harness equivalence is claimed.                                                                                                                                                                                                                                                                                                                           |
| `DeepSeekSchemaHarness.SchemaExecutedTool`, `executeCertifiedFunctionCall`, `appendCertifiedToolResult`, `appendCertifiedToolResultToRunner`                                                            | **Checked/proved/exercised transport:** retains the provider certificate while reifying a successful schema-aware execution as `DeepSeekHarness.ExecutedTool`, then reuses exact tool-result message, next-sequence, protocol-projection, and runner append theorems.                                                                                                                                                                                                                                                                                                                                                                                                             | A post-execution bridge from the schema-certified path into the existing local session/runner surface.                                                                            | No re-execution, provider obedience, call-ID authenticity, live external effects, persistence, or deployed Harness equivalence is claimed.                                                                                                                                                                                                                                                                                                                                   |
| `DeepSeekSchemaRound.AcceptedSingleToolCall`, `acceptSingleToolCall`, `executeSchemaRound`                                                                                                              | **Checked/proved/exercised bounded round:** an accepted API response is required to contain exactly one function call; the exact response/tool-call witness is retained, schema-aware dispatch is performed, and the existing runner receives the certified result. Zero- and multi-call payloads fail with typed shape errors.                                                                                                                                                                                                                                                                                                                                                   | A complete-body one-tool response-to-runner composition.                                                                                                                          | No multi-tool round, live transport, provider obedience, call-ID authenticity, persistence, or deployed Harness equivalence is claimed.                                                                                                                                                                                                                                                                                                                                      |
| `DeepSeekSchemaMultiRound.AcceptedToolCalls`, `executeSchemaCalls`, `executeSchemaMultiRound`                                                                                                           | **Checked/proved/exercised bounded multi-call round:** a nonempty accepted response is retained as an exact call list; each call is schema-, generic-, policy-, and execution-certified against the preceding model, failures retain their list position, the execution list has an exact length equality, and every certified result is appended to the existing runner.                                                                                                                                                                                                                                                                                                         | Homogeneous complete-body multi-call response composition.                                                                                                                        | One explicit binding is used for every call; heterogeneous registries, live transport, provider obedience, call-ID authenticity, persistence, and deployed Harness equivalence remain external.                                                                                                                                                                                                                                                                              |
| `DeepSeekSchemaRegistry.SchemaToolRegistry`, `resolveSchemaTool`, `executeSchemaRegistryCalls`, `executeSchemaRegistryRound`                                                                            | **Checked/proved/exercised heterogeneous registry:** a finite name-unique dependent registry selects entry-specific provider schemas and generic operations; weather/clock calls execute sequentially with exact list/runner accounting, while a later unknown name becomes a typed indexed error.                                                                                                                                                                                                                                                                                                                                                                                | Heterogeneous complete-body schema registry/runner composition.                                                                                                                   | Provider-complete discovery, live transport, provider obedience, call-ID authenticity, persistence, and deployed Harness equivalence remain external.                                                                                                                                                                                                                                                                                                                        |
| `DeepSeekScopedRegistry.ScopedRegistry`, `resolveScopedTool`, `dispatchScopedCall`                                                                                                                      | **Checked/proved/exercised scoped routing:** nearest-first lexical scopes select dependent entries; a matching restriction is terminal rather than falling through; successful admission retains a typed automatic/review approval ticket before `View.execute`, while resolve, policy, approval, and provider failures remain typed.                                                                                                                                                                                                                                                                                                                                             | Pure local scope/shadow/restriction/approval adapter adjacent to Harness tool dispatch.                                                                                           | Scope construction, authenticated approval, provider discovery, asynchronous policy waterfalls, persistence, external effects, and deployed Harness equivalence remain external.                                                                                                                                                                                                                                                                                             |
| `DeepSeekSchemaConversation.executeSchemaRegistryConversationRound`                                                                                                                                     | **Checked/proved/exercised transport composition:** registry-derived tool declarations are tied to a typed complete-body request plan; an explicit transport returns a validated response, and one accepted heterogeneous round retains the plan, response, accepted calls, dependent batch, and runner endpoint.                                                                                                                                                                                                                                                                                                                                                                 | Heterogeneous registry attached to the typed DeepSeek transport boundary.                                                                                                         | No remote credential/provider-obedience, call-ID authenticity, retry, cancellation, persistence, external-effect, or deployed Harness equivalence is claimed.                                                                                                                                                                                                                                                                                                                |
| `DeepSeekSchemaConversationBytes.executeSchemaRegistryConversationRound`                                                                                                                                | **Checked/proved/exercised byte composition:** a typed complete plan is converted to exact UTF-8 request bytes; a successful byte response retains source bytes, decoded text, and parse/decode certificates before one heterogeneous registry round is admitted, preserving accepted calls, dependent batch, and runner endpoint.                                                                                                                                                                                                                                                                                                                                                | Raw-byte heterogeneous schema conversation boundary.                                                                                                                              | Injected transport and canonical UTF-8/JSON behavior only; no remote reachability, credential/provider obedience, byte-level deployed compatibility, retry, cancellation, persistence, external effects, or Harness equivalence is claimed.                                                                                                                                                                                                                                  |
| `DeepSeekSchemaConversationLoop.executeSchemaRegistryConversationStep`, `runSchemaConversation`                                                                                                         | **Checked/proved/exercised finite loop:** certified tool rounds advance the dependent model and runner under explicit fuel, a validated no-tool response is retained as a terminal witness, and the existential round history is distinct from fuel exhaustion.                                                                                                                                                                                                                                                                                                                                                                                                                   | Caller-fueled heterogeneous complete-body conversation loop.                                                                                                                      | No retries, in-flight cancellation, persistence, external tool effects, provider obedience, or deployed Harness equivalence is claimed.                                                                                                                                                                                                                                                                                                                                      |
| `DeepSeekSchemaLocalHttp.runWithKey`, `LocalSchemaHttpResult`, `Example.run`, `Example.summarize`                                                                                                       | **Checked/exercised local heterogeneous HTTP composition:** a one-shot loopback server validates the two declared tools, complete mode, route, method, authorization, and model; real curl drives the weather/clock tool round and terminal response, while the dependent result retains the prepared plan, tool/terminal witnesses, request counts, server exit, final model, and runner endpoint.                                                                                                                                                                                                                                                                               | Local process/HTTP evidence for the heterogeneous schema conversation loop.                                                                                                       | No remote reachability, TLS, credential/provider authenticity, executable trust, retry/cancellation, persistence, external effects, or deployed Harness equivalence is claimed.                                                                                                                                                                                                                                                                                              |
| `DeepSeekSchemaTransportRetryCancellation.executeSchemaRetryStep`, `SchemaRetryTrace`, `SchemaRetryStop`, `run`                                                                                         | **Checked/proved/exercised composed boundary:** one complete request plan is reused across bounded retry attempts; the successful `ValidatedResponse` is admitted once and drives either a terminal witness or heterogeneous registry execution; pre-round cancellation retains the exact unchanged endpoint; the trace indexes each tool round by its dependent runner/model endpoint, with completion, cancellation, and fuel exhaustion distinct. Fixtures cover timeout-before-send and 503→200 weather/clock execution followed by a terminal response.                                                                                                                      | Heterogeneous schema registry composed with injected retry and caller-controlled pre-round cancellation.                                                                          | No in-flight IO interruption, provider backoff/idempotency, persistence, external effects, credential/network trust, provider obedience, or deployed Harness retry/cancellation equivalence is claimed.                                                                                                                                                                                                                                                                      |
| `DeepSeekSchemaProcessRetryCancellation.processRetryTransport`, `Example.cancellationRun`, `Example.successRun`                                                                                         | **Checked/exercised process composition:** the same dependent retry/cancellation result is driven by the existing `IO.Process` adapter; the deterministic `sh` fixture emits one transient 503, then a heterogeneous weather/clock body, then a terminal no-tool body, while the proof-carrying trace retains retry history and exact runner/model endpoints.                                                                                                                                                                                                                                                                                                                     | Local process-backed evidence for the heterogeneous retry boundary.                                                                                                               | No network reachability, credential validity, provider obedience, shell/process trust, in-flight interruption, backoff/idempotency, persistence, external effects, or deployed Harness equivalence is claimed.                                                                                                                                                                                                                                                               |
| `DeepSeekRequestMode.TypedRequestPlan`, `buildTypedCompleteRequest`, `buildTypedStreamingRequest`, `executeComplete`                                                                                    | **Checked/proved mode boundary:** complete and streaming plans retain an equality tying the serialized `stream` flag to their index; terminal execution accepts only the complete index, and Harness round paths use the typed complete builder internally.                                                                                                                                                                                                                                                                                                                                                                                                                       | Type-level protection against passing a streamed request to the terminal decoder.                                                                                                 | The mode certificate does not prove live transport, provider behavior, or deployed Harness equivalence; raw unindexed `RequestPlan` remains available for compatibility.                                                                                                                                                                                                                                                                                                     |
| `DeepSeekCurlTransport.runProcess`, `processTransport`, `curlTransport`                                                                                                                                 | **Checked/exercised at the process boundary:** a configured executable receives the request body on stdin, direct URL/header argv values, and a status trailer; process failures are typed and a deterministic `sh` fixture composes with `DeepSeekApi.execute`.                                                                                                                                                                                                                                                                                                                                                                                                                  | A concrete executable adapter adjacent to the typed DeepSeek request/response seam.                                                                                               | No network/credential/process-trust claim, timeout guarantee, shell/curl correctness, complete provider schema, streaming, or deployed adapter equivalence is claimed.                                                                                                                                                                                                                                                                                                       |
| `DeepSeekCurlStream.executeSse`, `curlProcess`, `fixtureResponse`                                                                                                                                       | **Checked/exercised at the process/SSE boundary:** a complete executable response is status-checked, then passed to strict SSE validation; process, HTTP-status, framing, and stream errors remain distinct, and the deterministic `sh` fixture returns the example body.                                                                                                                                                                                                                                                                                                                                                                                                         | A complete-body process-backed adapter adjacent to the strict in-memory DeepSeek stream boundary.                                                                                 | No incremental reader, buffering/backpressure, cancellation, reconnect, network/credential/process-trust, or provider-complete assembler equivalence is claimed.                                                                                                                                                                                                                                                                                                             |
| `DeepSeekCurlOutcome.executeOutcome`, `ProcessedOutcome`, `OutcomeClientError`                                                                                                                          | **Checked/exercised at the process/outcome boundary:** a deterministic executable response is status-checked and classified through the provider-failure/text/tool/mixed/multi terminal-outcome sum; process, HTTP-status, and semantic errors remain distinct.                                                                                                                                                                                                                                                                                                                                                                                                                   | Process-backed complete-body adapter over the unified dependent outcome.                                                                                                          | No incremental reader, retry, cancellation, network/credential/process-trust, session append, or deployed provider-equivalence claim is made.                                                                                                                                                                                                                                                                                                                                |
| `DeepSeekOutcomeSession.dispatchOutcome`, `executeAndDispatchOutcome`                                                                                                                                   | **Checked/proved composition:** a validated provider failure returns with the same runner, while a successful rich outcome is terminally finished and appended through the existing proof-carrying runner.                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Typed local policy boundary between terminal provider outcomes and session state.                                                                                                 | Source-event evidence is caller-supplied; failure-to-message policy, persistence, retry, cancellation, external tools, and whole-session equivalence remain outside.                                                                                                                                                                                                                                                                                                         |
| `DeepSeekOutcomeConversation.dispatchOutcome`, `executeAndDispatchOutcome`, `executeOutcomeWithTools`, `executeAndRunOutcome`                                                                           | **Checked/proved/exercised:** a process-backed provider failure preserves `ConversationRunner`; successful rich terminal views append with model/tool-count proofs, project calls to `FunctionCall`, route them through the existing dependent executor, and append certified tool results. Failure/text/tool/mixed/multi dispatch plus text/counter-tool execution fixtures cover the branches.                                                                                                                                                                                                                                                                                  | Typed terminal-outcome handoff and dependent-execution bridge into the existing conversation runner.                                                                              | No provider-ID authentication, failure/retry policy, persistence, cancellation, external process trust, or whole-session/deployed equivalence is claimed.                                                                                                                                                                                                                                                                                                                    |
| `DeepSeekOutcomeTransportLoop.executeOutcomeTransportRound`, `runOutcomeTransport`, `Example.run`, `Example.apiFailureRun`                                                                              | **Checked/proved/exercised:** generic `Transport` requests carry a type-indexed streaming certificate; complete terminal bodies are validated, non-success API-error envelopes retain typed provider messages without mutating the runner, stream failures remain separate, tool calls execute through the existing dependent path, and deterministic success/failure fixtures cover both stops.                                                                                                                                                                                                                                                                                  | Generic transport-backed complete-body rich-outcome continuation with typed API-error handling.                                                                                   | No error authenticity, incremental IO, backpressure, reconnect, retry, cancellation, credential trust, external tool trust, or deployed Harness equivalence is claimed.                                                                                                                                                                                                                                                                                                      |
| `DeepSeekCurlSession.executeText`, `executeAndAppendText`, `ProcessedResponse`                                                                                                                          | **Checked/exercised at the process/session boundary:** a terminal text SSE body retains its wire certificate, passes the accepted rich/session projection, and appends to the pure runner with exact next-sequence and next-call proofs; a terminal text `sh` fixture exercises the path.                                                                                                                                                                                                                                                                                                                                                                                         | Complete-body process-to-session composition over the source-honest terminal text subset.                                                                                         | Source-event evidence and local IDs remain caller-supplied/runtime-local; no incremental transport, persistence, external execution, provider-ID authenticity, or whole-session equivalence is claimed.                                                                                                                                                                                                                                                                      |
| `DeepSeekHarnessProcess.PreparedRequest`, `prepareRequest`, `executePreparedText`, `executeSourceText`, `ProcessRound`                                                                                  | **Checked/proved/exercised:** retains the typed Harness `RequestSource`, exact `RequestPlan`, and successful `buildRequestPlan` equation before process launch; a successful complete-body response retains its wire/semantic certificate and exact indexed runner append endpoint, while request and process errors remain separate.                                                                                                                                                                                                                                                                                                                                             | Request-provenance composition above the local process/session runner.                                                                                                            | No credential/provider authenticity, incremental transport, persistence, external tool execution, cancellation, or deployed Harness equivalence is claimed.                                                                                                                                                                                                                                                                                                                  |
| `DeepSeekHarnessExtensions.build*For`, `appendAssistantFor`, `ExtensionRunner`, `extensionRunnerText`                                                                                                   | **Checked/proved/exercised indexed extension boundary:** arbitrary dependent `Session.ExtensionSchema`s build complete and streaming request plans, keep log-only events out of the model surface, project surface events through the schema, append a rich assistant without changing the schema, and carry sequence/tool-count invariants through a terminal stream fixture.                                                                                                                                                                                                                                                                                                    | Generic schema-preserving request and terminal-response runner composition.                                                                                                       | Caller-supplied extension JSON codecs, provider compatibility, persistence, transport trust, and deployed Harness equivalence remain external.                                                                                                                                                                                                                                                                                                                               |
| `SessionExtensionRefinement.ExtensionCodec`, `decodeEvent`, `appendDecodedEvent`, `decodeAndAppend`, `ExtensionReplay`, `ValidatedExtensionLog`, `validate`, `Example.exampleCodec`                     | **Checked/proved/exercised typed extension ingress and replay:** the caller-supplied codec returns a dependent kind/payload sum; the generic envelope checks one declared tag, safe sequence/time fields, rejects unsupported current-Harness metadata, and appends only at the exact next session sequence. `ExtensionReplay` retains the raw list order, each dependent event, exact append equations, and shared intermediate sessions; `ValidatedExtensionLog` proves final sequence and typed-event-count equations. Heartbeat (log-only) and banner (surface) fixtures prove both visibility paths plus wrong-tag, ignorable, malformed-payload, and stale-sequence errors. | Explicit API-as-type-signature boundary for schema-owned extension JSON, including ordered multi-event validation.                                                                | Arbitrary JSON synthesis, codec completeness, provider compatibility, persistence, transport trust, and deployed Harness equivalence remain external.                                                                                                                                                                                                                                                                                                                        |
| `SessionExtensionArchive.ValidatedExtensionArchive`, `validate`, `Example.archivedExample`                                                                                                              | **Checked/proved/exercised archive attachment:** the lossless `SessionEventArchive` and dependent extension replay share one exact raw list; success proves every archived record is a required extension, retains raw order, and exposes the final indexed session. Known core tags and ignorable extensions reject before semantic replay.                                                                                                                                                                                                                                                                                                                                      | Positive persistence-facing seam for caller-owned extension records.                                                                                                              | Mixed current-Harness replay, durable storage, provider compatibility, transport trust, and deployed Harness equivalence remain external.                                                                                                                                                                                                                                                                                                                                    |
| `DeepSeekHarnessExtensionArchive.RestoredRunner`, `restoreRunner`, `RequestCertificate`, `buildRequestCertificate`                                                                                      | **Checked/proved/exercised schema-indexed attachment:** a required-extension archive restores a `DeepSeekHarnessExtensions.ExtensionRunner` with derived tool-call count and exact equality to the indexed final session; the request certificate rebuilds from that same endpoint, and the extension fixture exercises the positive path.                                                                                                                                                                                                                                                                                                                                        | Schema-owned runner/request continuation above the lossless extension archive.                                                                                                    | Mixed current-Harness replay, provider compatibility, transport trust, durable storage, and deployed Harness equivalence remain external.                                                                                                                                                                                                                                                                                                                                    |
| `DeepSeekHarnessExtensionRequest.CertifiedRequest`, `buildCertifiedRequest`, `CertifiedRequest.build_eq_archive`                                                                                        | **Checked/proved/exercised request provenance:** a restored indexed extension session is paired with `DeepSeekToolSchema.CertifiedRequestSource`; the request retains the dependent tool/name proof and exact builder equation, while the duplicate-tool fixture rejects before construction.                                                                                                                                                                                                                                                                                                                                                                                     | Schema-validated request handoff above the extension archive runner.                                                                                                              | Provider-side tool obedience, credentials, transport trust, durable storage, and deployed Harness equivalence remain external.                                                                                                                                                                                                                                                                                                                                               |
| `DeepSeekHarnessProcessOutcome.PreparedStreamingRequest`, `prepareStreamingRequest`, `executePreparedOutcome`, `executeSourceOutcome`, `ProcessOutcomeRound`                                            | **Checked/proved/exercised:** retains a typed `stream: true` request plan through complete-body provider-failure or rich terminal classification, dependent tool execution, and the final `ConversationRunner` endpoint; the execution equality ties the process outcome to the dependent result, and request/process/execution errors remain distinct.                                                                                                                                                                                                                                                                                                                           | Rich process-backed request provenance and dependent runner composition.                                                                                                          | No provider/credential authenticity, incremental delivery, cancellation, persistence, external tool trust, or deployed Harness equivalence is claimed.                                                                                                                                                                                                                                                                                                                       |
| `DeepSeekStreamHarness.executeConversationStreamRound`, `executeConversationMultiStreamRound`, `runConversationMultiStream`, `ConversationRunner.appendFinished`                                        | **Checked/exercised typed stream continuation:** complete-body terminal rich tool streams retain wire/session certificates, assign local numeric IDs, route each streamed call through dependent admission/policy/provider execution, and append certified typed results to the generic conversation runner; streamed rounds use `buildStreamingRequestPlan`, with a source/body certificate proving `stream: true`, and the executable fixture rejects the non-streaming body; deterministic process fixtures exercise both one-call and two-call terminal streams, then the fuel-bounded loop stops on the text-only terminal response or typed exhaustion.                     | Complete-body stream-to-harness continuation over the source-honest rich tool subset.                                                                                             | No incremental delivery, cancellation, backpressure, reconnect, provider-complete assembly, credential/network/process trust, or deployed Harness equivalence is claimed.                                                                                                                                                                                                                                                                                                    |
| `DeepSeekCurlIncremental.executeSseIncremental`, `readBodyLines`, `IncrementalResponse`                                                                                                                 | **Checked/exercised at the process/stream boundary:** each complete body line reaches the callback under an explicit read budget before the private status trailer is consumed; the exact reconstructed body is then strict-validated and returned with the observed lines.                                                                                                                                                                                                                                                                                                                                                                                                       | A line-oriented process-to-SSE bridge immediately below the strict in-memory parser.                                                                                              | No byte-level framing, backpressure, cancellation, reconnect, network/credential/process trust, or provider-complete assembler equivalence is claimed.                                                                                                                                                                                                                                                                                                                       |
| `DeepSeekCurlPrefix.executeSsePrefix`, `PrefixResponse`, `cleanup`                                                                                                                                      | **Checked/exercised typed process prefix:** each complete process line advances `DeepSeekStreamIncremental.PrefixState` before the next read; fuel/cancellation returns a retained raw prefix and synchronously kills/waits the child, while terminal success retains status, raw body, normalized frames, and strict `[DONE]` validation.                                                                                                                                                                                                                                                                                                                                        | A process-backed line-stop boundary that preserves both observed output and proof-carrying normalized state.                                                                      | No byte framing, blocked-read cancellation, backpressure, reconnect, network/credential/process trust, provider-complete assembler, or deployed stream equivalence is claimed.                                                                                                                                                                                                                                                                                               |
| `DeepSeekCurlPrefixSession.executeText`, `executeAndAppendText`, `ProcessedPrefix`                                                                                                                      | **Checked/exercised terminal semantic bridge:** completed process prefixes are projected through the accepted text/tool/mixed/multi validators and can append to the typed session runner with exact sequence/tool-count proofs; fuel/cancellation remain explicit typed stops.                                                                                                                                                                                                                                                                                                                                                                                                   | Incremental process prefix to terminal semantic/session composition.                                                                                                              | No blocked-read interruption, byte framing, backpressure, reconnect, external tool execution, provider-complete assembler, or deployed stream equivalence is claimed.                                                                                                                                                                                                                                                                                                        |
| `DeepSeekAsyncHarness.ProcessJob`, `RaceResult`, `executeRace`                                                                                                                                          | **Checked/exercised cooperative process race:** two complete-body text-prefix jobs run in separate `ContextAsync` children; `ContextAsync.race` retains the first typed result, requests loser cancellation, and pure phase facts distinguish waiting from terminal success/failure.                                                                                                                                                                                                                                                                                                                                                                                              | Executable async observation adjacent to the line-oriented process/session adapter.                                                                                               | The synchronous line read is not interruptible by the cooperative request; no wall-clock fairness, arbitrary cleanup, network/credential/process trust, or deployed Harness async equivalence is claimed.                                                                                                                                                                                                                                                                    |
| `DeepSeekAsyncStreamHarness.StreamProcessJob`, `RaceResult`, `executeRace`                                                                                                                              | **Checked/exercised streamed process race:** two complete-body streamed Harness continuations run in separate `ContextAsync` children; the winner retains dependent tool executions, two round witnesses, final model, and runner endpoint.                                                                                                                                                                                                                                                                                                                                                                                                                                       | Executable async observation over the streamed tool/session continuation.                                                                                                         | Synchronous line reads remain non-interruptible; no wall-clock fairness, arbitrary cleanup, network/credential/process trust, or deployed Harness async equivalence is claimed.                                                                                                                                                                                                                                                                                              |
| `DeepSeekAsyncStreamCancellation.ProcessJob`, `RaceResult`, `executeRace`                                                                                                                               | **Checked/exercised cancellation-bearing streamed race:** one real child cancels before round zero while the other is a process-backed streamed continuation; the typed result retains policy, reason, unchanged runner/model endpoint, and empty completed-round prefix.                                                                                                                                                                                                                                                                                                                                                                                                         | Executable pre-round cancellation bridge over the streamed async observation.                                                                                                     | It does not interrupt blocked reads or establish fairness, cleanup, network/credential/process trust, or deployed cancellation equivalence.                                                                                                                                                                                                                                                                                                                                  |
| `DeepSeekAsyncStreamRetryCancellation.RetryProcessJob`, `JobResult`, `RaceResult`, `executeRace`                                                                                                        | **Checked/exercised retry-aware cooperative race:** each child retains its retry policy and dependent final runner/model indices; the cancellation-first fixture keeps the typed pre-round reason and empty accepted retry trace, while a delayed-child success-first fixture retains two accepted rounds and the final endpoint.                                                                                                                                                                                                                                                                                                                                                 | Executable async observation over retry-aware streamed conversations.                                                                                                             | It does not interrupt blocked reads or establish fairness, cleanup, reconnect, network/credential/process trust, or deployed retry/cancellation equivalence.                                                                                                                                                                                                                                                                                                                 |
| `DeepSeekStream.ToolCallDelta`, `StreamChunk`, `DataFrame`, `parseSse`, `validateSse`                                                                                                                   | **Checked/proved at the wire boundary:** strict `data:` / `[DONE]` SSE text is decoded into typed delta frames; each accepted data frame retains raw payload, JSON parse, and typed decode equalities; UTF-8, framing, terminal, JSON, and decode errors stay distinct.                                                                                                                                                                                                                                                                                                                                                                                                           | In-memory ingress for the DeepSeek/OpenAI-compatible streaming vocabulary.                                                                                                        | This is a strict subset and no live reader, HTTP delivery, buffering/backpressure, cancellation, assembler, or provider-complete stream equivalence is claimed.                                                                                                                                                                                                                                                                                                              |
| `DeepSeekStreamFailure.FailureView`, `projectFrames`, `validateFailureStream`                                                                                                                           | **Checked/proved:** complete strict SSE bodies ending in `content_filter` or `insufficient_system_resource` retain the typed leading frames, terminal raw `DataFrame`, singleton choice/reason, and optional usage; ordinary finishes are rejected.                                                                                                                                                                                                                                                                                                                                                                                                                               | Source-honest provider terminal-failure wire boundary.                                                                                                                            | No normal rich/session projection, wire `error`/`aborted` envelope, retry, cancellation, live delivery, or deployed provider-equivalence claim is made.                                                                                                                                                                                                                                                                                                                      |
| `DeepSeekTerminalOutcome.TerminalOutcome`, `validateTerminalOutcome`                                                                                                                                    | **Checked/proved:** fixed-order complete-body dispatcher classifies a provider failure or one of the text, one-tool, mixed, and finite multi-call rich certificates, retaining the selected dependent witness and typed wire/projection rejection.                                                                                                                                                                                                                                                                                                                                                                                                                                | Unified terminal-outcome boundary over the existing disjoint validators.                                                                                                          | No new wire language, session-message construction, retry, cancellation, live delivery, provider-complete assembler, or deployed provider-equivalence claim is made.                                                                                                                                                                                                                                                                                                         |
| `DeepSeekStreamIncremental.PrefixState`, `pushLine`, `finish`, `consumeLines`, `consumeBody`                                                                                                            | **Checked/proved/exercised pure incremental prefix:** each accepted complete line retains the exact accumulated body, parsed frame list, line count, and `parseSsePrefix` equation; `finish` requires the complete `[DONE]` validator, while `LinePolicy` can stop before the next line and preserve the prefix.                                                                                                                                                                                                                                                                                                                                                                  | A source-honest state machine beneath the complete-body wire validator.                                                                                                           | No live HTTP reader, byte framing, buffering/backpressure, process cancellation, reconnect, provider-complete assembler, or deployed stream equivalence is claimed.                                                                                                                                                                                                                                                                                                          |
| `DeepSeekRichStream.ValidatedTextStream`, `projectFrames`, `validateTextStream`                                                                                                                         | **Checked/proved semantic bridge:** a validated wire stream with one assistant choice at index zero, text-only deltas, terminal usage, and stop/max-token finish becomes an exact `RichStream.ValidatedTrace`; wire, projection, and rich certificates are retained.                                                                                                                                                                                                                                                                                                                                                                                                              | A deliberately narrow DeepSeek SSE-to-local-rich-stream normalization.                                                                                                            | Reasoning, tool calls, extra choices, unsupported finishes, missing usage/finish, live transport, and provider assembler completeness are rejected or remain external.                                                                                                                                                                                                                                                                                                       |
| `DeepSeekRichToolStream.ValidatedToolStream`, `projectFrames`, `validateToolStream`                                                                                                                     | **Checked/proved tool bridge:** a validated wire stream with at most one indexed function call becomes an exact rich tool-call trace, preserving raw argument fragments, stable IDs/names, terminal usage, and typed certificates.                                                                                                                                                                                                                                                                                                                                                                                                                                                | A deliberately narrow DeepSeek SSE-to-local tool-block normalization.                                                                                                             | Multiple calls, missing IDs/names, reasoning, mixed deltas, live transport, execution, and provider assembler completeness are rejected or remain external.                                                                                                                                                                                                                                                                                                                  |
| `DeepSeekRichMixedStream.ValidatedMixedStream`, `projectFrames`, `projectChunks`, `validateMixedStream`                                                                                                 | **Checked/proved mixed bridge:** one choice can interleave text, reasoning, and one indexed function call across frames; first-seen contiguous block indices, stateful tool metadata, exact block ends, usage, terminal finish, wire/projection/rich certificates, and same-frame mixed-kind rejection are explicit.                                                                                                                                                                                                                                                                                                                                                              | A source-honest composition of provider text/reasoning/tool deltas into the local rich-stream language.                                                                           | Multiple choices/calls, unsupported provider finishes, replay metadata, live transport, tool execution, and deployed assembler completeness remain outside.                                                                                                                                                                                                                                                                                                                  |
| `DeepSeekRichMultiStream.ValidatedMultiStream`, `projectFrames`, `projectChunks`, `validateMultiStream`                                                                                                 | **Checked/proved multi-call bridge:** one choice can interleave text, reasoning, and any finite list of indexed function calls; provider indices map to first-seen contiguous local blocks, per-call IDs/names and raw arguments are assembled independently, and exact block ends/usage/terminal certificates are retained.                                                                                                                                                                                                                                                                                                                                                      | Source-honest finite multi-call extension of the provider stream boundary.                                                                                                        | Same-frame cross-kind fields, multiple choices, unsupported provider finishes, replay metadata, live transport, tool execution, and deployed assembler completeness remain outside.                                                                                                                                                                                                                                                                                          |
| `DeepSeekProviderAssembler.push`, `assemble`, `validate`, `Certificate`                                                                                                                                 | **Checked/proved source-shaped assembler:** canonical post-decoder chunks retain first-seen order, open text/reasoning/tool-call fallback, first-close-wins, latest tool id/name, last usage/finish/replay metadata, max-token tool pruning, and typed unknown-block errors; successful folds carry exact state and assembly equations.                                                                                                                                                                                                                                                                                                                                           | Provider `BlockAssembler` semantics for the modeled text/reasoning/tool-call post-decoder vocabulary.                                                                             | Wire decoding, image/tool-result block schemas, opaque replay JSON, live transport, and deployed TypeScript behavioral equivalence remain outside.                                                                                                                                                                                                                                                                                                                           |
| `DeepSeekAssemblerToolRound.toFunctionCalls`, `executeAssembledTools`, `appendAssistant`, `appendToolResults`                                                                                           | **Checked/proved dependent continuation:** a successful assembled assistant view is converted to local function calls, executed through the existing model-indexed counter configuration, and appended with certified assistant/tool-result session messages; the executable witness reaches model `5` from `2`.                                                                                                                                                                                                                                                                                                                                                                  | Post-assembly seam from provider-shaped blocks into the local dependent Harness/session language.                                                                                 | Provider-ID authenticity, external effects, transport, persistence, and deployed Harness equivalence remain outside.                                                                                                                                                                                                                                                                                                                                                         |
| `DeepSeekProviderStreamAssembly.validateBody`, `mapRawChunks`, `ValidatedProviderAssembly`                                                                                                              | **Checked/proved wire-to-assembler composition:** strict one-choice/multi-call SSE projection is mapped into the source-shaped provider chunk vocabulary with both source and fold certificates; replay metadata is rejected and structured failure/abort causes are normalized.                                                                                                                                                                                                                                                                                                                                                                                                  | Accepted rich multi-stream subset reaching the source-shaped assembler.                                                                                                           | Wire transport, image/tool-result schemas, provider-ID authenticity, and deployed TypeScript equivalence remain outside.                                                                                                                                                                                                                                                                                                                                                     |
| `DeepSeekStreamToolRound.executeBodyTools`, `appendRound`, `ValidatedStreamToolRound`                                                                                                                   | **Checked/proved wire-backed dependent continuation:** the composed stream assembly reaches the existing model-indexed execution certificate and appends exact assistant/tool-result session messages; the executable fixture reaches model `5` from `2`.                                                                                                                                                                                                                                                                                                                                                                                                                         | Local composition of strict stream, provider assembly, dependent execution, and session append.                                                                                   | Network/authentication, external effects, persistence, retry/cancellation, and deployed Harness equivalence remain outside.                                                                                                                                                                                                                                                                                                                                                  |
| `DeepSeekScopedStreamToolRound.executeBodyScopedTools`, `ScopedExecutionTrace`, `appendRound`                                                                                                           | **Checked/proved scoped continuation:** each assembled call is resolved in nearest-first scope order, terminal restrictions fail closed, and automatic/review approval evidence is retained before dependent execution; the model-indexed trace converts to the existing assistant/tool-result append with exact message/sequence proofs. The fixture executes weather then clock and checks restricted shadow plus denied approval.                                                                                                                                                                                                                                              | Scoped/approval-routed local composition over the strict stream/provider/session seam.                                                                                            | Scope construction, authenticated approval, external effects, persistence, cancellation, and deployed Harness equivalence remain outside.                                                                                                                                                                                                                                                                                                                                    |
| `DeepSeekProcessStreamToolRound.executeWith`, `ProcessedRound`, `counterRun`                                                                                                                            | **Checked/proved process-backed continuation:** a configured local process returns a complete body with status/SSE certificates, then the exact provider/dependent/session composition is reused; the deterministic fixture reaches model `5` from `2`.                                                                                                                                                                                                                                                                                                                                                                                                                           | Complete-body local process adapter.                                                                                                                                              | Network reachability, credentials, process trust, incremental delivery, cancellation, persistence, external effects, and deployed Harness equivalence remain outside.                                                                                                                                                                                                                                                                                                        |
| `DeepSeekProcessScopedStreamToolRound.executeWith`, `ProcessedScopedRound`, `Example.scopedDualRun`                                                                                                     | **Checked/proved process-backed scoped continuation:** a configured local process returns a complete scoped SSE body with status and strict-wire certificates, then scoped approval, dependent execution, and exact assistant/tool-result append are reused; the fixture validates three frames and the dual-call endpoint.                                                                                                                                                                                                                                                                                                                                                       | Complete-body process adapter over the scoped stream/approval/session seam.                                                                                                       | Network reachability, credentials, process trust, incremental delivery, cancellation, persistence, external effects, and deployed Harness equivalence remain outside.                                                                                                                                                                                                                                                                                                        |
| `DeepSeekProcessScopedConversation.runAux`, `run`, `Example.twoStepSummary`, `Example.oneStepFuelSummary`                                                                                               | **Checked/proved process-backed scoped conversation:** a round-indexed process family feeds successive scoped rounds; each accepted body retains its scoped execution and exact session append, while a no-call body yields a typed terminal and exhausted fuel yields a distinct stop. The local fixture checks a two-round four-message completion and a one-round exhausted prefix.                                                                                                                                                                                                                                                                                            | Finite complete-body process conversation over the scoped approval/session seam.                                                                                                  | Retries, cancellation, persistence, external effects, live network/provider obedience, and deployed Harness equivalence remain outside.                                                                                                                                                                                                                                                                                                                                      |
| `DeepSeekProcessScopedRequestConversation.executePrepared`, `run`, `Example.twoStepSummary`, `Example.oneStepFuelSummary`                                                                               | **Checked/proved request-indexed continuation:** each current session builds a typed streaming request, and the successful process/SSE/scoped result is indexed by that exact `HttpRequest`; the fixture proves distinct round bodies, a two-round terminal, and a one-round fuel stop.                                                                                                                                                                                                                                                                                                                                                                                           | Request-provenance refinement over the finite process/scoped conversation.                                                                                                        | Live network/provider behavior, credentials, retries, cancellation, persistence, external effects, and deployed Harness equivalence remain outside.                                                                                                                                                                                                                                                                                                                          |
| `DeepSeekProcessScopedRequestBytePrefixConversation.executePrepared`, `run`, `Example.twoStepSummary`, `Example.prefixFuelSummary`                                                                      | **Checked/proved arbitrary-byte request-indexed continuation:** each round retains the exact request, raw chunks, pending framing, status, strict SSE completion, scoped execution, and exact session append; the fixture proves two completed rounds with distinct request bodies and a typed prefix-fuel stop.                                                                                                                                                                                                                                                                                                                                                                  | Byte-prefix refinement over the request-indexed process/scoped conversation.                                                                                                      | Network/authentication, executable trust, blocked-read cancellation, backpressure, reconnects, retries, persistence, external effects, and deployed Harness equivalence remain outside.                                                                                                                                                                                                                                                                                      |
| `DeepSeekSessionBridge.FinishedAssistant`, `finishAssistant`, `appendFinishedAssistant`                                                                                                                 | **Checked/proved session seam:** a terminal `RichStream.ValidatedTrace` yields an explicit assistant view and appends one session assistant payload after the caller supplies a unique numeric call-ID assignment and earlier source-event evidence.                                                                                                                                                                                                                                                                                                                                                                                                                              | Local numeric-ID/session-surface seam after rich-stream validation.                                                                                                               | Provider-ID authenticity, source-event provenance beyond supplied proofs, persistence, and whole-session equivalence are not claimed.                                                                                                                                                                                                                                                                                                                                        |
| `DeepSeekSessionRunner.AcceptedResponse`, `finishText`, `finishTool`, `finishMixed`, `finishMulti`, `Runner.append`, `Runner.appendMixed`, `Runner.appendMulti`                                         | **Checked/proved composition:** accepted text/one-tool/mixed/multi-call terminal responses append in order while session sequence and tool-call counts remain exact.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Pure local sequential composition after the bounded DeepSeek validators.                                                                                                          | No live transport, cancellation, persistence, external execution, or full Harness session equivalence is claimed.                                                                                                                                                                                                                                                                                                                                                            |
| `DeepSeekApiSession.AcceptedApiResponse`, `acceptResponse`, `Runner.appendApi`                                                                                                                          | **Checked/proved response composition:** a decoded singleton index-zero response with supported finish and nonempty payload enters the same local session runner with exact local IDs and sequence/count invariants.                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Non-streaming API-to-session normalization.                                                                                                                                       | Extra choices, unsupported/empty terminal responses, transport, persistence, provider-ID authentication, external execution, and whole-session equivalence remain outside.                                                                                                                                                                                                                                                                                                   |
| `DeepSeekSessionRequest.executeCompleteAndAppend`, `appendAccepted_*`                                                                                                                                   | **Checked/proved indexed request-to-append composition:** a `Session.ModelRequest` with explicit source agreement crosses an injected complete transport, is admitted by the singleton response guard, and appends the accepted response to the same schema-indexed runner while retaining body, header, sequence/local-ID/tool-count invariants, and request reconstruction.                                                                                                                                                                                                                                                                                                     | End-to-end local complete-response bridge over the indexed request and extension runner.                                                                                          | Credentials, live/provider behavior, parser-backed tool-schema validity, persistence, external effects, and deployed Harness equivalence remain outside.                                                                                                                                                                                                                                                                                                                     |
| `DeepSeekSessionRequestBytes.buildCompleteBytePlan`, `executeCompleteBytesAndAppend`, `ByteCompleteAppendResult`                                                                                        | **Checked/proved/exercised byte-to-session composition:** the canonical UTF-8 request bytes are retained, an injected byte transport returns raw response bytes and decoded text, typed response admission succeeds, and the accepted response appends to the same schema-indexed runner with exact endpoint certificates.                                                                                                                                                                                                                                                                                                                                                        | Raw-byte transport refinement immediately above `DeepSeekSessionRequest`.                                                                                                         | Invalid UTF-8, HTTP/transport failures, semantic rejection, live networking, credentials, provider behavior, persistence, and deployed Harness equivalence remain outside.                                                                                                                                                                                                                                                                                                   |
| `DeepSeekHarnessLocalHttp.runCompleteAppendWithKey`, `CompleteAppendProbeResult`                                                                                                                        | **Checked/exercised local process bridge:** the same indexed complete-response append is driven through a one-shot loopback HTTP server and real curl, retaining port/request/validity/server-exit evidence beside the dependent accepted response and updated runner.                                                                                                                                                                                                                                                                                                                                                                                                            | Local process/HTTP evidence for the request-to-append composition.                                                                                                                | Remote reachability, TLS, credential/provider authenticity, executable trust, persistence, retries, and deployed Harness equivalence remain outside.                                                                                                                                                                                                                                                                                                                         |
| `DeepSeekHarness.RequestSource`, `buildChatRequest`, `executeRound`, `ExecutedTool`, `appendRoundToolResults`, `ConversationRunner`, `executeConversationRound`, `runConversation`                      | **Checked/exercised typed round and continuation:** local session messages become a request plan; the explicit transport and singleton response guard run; each returned function call retains parse, admission, policy, and dependent provider-reply evidence; certified outcomes can be encoded and appended as typed tool-result messages with exact local IDs, source references, message order, and protocol projection; `runConversation` repeats the certified round under explicit fuel and retains either a no-tool-call completion witness or explicit exhaustion; the test suite executes the deterministic two-response loop in both modes.                           | A bounded model-round composition over the existing API/session/generic-tool seams with a proof-carrying continuation state.                                                      | Persistence, remote credentials, asynchronous scheduling, process trust, request-boundary treatment of error tool results, cancellation, and deployed Harness equivalence remain outside.                                                                                                                                                                                                                                                                                    |

| `DeepSeekHarnessPersistence.RestoredRunner`, `restoreRunner`, `RequestCertificate`, `buildRequestCertificate` | **Checked/proved/exercised logical restore attachment:** a validated Harness JSONL archive restores a `ConversationRunner` whose session is exactly the archive's final session; a request certificate rebuilt from that runner has the same result as rebuilding from the archive session, and a deterministic tool-session archive exercises the message projection. | A logical persistence-to-harness continuation seam over the existing JSONL refinement. | Filesystem/compression/torn-tail repair, concurrent writers, archive authenticity, and complete deployed persistence equivalence remain outside. |
| `DeepSeekHarnessPersistenceIO.RestoredRunner`, `restoreRead`, `readRunner`, `replaceAndRestore`, `appendAndRestore` | **Checked/proved/exercised byte-backed attachment:** a decoded UTF-8/JSONL `ReadCertificate` from memory or a temporary file is tied to the logical persisted archive before restoring a `ConversationRunner`; replacement and validated append operations re-enter that same certificate before exposing the updated runner. | Composition of the byte/text persistence adapter with the DeepSeek runner restore seam. | Backend acknowledgement is not fsync; compression, torn-tail repair, concurrent writers, authenticity, crash recovery, and deployed persistence equivalence remain outside. |
| `DeepSeekHarnessPersistenceTransportRound.ConversationTransportToolRound`, `PersistedRound`, `executeSource`, `executeRestored` | **Checked/proved/exercised high-level bridge:** a restored byte-backed runner builds a complete typed request, crosses an injected transport, retains one response decoder plus `acceptValidated`, executes typed tool calls, and returns an exact assistant/tool endpoint tied back to the archive session; the memory/counter fixture runs this composition. | Persistence-to-request-to-single-decoder-transport-to-tool continuation above the existing local certificates. | Live network/credentials/provider obedience, fsync/stable media, retries, cancellation, external effects, and deployed Harness equivalence remain external. |
| `DeepSeekHarnessTransportConversation.TransportTrace`, `TransportStop`, `runTransport` | **Checked/proved/exercised bounded continuation:** single-decoder round certificates compose into an endpoint-indexed trace; completion retains the final no-tool proof and exhaustion retains the exact certified prefix, with a deterministic two-response fixture covering both stops. | Multi-round request/response/tool continuation above the persisted single-round bridge. | Live provider behavior, credentials, retries, cancellation, durability/fsync, external effects, and deployed Harness equivalence remain external. |
| `DeepSeekHarnessTransportRetry.ValidatedRetryResult`, `RetriedTransportRound`, `executeValidatedRetry`, `executeSource` | **Checked/proved/exercised bounded retry:** one complete request plan is reused across attempts; retryable transport/transient-HTTP failures remain in `RetryHistory`, the successful body is validated once, and the same dependent response feeds `acceptValidated` plus typed tool execution. The 503-to-200 fixture checks the exact assistant/tool endpoint and attempt bound. | Immediate retry composition over the typed injected-transport boundary. | Provider backoff/idempotency, cancellation, persistence, external effects, live-provider behavior, credentials, and deployed Harness equivalence remain external. |
| `DeepSeekHarnessTransportRetryConversation.RetryTransportTrace`, `RetryTransportStop`, `runTransport` | **Checked/proved/exercised retry-aware continuation:** retried single-decoder rounds compose into an endpoint-indexed fuel-bounded trace; each head retains `RetryHistory`, the tail starts at the exact prior runner/model endpoint, and a fixture retries a tool round before reaching a no-tool completion. | Multi-round retry composition above the typed injected-transport round. | Provider backoff/idempotency, cancellation, persistence, external effects, live-provider behavior, credentials, and deployed Harness equivalence remain external. |
| `DeepSeekHarnessEndToEnd.PersistedRetryRun`, `runFixture`, `summary`, `runSummary` | **Checked/proved/exercised end-to-end local composition:** a byte-backed validated archive restores the exact initial `ConversationRunner`, which remains the index of a retry-aware two-round trace; the executable fixture checks archive `nextSeq = 8`, final `nextSeq = 11`, one transient failure, model `0`, and typed completion. | Persistence-read attachment composed with the injected retry conversation. | Memory backend and deterministic transport only; no fsync/stable media, live provider/credentials, backoff/idempotency, cancellation, external effects, or deployed Harness equivalence. |
| `DeepSeekHarnessPersistenceProcessOutcome.PersistedProcessRound`, `executeRestored`, `runFixture`, `summary` | **Checked/proved/exercised process-backed composition:** a byte-backed validated archive restores the runner that indexes a streaming request plan, complete `IO.Process` body, rich outcome classification, dependent tool execution, and exact final endpoint; the shell fixture checks archive `nextSeq = 8`, endpoint `nextSeq = 10`, streaming mode, and body length `523`. | Persistence-read attachment composed with the actual local process outcome adapter. | No process/credential authenticity, incremental delivery, blocked-read cancellation, durable persistence, external effects, or deployed Harness equivalence. |
| `DeepSeekHarnessPersistenceStreamRetry.PersistedStreamRetryRun`, `runRestored`, `runFixture`, `summary` | **Checked/proved/exercised process-backed multi-round composition:** the byte-restored runner remains the initial index of a dependent streamed retry trace; a deterministic shell process emits two counter tool calls, the typed executions advance the runner, and the next serialized request receives terminal text. Executable projections check `8 -> 12`, two rounds, two first-round calls, one attempt, model `0`, and typed completion. | Persistence-read attachment composed with the actual process-backed streamed conversation loop. | No provider/process authenticity, durable recovery, backoff/idempotency, blocked-read cancellation, external effects, or deployed Harness equivalence. |
| `DeepSeekHarnessPersistenceStreamRetryCancellation.PersistedCancellationRun`, `runRestored`, `runFixture`, `summary` | **Checked/proved/exercised process-backed cancellation composition:** the byte-restored runner accepts one streamed tool round, then a pre-round timeout decision retains the exact one-round prefix, runner/model endpoint, round number, and reason before any second request is selected. Executable projections check `8 -> 11`, one round, two first-round calls, timeout, and model `0`. | Persistence-read attachment composed with the process-backed retry/cancellation boundary. | No in-flight process/HTTP interruption, cleanup, provider authenticity, durable recovery, external effects, or deployed cancellation equivalence. |
| `DeepSeekHarnessPersistenceFileStreamRetryCancellation.FilePersistedCancellationRun`, `runRestored`, `runFixture`, `summary` | **Checked/proved/exercised temporary-file process cancellation:** a real `DurableIO.FileBackend` writes and reads the archive before the restored runner enters the process-backed streamed cancellation trace; exact session equality, `8 -> 11` prefix, timeout reason, and temporary-file storage are retained. | Filesystem-backed persistence composition above the existing process/retry/cancellation seam. | `withTempFile` cleanup is scoped and backend acknowledgement is not fsync; stable media, crash recovery, in-flight interruption, process cleanup, provider authenticity, external effects, and deployed Harness equivalence remain external. |
| `DeepSeekHarnessEventFileStreamRetryCancellation.FileEventCancellationRun`, `runRestored`, `runFixture`, `summary` | **Checked/proved/exercised current-event temporary-file cancellation:** the supported current-Harness JSONL fixture is written to and read from a temporary file, exact source/read byte equality is retained before event archive/session restoration, and the restored runner enters the process-backed streamed cancellation trace with executable `8 -> 11`, one-round, two-call, timeout, and model-`0` projections. | Current-event text/byte refinement composed with filesystem-backed persistence and process/retry/cancellation. | `withTempFile` cleanup is scoped; fsync, stable media, crash recovery, blocked-read interruption, process cleanup, provider authenticity, external effects, and deployed Harness equivalence remain external. |
| `DeepSeekHarnessEventFileProcessSchema.FileSchemaProcessRun`, `executeFileRound`, `runFixture`, `summaryForRun` | **Checked/proved/exercised current-event file/schema composition:** the supported event JSONL fixture is restored from a temporary file, then indexes the heterogeneous weather/clock streaming plan, complete body, schema-dispatched tool step, and dependent `8 -> 11` runner endpoint; source/read bytes, archive/session, request/body, processed-step, and projection certificates remain attached. | Current-event text/byte refinement composed with filesystem-backed heterogeneous schema processing. | Provider obedience, credential authenticity, durable storage, byte framing, cancellation, external effects, and deployed Harness equivalence remain external. |
| `DeepSeekHarnessEventFileLocalSseSchema.FileSseSchemaRun`, `runFileRound`, `runFixture`, `summaryForRun` | **Checked/proved/exercised current-event file→loopback heterogeneous SSE composition:** the temporary-file restored session indexes two real request-indexed byte-prefix rounds, the scoped weather/clock registry validates both requests, complete byte evidence and distinct rebuilt bodies are retained, the first round executes two tools, and terminal completion reaches exact `8 -> 12`. | Current-event text/byte refinement composed with scoped registry execution and real loopback HTTP/SSE delivery. | The deterministic fixture model/server and scoped approval are local evidence; no provider obedience, credential authenticity, durable storage, blocked-read cancellation, external effects, or deployed Harness equivalence is derived. |
| `DeepSeekHarnessEventFileLocalSseSchemaErrors.FileFailureRun`, `runFixture`, `summaryForRun` | **Checked/proved/exercised current-event file→loopback heterogeneous SSE failure continuation:** the temporary-file restored session drives two validated requests, both scoped weather/clock providers return exact typed failures, certified `isError` tool results are appended, and terminal text completes at exact `8 -> 12`. | Current-event text/byte refinement composed with opt-in schema error results and real loopback HTTP/SSE delivery. | The deterministic model/server and scoped approval are local evidence; no provider obedience, credential authenticity, durable storage, blocked-read cancellation, external effects, or deployed Harness equivalence is derived. |
| `DeepSeekHarnessPersistenceStreamBytePrefixTimeout.PersistedTimedRun`, `CompletedPersistedTimedRun`, `runFixture`, `runCompletedFixture`, `summary`, `completedSummary` | **Checked/proved/exercised local composition:** a memory-backed validated archive restores the exact `ConversationRunner`; the fuel fixture checks `8 -> 10`, one timed streamed tool round, model `0`, and explicit exhaustion, while a terminal companion switches bodies after the first tool result and checks `8 -> 11`, two rounds, completion, model `0`, and restored-session equalities. | Persistence-read attachment composed with the timed byte-prefix streamed Harness boundary. | No stable-media durability, crash recovery, blocked-read interruption, process/provider authenticity, cleanup, external effects, or deployed Harness equivalence. |
| `DeepSeekHarnessTransportRetryCancellation.RetryCancellableTransportStop`, `RetryCancellableTransportRunResult`, `runAux`, `run` | **Checked/proved/exercised retry-aware pre-round cancellation:** the indexed injected-transport retry trace checks cancellation before each request, retains the exact dependent prefix and endpoint on cancellation, and distinguishes cancellation, completion, and fuel exhaustion; fixtures cover no-request cancellation plus 503 retry followed by terminal completion. | Composition of the complete-body retry trace with caller-controlled cancellation. | No blocked-read/in-flight IO interruption, provider backoff/idempotency, persistence, external effects, or deployed Harness retry/cancellation equivalence. |
| `DeepSeekHarnessEventArchive.SupportedEventLog`, `RestoredRunner`, `restoreRunner`, `buildRequestCertificate` | **Checked/proved/exercised current-event attachment:** a lossless `ArchivedLog` and stateful `ValidatedJsonLog` must agree before a `ConversationRunner` can be restored; the restored session is exactly the validated endpoint, raw event order is preserved, opaque/extension events reject the path, and the supported tool-message fixture rebuilds a typed request. | A certificate-gated bridge from the full current-Harness event vocabulary into the local runner. | Unsupported payload semantics, opaque-event replay, filesystem/compression/durability, provider credentials, and complete deployed Harness equivalence remain outside. |
| `DeepSeekHarnessEventIgnorableProjection.Projection`, `SupportedProjection`, `projectSupported`, `ignorable_fixture_summary`, `required_fixture_rejected` | **Checked/proved/exercised archive projection:** the physical archive gets an indexed keep/drop ledger; only opaque `ignorable: true` rows are dropped, supported rows retain raw positions and decode certificates, and a required opaque vendor row returns `requiredOpaque`. | Source-authorized ignorable-row projection below runner restoration. | No sequence renumbering, local `SessionRefinement` replay, payload semantics, persistence, or deployed Harness equivalence is claimed. |
| `DeepSeekHarnessEventIgnorableNormalization.normalize`, `NormalizedLog`, `ignorable_middle_summary`, `ignorable_middle_source_positions` | **Checked/proved/exercised supported-subset normalization:** retained projected rows receive contiguous local sequence numbers, supported `sourceEventSeqs`/`surfaceOp` references are remapped through the physical-to-local map, and the rewritten JSON is validated into a local session endpoint; duplicate physical sequences, missing references, malformed rewrites, required opaque rows, and semantic failures reject. | Source-authorized ignorable-row projection → bounded local replay seam. | Opaque payload semantics, persistence, provider authenticity, and complete deployed Harness equivalence remain external. |
| `DeepSeekHarnessEventSimulation.replayOccurrences`, `SourceReplay`, `SourceLedger`, `toolNormalizedSimulation` | **Checked/proved/exercised occurrence-indexed simulation:** every normalized retained occurrence is consumed by a `ReplayStep` carrying its exact pre-state refinement; the typed decision ledger preserves keep/drop positions, and the dependent replay proves source/local sequence projections, protocol erasure, and final session-projection equality. | Bounded normalized source log → intrinsic local transition trace. | Finite supported subset only; provider behavior, opaque semantics, bytes, persistence, cancellation delivery, and complete deployed Harness equivalence remain external. |
| `DeepSeekHarnessEventArchiveReplay.ArchiveReplay`, `archiveReplay`, `toolArchiveReplay`, `ArchiveReplay.droppedRaw_eq_decisionDrops` | **Checked/proved/exercised archive-aware replay:** the normalized `SourceReplay` and inductive keep/drop ledger share one dependent certificate; all physical archive rows, retained raw rows, dropped opaque rows/positions, and the normalized endpoint are projected exactly. The tool fixture checks nine rows, eight supported transitions, and one explicit opaque no-op. | Lossless physical archive → occurrence-indexed supported transition replay. | Still a finite supported-subset bridge: dropped rows are explicitly ignorable no-ops; opaque payload semantics, provider behavior, bytes, persistence, cancellation delivery, and deployed Harness equivalence remain external. |
| `DeepSeekHarnessEventIgnorableRunner.restoreRunner`, `buildRequestCertificate`, `toolNormalizedRunner`, `toolNormalizedRequest` | **Checked/proved/exercised runner attachment:** the normalized final session becomes a typed `ConversationRunner` with exact session/step/tool-call-count witnesses, and the tool fixture rebuilds a dependent `ChatRequest` with expected user/assistant/tool messages. | Bounded normalized-log → local runner/request attachment. | Provider authenticity, transport, persistence, opaque payload semantics, and complete deployed Harness equivalence remain external. |
| `DeepSeekHarnessEventIgnorableTransport.runRestoredTransport`, `runToolNormalizedTransport`, `toolNormalizedTransport`, `RestoredTransportRun` | **Checked/proved/exercised transport continuation:** a normalized runner enters the existing process-backed complete-response conversation trace, retaining the original session/step certificates beside final runner/model/trace/stop evidence; the deterministic fixture exercises one no-tool completion. | Bounded normalized-log → injected transport conversation attachment. | Credentials, provider authenticity, durable persistence, external effects, and complete deployed Harness equivalence remain external. |
| `DeepSeekHarnessEventText.restoreTextRunner`, `restoreBytesRunner`, `RestoredTextRunner`, `RestoredBytesRunner` | **Checked/proved/exercised text attachment:** UTF-8/JSONL text or bytes retain exact source/decoded text, archive lines, validated session, and restored-runner equalities; invalid UTF-8 and opaque/extension events reject. | Text/byte ingress immediately above the certificate-gated current-event archive seam. | No external logger framing, transport, persistence, provider authenticity, opaque replay, or deployed Harness equivalence claim. |
| `DeepSeekHarnessEventProcessTimeout.TimeoutPrefixStop`, `TimedProcessPrefixResult`, `executeWithTimeout`, `blockedReadProcessRun`, `delayedToolProcessRun`, `fastProcessRun` | **Checked/proved/exercised local blocked-read interruption:** a per-read `Std.Async.Sleep` races the synchronous process stdout read; a timer winner kills/waits the configured child, while the dependent result retains the accepted cursor prefix, line ledger, exit code, stderr, timeout index, and endpoint theorem; completion and timeout fixtures are executable. | Timer-driven interruption directly below the current-event process-prefix cursor. | No arbitrary descendant cleanup, fairness, backpressure, provider/executable authenticity, crash durability, or deployed asynchronous Harness equivalence. |
| `DeepSeekHarnessEventProcessTimeoutRefinement.PrefixValidatedLog`, `Cursor.validatedLog`, `executeAndValidateWithTimeout` | **Checked/proved proof-level composition:** the timed cursor's stored per-entry `RefinedEvent` witnesses are transported from its snoc `PrefixSequence` into a cons-shaped `SessionRefinement.ValidatedSequence`, retaining exact entries, final state, and protocol projection without reparsing. | Cursor-to-session-sequence attachment above the executable timeout reader. | The attachment is noncomputable proof packaging; no raw `decodeEvents` equivalence, JSONL byte framing, provider authenticity, persistence, or deployed Harness refinement is claimed. |
| `DeepSeekHarnessPayloadText.restoreTextPayloadRunner`, `restoreBytesPayloadRunner`, `RestoredPayloadRunner`, `RestoredBytesPayloadRunner` | **Checked/proved/exercised payload-preserving attachment:** the runner and `SessionPayloadArchive.PayloadLog` share the exact parsed-line index; raw payload objects, block tags, usage, and tool-result metadata are retained beside the semantic runner. | Payload-shape retention immediately above the text event-archive seam. | No provider schema semantics, opaque replay, logger/transport/persistence, or deployed Harness equivalence claim. |
| `DeepSeekHarnessPayloadPersistence.restoreRunner`, `restoreBytesRunner`, `restoreRead`, `RestoredRunner`, `RestoredBytesRunner`, `ReadRestoredRunner` | **Checked/proved/exercised persisted payload attachment:** validated logical JSONL, pure bytes, and executable memory/temporary-file reads retain the expanded-event payload log beside the exact restored runner/session endpoint. | Persistence composition of the semantic runner and raw current-Harness payload archive. | Payload/storage errors remain separate; no provider schema semantics, fsync, crash atomicity, torn-tail repair, or deployed persistence/Harness equivalence is claimed. |
| `DeepSeekHarnessOpaqueMetadata.RestoredRunner`, `restoreRunner`, `RequestCertificate`, `metadataRestored_metadata_exact` | **Checked/proved/exercised metadata attachment:** a sanitized validated session restores a runner while the exact tool-result `error`/`meta` JSON values remain in source order beside it; request reconstruction uses only the sanitized local session. | A lossless quarantine bridge for provider/tool-owned metadata. | No provider/tool schema semantics, opaque replay/resume behavior, persistence, or complete deployed Harness equivalence. |
| `DeepSeekHarnessMetadataArchive.AttachedLog`, `RestoredRunner`, `validate`, `metadataAttached_has_one_opaque_event` | **Checked/proved/exercised composition:** the full current-event envelope archive and sanitized metadata log share one input; raw envelope order, the known opaque tool-result record, exact metadata, and the sanitized runner/request projection are all retained. | Current-event archive + quarantined metadata attachment. | No provider/tool schema semantics, opaque replay, persistence, or complete deployed Harness equivalence. |
| `DeepSeekHarnessErrors.executeFunctionCallRecoverable`, `executeFunctionCallsRecoverable`, `ConversationRunner.appendRecoverableToolResults`, `executeConversationRoundRecoverable` | **Checked/proved/exercised opt-in failure continuation:** provider `.error` results become `ProviderFailedTool` evidence with exact parsed call, admission, allow-policy, provider message, and model-preservation fields; `.include` request policy appends those failures as `isError` tool messages and a subsequent typed request can consume them. | A complete-body recoverable continuation over the existing pure runner. | The default policy remains fail-closed; parse/admission/policy errors still fail the round, and retries, cancellation, persistence, asynchronous delivery, real provider behavior, and deployed Harness equivalence remain outside. |
| `DeepSeekHarnessRetry.RetryPolicy`, `RetryHistory`, `executeWithRetry`, `executeConversationRoundRetry` | **Checked/proved/exercised bounded retry:** an explicit policy retries only transport/transient-HTTP `ClientError`s, retains the prior failure list under a proof-carrying bound, reuses one request plan, and carries the history into a successful typed conversation continuation. | Complete-body immediate retry over the explicit transport seam. | No provider backoff, idempotency of arbitrary requests/tools, cancellation, persistence, asynchronous delivery, or deployed Harness retry equivalence is claimed. |
| `DeepSeekHarnessCancellation.CancellationPolicy`, `CancellableStop`, `CancellableRunResult`, `runConversationCancellable` | **Checked/proved/exercised pre-round cancellation:** a caller decision is evaluated before each complete request round; cancellation retains the exact completed witness prefix, unchanged runner/model endpoint, and decision proof, while completion and fuel exhaustion remain distinct. | Boundary-safe control over the pure complete-body conversation runner. | No interruption of an in-flight process, HTTP request, stream reader, or external tool; no cleanup, backpressure, persistence, asynchronous delivery, or deployed Harness cancellation equivalence is claimed. |

`Cordis.DeepSeekStreamHarnessCancellation` provides the analogous boundary for
`runConversationMultiStream`: cancellation is checked before a complete streamed round and
retains the exact completed streamed prefix plus runner/model endpoint. It does not claim to
interrupt an in-flight process read or to provide deployed cancellation semantics.

`Cordis.DeepSeekStreamHarnessPrefix` connects that continuation to the checked line-oriented
process prefix: completion appends the certified multi-call tool results, while a line policy or
read budget returns the exact parsed prefix and typed stop. Byte framing, blocked-read
interruption, and deployed stream semantics remain outside the claim.

`Cordis.DeepSeekAsyncHarness` is the narrow executable race bridge over the same process prefix:
two fixture jobs run as cooperative `ContextAsync` children, `ContextAsync.race` returns the first
typed result and requests cancellation of the loser, and a pure theorem classifies the winner as a
terminal phase. Because each job still performs a synchronous line-oriented read, this is not
blocked-read interruption, fairness, arbitrary cleanup, or deployed asynchronous equivalence.

`Cordis.DeepSeekAsyncStreamHarness` lifts that observation over the streamed dependent
tool/session continuation. Each child can execute a tool-call round and a later text terminal under
explicit fuel; the winning result retains typed tool executions, round witnesses, final model, and
runner endpoint. Synchronous reads, fairness, cleanup, and deployed async equivalence remain
external.

`Cordis.DeepSeekAsyncStreamCancellation` carries a cancellation policy through the same streamed
race. The executable fixture cancels one child before round zero and retains the typed reason,
unchanged runner/model endpoint, and empty completed-round prefix; the sibling remains a real
process-backed continuation. This is a pre-round boundary, not blocked-read interruption or
deployed cancellation equivalence.

`Cordis.DeepSeekStreamHarnessErrors` carries the opt-in provider-failure policy through the same
complete-body stream boundary: typed failure evidence and model preservation are retained, and
the streamed session receives an `isError` tool result for a later `.include` request. Its
fuel-bounded loop retains that failed round and a later streamed text terminal. Incremental
recovery, retries, persistence, and deployed error behavior remain external.

`Cordis.DeepSeekStreamHarnessRetry` adds a checked bounded retry over that complete-body stream
boundary. Process and transient-HTTP failures retain ordered `SessionClientError` history and
may retry under policy; stream framing, semantic response, and tool failures remain terminal.
Backoff, idempotency, cancellation, persistence, and deployed retry equivalence remain external.

`Cordis.DeepSeekStreamHarnessRetryConversation` composes that process-backed retry round into an
indexed fuel-bounded trace. Heads retain complete SSE bodies, ordered histories, dependent
assistant/tool endpoints, and exact runner/model tail indices; the executable fixtures cover a
streamed tool-to-text completion and an exhausted transient-HTTP policy. Backoff, idempotency,
blocked-read cancellation, persistence, reconnects, external effects, and deployed retry
equivalence remain external.

`Cordis.DeepSeekStreamHarnessRetryCancellation` composes that trace with a pre-round cancellation
policy. Its cancellation result retains the accepted retry-aware prefix, endpoint, decision
certificate, and each head's retry history; no claim is made about interrupting blocked IO,
cleanup, reconnect, or deployed Harness cancellation semantics.
`Cordis.DeepSeekAsyncStreamRetryCancellation` lifts that boundary into a cooperative race. Its
dependent `JobResult` keeps the retry policy in typed errors and the final endpoint in typed
successes; `RaceResult` records the winning child. The fixtures include a real process-backed
cancellation-first race and a delayed-child success-first race with two accepted rounds, but
fairness, blocked-read interruption, cleanup, reconnect, and deployed async retry/cancellation
equivalence remain external.

### Current-development transformation and operation independence

Local sources: [`Cordis/Transformation.lean`](../Cordis/Transformation.lean),
[`Cordis/OperationIndependence.lean`](../Cordis/OperationIndependence.lean),
[`Cordis/MediatedTheorem.lean`](../Cordis/MediatedTheorem.lean), and
[`Cordis/PartialTransformation.lean`](../Cordis/PartialTransformation.lean), followed by the
relation-respecting descent in
[`Cordis/ObservationalPartialTransformation.lean`](../Cordis/ObservationalPartialTransformation.lean).

| Lean declaration                                                                                                                            | Status and exact Lean guarantee                                                                                                                                                                                                                       | Paper correspondence                                              | Boundary                                                                                                                                                                    |
| ------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `Transformation.EffectGenerator`, `Closure`, `OfEffect`, `seq_monoid_subset_joint`                                                          | **Proved:** an effect monoid is the least identity/composition closure of its forward map and every yielded inverse; generator commutation closes; sequencing adds no source.                                                                         | Definition 17 and Lemma 18.                                       | Exact total `Cordis.Effect`; partial/Kleisli computation monoids and removal are handled in later rows.                                                                     |
| `Transformation.Independent`, `of_generators`, `independentAt`, `seq_commute`                                                               | **Proved:** full cross-monoid commutation and bidirectional yielded-inverse stability promote from generators and imply the exact adjacent certificate.                                                                                               | Definition 19.                                                    | Adjacent equality is a consequence, not the definition; removal and inverse-order results are the next row.                                                                 |
| `Removal.RemovalTrace`, `removal_inverse_relation`, `later_inverses_unchanged`, `inverse_permutation_recovers`                              | **Proved:** an indexed original/omitted suffix trace retains every later application state and inverse equality; any permutation of exact yielded inverses recovers the start.                                                                        | Theorem 20 and Corollary 21.                                      | Finite pairwise-independent exact effects only; no observational, asynchronous, or external-effect theorem.                                                                 |
| `ExactOperationIndependent`, `OutcomeStable`, generator promotion                                                                           | **Proved:** full exact-effect independence plus heterogeneous outcome stability holds over every foreign transformation.                                                                                                                              | Definition 39 for total exact operations.                         | Total exact interpretation only; partial dependent operations use the separate finite-word model.                                                                           |
| `FiniteKeyIndependent`, `distinctKeys_finiteIndependent`                                                                                    | **Proved:** arbitrary finite forward/inverse words at distinct dependent keys commute including undefinedness, and preserve complete successor/inverse/outcome data.                                                                                  | Finite executable form of Theorem 40.                             | Paper states abstract transformation monoids; this theorem quantifies over intrinsic finite word syntax.                                                                    |
| `Computation`, `Computation.run`, `run_recovers`                                                                                            | **Checked/proved:** outcomes select typed continuations and every successful mediated computation composes inverses in LIFO order and recovers exactly.                                                                                               | Definition 41.                                                    | The interpreter is partial finite syntax; it is not external asynchronous execution.                                                                                        |
| `MediatedClosure`, `ForwardOnlyGap.inverse_stability_fails`                                                                                 | **Checked/proved boundary:** forward commutation cannot establish inverse stability; complete exact outcome-dependent closure is named explicitly.                                                                                                    | Initial exact closure frontier for T42.                           | Exact representative equality is stronger than the paper's quotient reading, as the next row proves.                                                                        |
| `RealizedPath`, `ObservationalMediatedClosure`, `PairwiseOverlapComplete`, `toExact`, representative counterexample                         | **Checked/proved/boundary:** realized paths retain exact branches; representative coherence promotes quotient closure; finite models separate exact representatives and expose the old partial-domain defect.                                         | Initial observational T42 specification.                          | The old `PairwiseOverlapComplete` is false for partial computation versus unit; neither counterexample refutes the paper's total theorem.                                   |
| `BoundedPartialIndependence`, `rawComputation_swap_success`, `pairwiseOverlap_boundedPartialIndependence`, `partialPairwiseOverlapComplete` | **Proved:** composite partial domains, exact after/undo, typed outcomes, and conditional yielded inverses are preserved while swapping arbitrary finite mediated computation trees.                                                                   | Finite whole-run analogue of Theorem 42.                          | Whole-run evaluation is not the full transformation monoid of Definition 19; the older individual-domain closure is kernel-refuted.                                         |
| `PartialMap`, `Closure`, `Independent`, `pairwiseOverlap_independent`, `Independent.toBoundedPartial`, `WholeRunGap`                        | **Proved:** complete partial forward/yielded-inverse Kleisli monoids commute across pairwise-overlapping trees and preserve domains/inverses under every successful foreign closure map.                                                              | Finite partial analogue of D17/D19/T42.                           | Success-conditional exact partial semantics; not the paper's literal total/quotient, external, asynchronous, or infinite setting.                                           |
| `ObservationalPartialTransformation.MapsRelated`, `GeneratorRespects`, `evaluate_related`, `pairwiseOverlap_independent`, `RespectGap`      | **Proved:** exact computation closures descend to relation-respecting partial closures; related representatives agree on definedness, successors, outcomes, branch choices, and yielded inverses.                                                     | Finite observational D17/D19/T42 analogue.                        | Assumes exact `PairwiseOverlap`; finite representative/Kleisli semantics and success-conditional stability are weaker than the paper's unrestricted total quotient setting. |
| `TotalQuotientIndependence.TotalComputation`, `Closure`, `TotalIndependent`, `effect`, `effect_admissible`                                  | **Proved:** an explicit success-at-every-context certificate selects total forward/yielded-inverse maps, links every finite total word to the existing exact closure, and derives quotient commutation, yield stability, recovery, and admissibility. | Finite totalized/quotient specialization of D17/D19/T42.          | Totality is an explicit certificate over the finite context model; this is not the paper's unrestricted total/quotient, external, asynchronous, or infinite theorem.        |
| `DomainTotalQuotientIndependence.DomainTotalComputation`, `DomainMap`, `Closure`, `DomainIndependent`, `independent_of_partial`             | **Proved:** a named invariant domain certifies totality and preservation for partial forward/yielded-inverse maps; closure membership and finite observational independence are transported without erasing behavior outside the domain.              | Domain-certified finite total/quotient refinement of D17/D19/T42. | The domain and exact overlap certificate are supplied. This is not unrestricted totality, quotient-only operation independence, or an external/asynchronous theorem.        |

### Current-development finite schedules

| Lean declaration                                        | Status and exact Lean guarantee                                                                                                                   | Paper correspondence                                      | Boundary                                                                                                                                        |
| ------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- |
| `Schedule.runEffects_eq_of_perm`, `CertifiedSchedule.*` | **Proved:** every permutation of a finite family carrying a pairwise complete-effect commutation certificate denotes the same successor and undo. | Adjacent to Definitions 19–21 and temporal composability. | Sequential pure semantic reordering, not transformation-monoid independence, arbitrary removal, tasks, cancellation, or wall-clock concurrency. |

### Current-development bounded scheduler slice

Local sources: [`Cordis/ParallelHarness.lean`](../Cordis/ParallelHarness.lean) and
[`Cordis/AsyncHarness.lean`](../Cordis/AsyncHarness.lean).

| Lean declaration                                   | Status and exact Lean guarantee                                                                                                                                                                                                            | Harness correspondence                                                                   | Boundary                                                                                                                                                                               |
| -------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `ParallelWindow`, `WindowOutcome.execute`          | **Proved:** a finite task window carries explicit parallel-mode, ID, effect, commutation, and result-permutation certificates; scheduled evaluation reaches the canonical endpoint and exposes model-order committed results.              | A proof-carrying analogue of the pinned tool-call scheduler's overlap-then-commit shape. | The task/result permutation and effect laws are supplied fields. Evaluation is a pure fold; no `IO`, wall-clock overlap, worker failure, fairness, or TypeScript refinement is proved. |
| `Plan`, `Plan.execute`                             | **Proved:** an optional `ExclusiveTask` is run after the certified window, with exact endpoint and recovery equality; its mode is intrinsically `.exclusive`.                                                                              | Exclusive barrier after a batch in the current Harness scheduler.                        | The barrier is one explicit pure task. Dynamic admission, nested barriers, retries, and external resource isolation remain outside.                                                    |
| `drain`, `drainOutcome`                            | **Proved:** draining a finite pending list emits one synthetic cancellation report per task, preserves the model exactly, and preserves task IDs/order.                                                                                    | Synthetic abort/cancellation reporting at a scheduler boundary.                          | This is a pure cancellation model; it does not prove cancellation of running promises, cleanup, persistence, or crash recovery.                                                        |
| `AsyncHarness.Step`, `Trace`, `SuccessfulSchedule` | **Proved:** indexed fibers move through pending/running/terminal phases under typed start/complete/fail/cancel steps; successful traces expose completion order, and a drained permutation certificate proves the canonical pure endpoint. | Bounded async/fiber lifecycle analogue for the Harness scheduler.                        | No live task handles, wall-clock fairness, promise cancellation delivery, cleanup, external IO, or deployed Harness refinement is proved.                                              |

`Cordis.ParallelSchedule` extends this bounded slice to an arbitrary finite
sequence of the same two segment kinds. `Plan.execute` folds the segment
certificates, proves exact equality with the canonical composed effect and
newest-first recovery, emits reports in model order, and preserves global task
ID uniqueness across all windows and barriers. It remains a pure schedule
certificate: no wall-clock overlap, worker IO, promise race, fairness, or
TypeScript scheduler refinement is claimed.

### Current-development durable settlement boundary

Local sources: [`Cordis/DurableSettlement.lean`](../Cordis/DurableSettlement.lean),
[`Cordis/DurableCodec.lean`](../Cordis/DurableCodec.lean), and
[`Cordis/DurableBytes.lean`](../Cordis/DurableBytes.lean), and
[`Cordis/DurableIO.lean`](../Cordis/DurableIO.lean).

| Lean declaration                                                                             | Status and exact Lean guarantee                                                                                                                                                                                                                              | Harness correspondence                                                | Boundary                                                                                                                                                                                                  |
| -------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `DurableSettlement.Log`, `Log.entries_length`, `Log.frames_length`, `Log.entries_map_frames` | **Proved:** an intrinsically indexed append-only log carries exact current state, sequence, digest, chronological entries, and raw frames; frame count and entry/frame projection agree definitionally through the indexed constructors.                     | A typed write-ahead/commit-frame analogue for a finite local history. | The transcript digest is a collision-free `List Nat`, not a cryptographic hash; the log is a pure Lean value, not bytes on disk.                                                                          |
| `Log.recovers`, `CrashPrefix.recovers_initial`, `CrashPrefix.retained_frames_are_prefix`     | **Proved:** every indexed log has an exact newest-first inverse, and a supplied crash-prefix certificate recovers the retained prefix to the initial state while exposing the discarded frame suffix.                                                        | Torn-log-prefix recovery and checkpoint selection.                    | `CrashPrefix` is a proof-carrying cut, not an arbitrary-file scanner; no fsync, partial-write detector, storage fault model, or external-side-effect rollback is inferred.                                |
| `CrashPrefix.resume`, `CrashPrefix.resume_recovers`                                          | **Proved:** a new typed entry can be appended after the recovered prefix and the resumed log retains exact sequence/digest/index/recovery witnesses.                                                                                                         | Resume after a retained durable checkpoint.                           | This is pure prefix resume only; process coordination, fork/conflict policy, authentication, retries, and global exactly-once execution remain external.                                                  |
| `DurableCodec.frameCodec`, `scanPrefix`, `scanJsonPrefix`                                    | **Proved:** raw numeric frame fields round-trip through the JSON AST; entry decoding and sequence/previous/successor/digest checks construct a typed `Log`, while malformed, torn, unknown, or non-contiguous frames return structured errors.               | Persistence-facing AST validation before typed settlement.            | JSON AST only: no byte parser, UTF-8/transport, filesystem, fsync, partial-write repair, or arbitrary crash-cut inference.                                                                                |
| `DurableBytes.decodeFrame`, `decodeFrames`, `scanBytesPrefix`                                | **Proved:** an explicit unary-length `List UInt8` format round-trips payloads, decodes numeric `RawFrame` bytes, retains a counted unconsumed suffix, and composes that prefix with `scanPrefix`.                                                            | Pure binary-prefix refinement below the AST boundary.                 | The byte format is a Lean contract, not Harness JSON-byte compatibility; frame count is supplied, and filesystem, fsync, authentication, and external durability remain outside.                          |
| `DurableIO.AppendPlan`, `readAndRecover`, `RecoveryCertificate`                              | **Checked/executable:** typed append plans write encoded bytes through memory or `IO.FS`, and recovery scans a counted prefix into a typed log while retaining a discarded suffix; `TestSuite` exercises memory resume, a torn suffix, and a temporary file. | Stateful adapter boundary below the pure durable settlement model.    | Host `IO` success is only an acknowledgement. No fsync/stable-media, crash-atomicity, authentication, multi-process coordination, arbitrary-file repair, or external-effect exactly-once theorem follows. |

### Current-development global registry and orchestration

Local sources: [`Cordis/GlobalRegistry.lean`](../Cordis/GlobalRegistry.lean),
[`Cordis/GlobalDynamics.lean`](../Cordis/GlobalDynamics.lean), and
[`Cordis/GlobalLifecycle.lean`](../Cordis/GlobalLifecycle.lean), plus the combined
[`Cordis/GlobalCalculus.lean`](../Cordis/GlobalCalculus.lean) relation and
[`Cordis/GlobalTraceFacts.lean`](../Cordis/GlobalTraceFacts.lean) audit, followed by
[`Cordis/GlobalTemporal.lean`](../Cordis/GlobalTemporal.lean)'s conditional recovery algebra and
[`Cordis/GlobalIteratorIndependence.lean`](../Cordis/GlobalIteratorIndependence.lean)'s
oracle-specific iterator-family certificate and temporal per-step bridge, followed by
[`Cordis/GlobalTransposition.lean`](../Cordis/GlobalTransposition.lean)'s bounded raw-diamond and
totalized pre-edit map squares, followed by
[`Cordis/GlobalForeignPhase.lean`](../Cordis/GlobalForeignPhase.lean)'s compositional read/frame
laws, derived compatibility, and framed raw diamond, followed by
[`Cordis/GlobalLandingTransposition.lean`](../Cordis/GlobalLandingTransposition.lean)'s exact
four-case L-Iter/L-Finish lifecycle diamond, followed by
[`Cordis/GlobalActivationTransposition.lean`](../Cordis/GlobalActivationTransposition.lean)'s
all-nine fixed-program Begin/Iter/Finish diamond and actual-second-step wrapper, followed by
[`Cordis/GlobalActivationOrchestrationTransposition.lean`](../Cordis/GlobalActivationOrchestrationTransposition.lean)'s
corrected occurrence-framed activation/orchestration exchange and literal-clause countermodels,
followed by [`Cordis/GlobalTraceRewrite.lean`](../Cordis/GlobalTraceRewrite.lean)'s exact
assignment-preserving rewrite of intrinsic adjacent trace windows, followed by
[`Cordis/GlobalDeletion.lean`](../Cordis/GlobalDeletion.lean)'s intrinsic keep/drop replay and
exact safe multi-vestigial orchestration suffix theorem, followed by
[`Cordis/GlobalPaperRelation.lean`](../Cordis/GlobalPaperRelation.lean)'s birth/clock-erased rule
observation and actual orchestration simulation, followed by
[`Cordis/GlobalProgress.lean`](../Cordis/GlobalProgress.lean)'s conditional state-local
no-deadlock theorem and progress countermodels, followed by
[`Cordis/GlobalProgressTermination.lean`](../Cordis/GlobalProgressTermination.lean)'s supplied
strict-potential budget and conditional `K + 4` trace bound, followed by
[`Cordis/GlobalProgressRun.lean`](../Cordis/GlobalProgressRun.lean)'s finite dependent runner,
which retains lifecycle traces, endpoint well-formedness, and quiescent/full-fuel stop
certificates under supplied authority and potential, followed by
[`Cordis/GlobalProgressAssignment.lean`](../Cordis/GlobalProgressAssignment.lean)'s explicit
lifecycle-step provenance authority and dependent trace-assignment reconstruction, followed by
[`Cordis/GlobalSupport.lean`](../Cordis/GlobalSupport.lean)'s combined-order recursion,
support/active theorem, and mixed-cycle/parent countermodels,
[`Cordis/GlobalRelations.lean`](../Cordis/GlobalRelations.lean)'s explicit observation candidates,
the orchestration invariance theorem in
[`Cordis/GlobalRuleInvariance.lean`](../Cordis/GlobalRuleInvariance.lean),
the lifecycle-observation substrate in
[`Cordis/GlobalRuleObservations.lean`](../Cordis/GlobalRuleObservations.lean),
the conditional lifecycle theorem in
[`Cordis/GlobalLifecycleBisimulation.lean`](../Cordis/GlobalLifecycleBisimulation.lean),
the structural nominal action in
[`Cordis/GlobalNameAction.lean`](../Cordis/GlobalNameAction.lean),
the conditional dynamics/lifecycle action in
[`Cordis/GlobalNameLifecycle.lean`](../Cordis/GlobalNameLifecycle.lean),
the corrected vestigial-entry slice in
[`Cordis/GlobalVestigial.lean`](../Cordis/GlobalVestigial.lean),
with the finite spatial consequences in
[`Cordis/GlobalSpatial.lean`](../Cordis/GlobalSpatial.lean).

| Lean declaration                                                                                                                                                                           | Status and exact Lean guarantee                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Paper correspondence                                                                                              | Boundary                                                                                                                                                                                                                                                                                                                                                           |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `StaticSignature`, `ComponentDecl`, `Fiber`, `Phase`, `GlobalState`                                                                                                                        | **Checked intrinsically:** global state stores only opaque execution codes, finite declarations, private typed tables, parents, and phase data.                                                                                                                                                                                                                                                                                                                                                                   | Data portions of Definitions 43–45 and 49.                                                                        | No function over `GlobalState` is stored, avoiding negative recursion; external dynamics and iterator meaning are deferred.                                                                                                                                                                                                                                        |
| `WellFormed`, `Ancestor.birth_lt`, `parent_acyclic`                                                                                                                                        | **Proved:** provision ownership and committed providers are coherent, parents are present and strictly older, and no positive parent chain cycles.                                                                                                                                                                                                                                                                                                                                                                | Strengthened Definition 45/58 structural invariant.                                                               | `parent_older` is explicit strengthening; parent presence alone in Definition 58 would permit a cycle.                                                                                                                                                                                                                                                             |
| `ActiveProvider`, `activeContext_value_iff`, `IsTargetView.unique`, `targetView_*`, `Quiescent`                                                                                            | **Proved/checked:** providers/values and target views are unique, and the derived heterogeneous active context has exactly the values supplied by active fibers.                                                                                                                                                                                                                                                                                                                                                  | Definitions 45–46 and widened D49 quiescence.                                                                     | Uses noncomputable unique choice over proof-finite registries; no live runtime lookup, read confinement, or asynchronous state.                                                                                                                                                                                                                                    |
| `OrchestrationStep`, `preserve_insert`, `preserve_retire`, `preserve_remove`, `Trace.preservesWellFormed`                                                                                  | **Proved:** explicit-freshness insert, unconditional retire, and retired/inactive/childless remove preserve strengthened well-formedness over traces.                                                                                                                                                                                                                                                                                                                                                             | O-Insert/O-Retire/O-Remove; orchestration part of T59.                                                            | This is code-free structural preservation; external iterator/registration semantics are the next row, while lifecycle/full T59 remain open.                                                                                                                                                                                                                        |
| `GlobalDynamics.Dynamics`, `RegistrationAdmission`, `IterationStep`, `Accumulator`, fueled `runFuel`                                                                                       | **Checked/proved under explicit external laws:** ordinary and registration code execution reconstruct recovery, confinement/read/respect/WF evidence; traces and newest-first code accumulators preserve WF and recover; exhaustion retains the next code.                                                                                                                                                                                                                                                        | Definitions 47–48/51 and fueled D52 substrate.                                                                    | Ordinary WF/read/equivariance are explicit integrator obligations; phase updates are handled only by the next bounded layer.                                                                                                                                                                                                                                       |
| `GlobalLifecycle.Transition`, `Landing`, `RecoveryAdmission`, lifecycle `Trace`                                                                                                            | **Checked/proved:** exact target/phase guards, executed iterator landings, inertia, leave/unload, and all eight constructors preserve strengthened WF over finite traces.                                                                                                                                                                                                                                                                                                                                         | Seven lifecycle rules from Sections 4.3.1–4.3.4.                                                                  | General unload recovery is a named admission; orchestration is not folded into this relation, oracle rejection is unmodeled, and full D53/T59 are unclaimed.                                                                                                                                                                                                       |
| `GlobalCalculus.Step`, combined `Trace`, `FromEmpty`, `installation_semantics`                                                                                                             | **Proved:** one exact-endpoint relation projects to all ten rule names and acted-on names, separates Equation 51 maps from edits, preserves WF, and tracks actual installation-status boundaries from an empty registry.                                                                                                                                                                                                                                                                                          | Bounded finite Definition 53 and first Lemma 54 fact.                                                             | Sequential finite traces only; recovery admission remains supplied, oracle rejection is unmodeled, and trace facts are the next row.                                                                                                                                                                                                                               |
| `RecoveryConfinement`, `foreignTables_preserved`, `actorStatic_continuous`, aligned trace/episode facts                                                                                    | **Proved/conditional:** non-unload rules preserve foreign fibers exactly; unload confinement yields table/control/static continuity; name-specific episodes open/close at Begin/Unload.                                                                                                                                                                                                                                                                                                                           | Bounded fragments of Lemma 54.                                                                                    | Bare recovery admission is formally insufficient; new-entry/retire-write provenance, temporal composability, and remaining global lemmas are open.                                                                                                                                                                                                                 |
| `Step.partialMap`, `EffectEquiv`, `TotalStepMap`, per-step commutation, `recover_interleaved`                                                                                              | **Proved under explicit certificates:** fallible off-source maps totalize only with evidence; per-record recovery commutation folds over foreign replay; owner inversion and reordering yield finite relational recovery.                                                                                                                                                                                                                                                                                         | Algebra underlying finite T61/Cor62.                                                                              | The next row supplies only the D60-to-per-step bridge; canonical paper `≈`, off-source totality, reordering, and continuation stability remain supplied or absent.                                                                                                                                                                                                 |
| `Program`, `Reach`, `Generator`, `Independent`, `ProgramRespects`, observational variants, `YieldedAccumulator`, `StepMapMember`, `perStepCommutes_of_generated`                           | **Proved/checked:** an oracle-specific program generates the least reachable partial-forward/actual-yielded-inverse Kleisli closure; exact generator commutation and yield stability promote through the closures; supplied forward-map respect lets exact independence descend to `EffectEquiv`-observational independence; occurrence-indexed families expose duplicate self-independence; explicit provenance and closure membership discharge temporal per-step recovery commutation.                         | Oracle-specific finite executable partial/Kleisli analogue of Definition 60 and bridge into the temporal algebra. | The oracle is part of the program; forwards may be partial; finite reach/bounds are separate certificate types; `ProgramRespects` is supplied while only inverse respect follows from `EffectEquiv.applyUndo_respects`; effect observation is not rule `≃`; the bridge consumes `TotalStepMap` and proves neither totalization, reordering, T61, nor Corollary 62. |
| `ForwardDiamond`, `independent_forward_diamond`, `TotalProgramStep`, `LifecycleYieldAgrees`, `setPhase_commute`, `ForeignPhaseCompatibility`                                               | **Proved/checked:** `Independent` constructs both off-axis raw executions/common endpoint and makes two supplied totalized closure-member pre-edit maps commute exactly; `ObservationalIndependent` gives the effect-relational square and retains separate `ProgramRespects`; lifecycle-visible agreement keeps exact undo codes; distinct-name phase updates commute.                                                                                                                                           | Bounded raw/map ingredients toward Lemma 71.                                                                      | No `ForeignPhaseCompatibility` inhabitant is constructed. Semantic inverse equality does not imply stored-code equality; no guard/target preservation or lifecycle step is transposed.                                                                                                                                                                             |
| `ForeignPhaseReadable`, `OrdinaryForeignPhaseFrame`, `RegistrationOracleForeignPhaseFrame`, `of_read_frames`, `PhaseFramedExecution`, `frame`, `phase_framed_diamond`, three countermodels | **Proved/checked:** read confinement supplies exact branch/yield data; separate ordinary successor and same-child oracle frames derive exact compatibility, with registration framing proved from freshness and structural commutation; two compatible independent programs form an exact framed raw diamond with retained lookups.                                                                                                                                                                               | Compositional Definition 48 control-read/frame layer below Lemma 71.                                              | The lower contracts, distinct owners, reach/execution, post-raw fiber lookups, and typed phase payloads are supplied. Independence, readability, and raw request stability are each insufficient alone; no lifecycle guard, target, `Transition`, or Lemma 71 exchange is constructed.                                                                             |
| `LifecycleYieldStable`, `ForwardLifecycleIndependent`, `LifecycleForwardDiamond`, `LandingProgramWitness`, target preservation, `landing_activation_diamond`, necessity models             | **Proved/checked:** exact cross-forward syntax augments semantic independence; program-aligned Iter/Finish landings preserve foreign source fibers and positive targets under source WF; off-axis landings are reframed from retained templates; all four common-source landing pairs construct actual lifecycle transitions to one exact final state.                                                                                                                                                            | Fixed-program/fixed-oracle landing-only fragment toward Lemma 71(1).                                              | Requires exact cross-forward yield stability, both phase compatibilities, distinct owners, common-source applicability, and WF. No Begin pair, existing-trace identity, episode program assignment, Lemma 71(2), or trace rewrite.                                                                                                                                 |
| `ProgramActivation`, `ActivationSwapLaws`, `after_unique`, `program_activation_diamond`, `transpose_program_activations`, necessity witnesses                                              | **Proved/checked:** root-aligned Begin and fixed-program Iter/Finish activations preserve foreign positive targets, have deterministic endpoints/rules, and form all nine exact common-source diamonds; a supplied actual second activation yields a swapped actual lifecycle transition at the same endpoint.                                                                                                                                                                                                    | Partial fixed-oracle exact-representative analogue of Lemma 71(1).                                                | Requires WF, distinct owners, common-source applicability, explicit root/reach/oracle provenance, branch-relevant phase framing, and exact landing yield syntax. No activation/orchestration clause (2), arbitrary stored-trace rewrite, episode assignment, or confluence.                                                                                        |
| `RegistrationSafe`, `ExactExecutionFrame`, `reconstructOrchestration`, `transpose_activation_orchestration`, literal/frame countermodels                                                   | **Proved/corrected:** the literal child/actor condition fails by new-parent enablement and exact birth ordering; the corrected occurrence-local theorem reconstructs the early same-template orchestration, moved fixed-program activation, and exact normal endpoint for structural Begin, framed ordinary landings, and safely framed registering Retire/Remove.                                                                                                                                                | Corrected partial fixed-oracle analogue of Lemma 71(2).                                                           | Registering activation/O-Insert is excluded; landing framing is supplied, not derived; the compact parent model is structural rather than a full activation package. No birth-erasing relation, stored-trace rewrite, Lemma 72, or confluence.                                                                                                                     |
| `StepPair`, `AdjacentOccurrence`, `ProgramOccurrence`, `TraceProgramAssignment`, `AssignedAdjacentSwap`, both occurrence adapters                                                          | **Proved:** an exact adjacent pair inside a dependent trace is replaced by the corrected swapped pair with identical outer source/endpoint; rule and actor lists differ by one permutation; actual fixed-program assignments for moved steps reconstruct the complete rewritten ledger.                                                                                                                                                                                                                           | Exact stored-trace consequence of the corrected bounded Lemma 71 analogues.                                       | Only supplied branch-law occurrences are rewritten. Registering activation/O-Insert, relation-only endpoints, suffix simulation, arbitrary normalization, deletion, Lemma 72, and confluence remain absent.                                                                                                                                                        |
| `DeletionReplay`, `RetainedStep`, assignment/sublist facts, `ForwardNamesTraceSquare`, lifetime/redraw and parent/allocator countermodels                                                  | **Proved/corrected:** positional keep/drop certificates construct an intrinsic shadow trace and assignment; finite already-vestigial families admit exact same-template replay for safe foreign orchestration suffixes; countermodels refute naive parent, clock, rank, and bare-name assumptions.                                                                                                                                                                                                                | Deletion substrate and exact orchestration-only fragment below Lemma 72.                                          | No general lifecycle episode projection or suffix simulation, no derived registration/retirement provenance, no birth-erased outside relation, and no Lemma 72, normalization, canonical form, or confluence.                                                                                                                                                      |
| `ForwardPaperTraceReplay`, `RelatedAdjacentRewrite`, assigned trace certificates                                                                                                           | **Proved:** the birth-erased relation lifts to finite intrinsic traces with assigned-step transport, endpoint well-formedness, final relatedness, and exact rule/actor projections; lifecycle matching remains an explicit assigned-simulation frontier.                                                                                                                                                                                                                                                          | Finite relation-aware trace replay below Lemma 72.                                                                | No automatically derived lifecycle simulation, general episode deletion, normalization, Lemma 72, or confluence.                                                                                                                                                                                                                                                   |
| `AssignedDeletedTraceReplay`, `ReplayDecision`, relation-indexed deletion certificates                                                                                                     | **Proved:** richer trace replay carries positional keep/drop decisions, shadow assignments, and a concrete safe orchestration deletion example.                                                                                                                                                                                                                                                                                                                                                                   | Assigned deletion substrate below Lemma 72.                                                                       | No general closing-episode deletion, lifetime/no-redraw theorem, lifecycle suffix simulation, normalization, Lemma 72, or Theorem 73.                                                                                                                                                                                                                              |
| `TracePackage`, `AnyRewriteWitness`, `RewriteChain`, `chain_final_related`, `chain_rules_perm`, `chain_actors_perm`                                                                        | **Proved:** a finite connected list of supplied `RelatedAdjacentRewrite` witnesses composes through dependent trace packages; the terminal package retains each transported assignment, is birth-erased related to the source endpoint, and permutes rules and actors. Executable examples include a one-link activation/orchestration witness reaching `[O-Insert, O-Insert, L-Begin]` with actors `[0, 1, 0]`, plus a connected reverse link returning `[O-Insert, L-Begin, O-Insert]` with actors `[0, 0, 1]`. | Certificate-driven finite rewrite-chain layer toward normalization.                                               | No rewrite strategy, canonical form, termination, confluence, Lemma 72, or Theorem 73 is derived.                                                                                                                                                                                                                                                                  |
| `GlobalPaperTraceNormalizer.Authority`, `normalizeFuel`, `normalize_some`, `Result.final_related`, `Result.rules_perm`, `Result.actors_perm`                                               | **Proved conditionally:** an explicit normal-form decision, source-indexed rewrite authority, and strictly decreasing Nat measure construct a finite dependent rewrite chain; its endpoint is birth-erased related to the source and its rule/actor ledgers are permutations.                                                                                                                                                                                                                                     | Terminating authority-driven layer above supplied rewrite certificates.                                           | No strategy or measure is inferred from CORDIS dynamics; no canonical form, global termination, Lemma 72, Theorem 73, or confluence is claimed.                                                                                                                                                                                                                    |
| `DecreasingSystem`, `AuthorityLinked`, `ConfluentAuthority`, `global_join`, `normalize_results_unique`, Boolean witness                                                                    | **Proved conditionally:** decreasing steps plus local joinability yield a global join; selected `normalizeFuel` paths are reconstructed and two normalizer endpoints are equal under `ConfluentAuthority`.                                                                                                                                                                                                                                                                                                        | Conditional confluence bridge above the supplied normalizer authority.                                            | The local joins, decreasing measure, and normal-form strategy remain supplied obligations; no CORDIS-specific canonical form, Theorem 73, or global confluence is derived.                                                                                                                                                                                         |
| `PaperFiberControl`, three paper-visible Setoids, strict-rule bridge, orchestration simulation, finite deletion relation/replay, lifecycle gaps                                            | **Proved/corrected:** allocator clock/birth fields are erased from current rule observation; opposite insertion endpoints become related; all three orchestration rules match bidirectionally between independently WF full-domain states; a well-formed source with `VestigialNames` replays safe traces directionally under effect/outside control.                                                                                                                                                             | Finite paper-visible relation and structural replay below Lemmas 55/57/72.                                        | No one-sided WF transport, outside-deleted bisimulation, lifecycle simulation, name quotient, relation-aware activation swap, general episode deletion, normalization, or confluence.                                                                                                                                                                              |
| `PrecedesAt`, `FinitePrecedenceRank`, `LandingOrRaiseAt`, `RecoveryReadyAt`, `LocalProgressLaws`, `lifecycle_progress`, rejection/freshness gaps                                           | **Proved/corrected:** fixed-program rejection is distinct from raw oracle-existential applicability; exhausted finite names can deadlock the raw relation; under explicit finite rank, current reloading/recovery readiness, and committed-provider soundness, every well-formed nonquiescent state admits an exact lifecycle transition.                                                                                                                                                                         | Conditional state-local no-deadlock fragment of Theorem 66.                                                       | Printed assumptions do not supply admission/freshness totality. No `(K + 4)` bound, target-turn finiteness, maximal termination, fairness, trace program assignment, support, deletion, or confluence.                                                                                                                                                             |
| `ProgressAuthority`, `StepPotential`, `ProgressRunResult`, `runFuel`, `certifiedRun`, `certifiedRun_quiescent`                                                                             | **Proved:** a supplied state-local authority and strict potential drive an exact dependent finite lifecycle runner; each endpoint remains well formed, the intrinsic trace is retained, and the stop certificate is either quiescence or full-fuel exhaustion. Initial-potential funding rules out the exhaustion branch.                                                                                                                                                                                         | Conditional finite execution bridge for Theorem 66.                                                               | The authority and potential are supplied, not derived. No target-turn accounting, freshness, fairness, trace-wide program assignment, maximal-execution semantics, support, confluence, or unrestricted Theorem 66.                                                                                                                                                |
| `AssignedProgressAuthority`, `assignTrace`, `AssignedProgressRunResult`                                                                                                                    | **Proved:** supplied lifecycle-step assignments are recursively lifted to a dependent `TraceProgramAssignment` for every finite progress-run trace while endpoint, length, stop, and initial-potential quiescence certificates are retained.                                                                                                                                                                                                                                                                      | Explicit provenance bridge above the conditional runner.                                                          | Fixed programs, roots, oracles, and reachability are supplied rather than inferred; no full Definition 60/66 provenance, maximal execution, fairness, support, deletion, normalization, or confluence.                                                                                                                                                             |
| `ReplayedProgressRun`, `replayRun`, replay projections                                                                                                                                     | **Proved conditionally:** an assigned finite progress-run trace is replayed from a birth-erased-related well-formed peer through a caller-supplied `ForwardAssignedStepSimulation`; source endpoint/stop/length evidence, transported assignment, exact rule/actor ledgers, and peer relatedness are retained.                                                                                                                                                                                                    | Relation-aware replay bridge above the assigned runner.                                                           | No simulator derivation, lifecycle bisimulation, maximal execution, unrestricted Theorem 66, or normalization is claimed.                                                                                                                                                                                                                                          |
| `ParentEdge`, `SupportEdge`, `SupportOrder`, `supported`, `support_solution_unique`, `TotalOnProvisionAt`, `ActiveParentClosed`, `support_eq_active`, countermodels                        | **Proved/corrected:** separate provider/parent acyclicity can yield a reachable combined cycle and nonunique support; explicit combined well-foundedness gives one recursive solution; quiescence, no failure, active provision totality, and active-parent closure identify it exactly with active names.                                                                                                                                                                                                        | Corrected local analogues of Definitions 67/69 and Lemmas 68/70.                                                  | Combined order and parent closure are explicit, not trace-derived. State totality is not component-wide; the active mixed model is static. No deletion, canonical form, birth-erasing relation, or confluence.                                                                                                                                                     |
| `ValueSetoids`, `RuleRelated`, `EffectRelated`, their setoids, `EffectUndoRespect.temporalEffectEquiv`                                                                                     | **Proved/checked:** rule observation retains related active coeffects plus exact registry domain/control; effect observation retains exact ambient/normalized tables; respectful undo supplies the temporal effect interface.                                                                                                                                                                                                                                                                                     | Finite candidates for Equation 53 and Lemmas 55–57.                                                               | Local birth ranks/codes are stronger exact control; rule bisimulation, name actions, and the full global lemmas are not derived.                                                                                                                                                                                                                                   |
| `FiberMatch`, active-value/context edit lemmas, `matchOrchestrationForward/Backward`, certificate                                                                                          | **Proved:** under source well-formedness, every insert/retire/remove step has a same-kind/name peer at a rule-related state; exact endpoints are well formed and rule-related without equating private tables.                                                                                                                                                                                                                                                                                                    | Orchestration-only fragment of Lemma 55.                                                                          | Full lifecycle invariance needs iterator/read, oracle/landing, recovery, and inertia laws; an ambient-sensitive inertia countermodel is checked.                                                                                                                                                                                                                   |
| active-provider/target/reliance/quiescence transport and five structural guard certificates                                                                                                | **Proved:** well-formed rule-related states retain provider identity, dependent targets, committed resolution, reliance, exact phase patterns, quiescence, and begin/reloading/leave/divert/unload guard availability.                                                                                                                                                                                                                                                                                            | Assumption-free observation core of Lemma 55.                                                                     | No lifecycle step is executed; landing tables, run errors, inertia, registration/oracle, and recovery admission remain explicit missing laws.                                                                                                                                                                                                                      |
| noncircular landing/error/inertia/recovery contracts, all-eight lifecycle and unified matching                                                                                             | **Proved conditionally:** supplied cross-source external laws produce exact same-rule/name peer transitions for every lifecycle constructor and a well-formed ten-rule certificate with related endpoints.                                                                                                                                                                                                                                                                                                        | Conditional well-formed analogue of Lemma 55.                                                                     | Contracts are not derived from base Dynamics; raw no-WF RuleBisimulation is not inhabited. Finish alone requires explicitly related newly active tables.                                                                                                                                                                                                           |
| `NameAction`, dependent data/state action laws, `wellFormed_act_iff`, orchestration equivariance                                                                                           | **Proved:** an executable bijection renames parent/view/retire references and acts on all payloads; actions compose/invert exactly, preserve strengthened WF, and commute bidirectionally with insert/retire/remove.                                                                                                                                                                                                                                                                                              | Structural/orchestration fragment of Lemma 56.                                                                    | Keys/components/catalog stay fixed; entry invariance is separate, and dynamics/oracle/recovery/inertia/lifecycle equivariance is not yet proved.                                                                                                                                                                                                                   |
| canonical result/request/oracle/landing/recovery actions and lifecycle/unified action certificates                                                                                         | **Proved conditionally:** exact run, external-undo, dynamics-equivalence, inertia, and catalog-entry action laws derive all-eight lifecycle and ten-rule forward/backward equivariance with exact acted endpoints/names.                                                                                                                                                                                                                                                                                          | Conditional fixed-catalog analogue of Lemma 56.                                                                   | External laws and source WF are required; component/catalog action and unconditional arbitrary-Dynamics equivariance are not claimed.                                                                                                                                                                                                                              |
| `Vestigial`, `effectRelated_remove`, forward/backward removal squares and exception countermodels                                                                                          | **Proved/corrected:** a vestigial entry is effect-equivalent to absence; safe insert/retire/remove steps commute exactly with deletion; four well-formed models certify every required exception.                                                                                                                                                                                                                                                                                                                 | Effect clause and orchestration fragment of Lemma 57.                                                             | Pinned L57(1) omits parent adoption; L57(2) omits parent removal in addition to its two insert exceptions. Lifecycle/iterator simulation is absent.                                                                                                                                                                                                                |
| `begin_dependencies_provided`, `LocatedEpisode`, `NestedEpisodes`, resolution/no-unload/table facts                                                                                        | **Proved/conditional:** admitted begins provide dependencies; explicitly nested episodes have strict boundary order; committed provider resolution persists and blocks unload; per-record table confinement composes.                                                                                                                                                                                                                                                                                             | Finite fragments of Theorem 63.                                                                                   | Nesting is supplied rather than inferred from maximal episodes; table constancy is conditional and foreign discharge excludes same-name iterator edits.                                                                                                                                                                                                            |
| `reloading_target_dichotomy`                                                                                                                                                               | **Proved:** one lifecycle step from a known reloading source is iter/finish with the same target, divert with a changed target, or raise.                                                                                                                                                                                                                                                                                                                                                                         | Local structural fragment of Theorem 64.                                                                          | No initial reloading interval, eventual close, parameterized Corollary 62 recovery, or full resolution-coherence theorem is claimed.                                                                                                                                                                                                                               |

### Public surface, examples, and executable checks

| Artifact                                                                                                                       | Status and exact role                                                                                                                                                                                                                                   | Boundary                                                                                                                                                       |
| ------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [`Cordis.lean`](../Cordis.lean)                                                                                                | **Checked build surface:** imports the kernel, generic/rich session harness, coeffect/context, global iterator/transposition/foreign-phase/landing layers, stream refinement, examples, lifecycle, policy, protocol, registry, tool, and wire modules.  | Importing a module makes its declarations available; it does not prove a deployed adapter used them correctly.                                                 |
| [`Examples/Counter.lean`](../Cordis/Examples/Counter.lean)                                                                     | **Proved pure example:** defines request-indexed read/increment contracts and verified pure implementations; `increment_returns_requested_value` states the modeled successor exactly.                                                                  | The counter is an in-memory `Nat`, not external I/O. Its capability predicates and registry are example values.                                                |
| [`Examples/CounterWire.lean`](../Cordis/Examples/CounterWire.lean)                                                             | **Checked/proved example boundary:** supplies codecs, catalog-name resolution, counter-limit and capability certificates, raw example calls, and `validateRaw`.                                                                                         | `allNeeds` and `allCapabilities` intentionally grant everything in this deterministic example; they are not authentication or production policy.               |
| [`Cordis/TestSuite.lean`](../Cordis/TestSuite.lean), [`Tests.lean`](../Tests.lean)                                             | **Checked executable tests:** runtime checks include exact rewrite/deletion projections and paper-visible parent/retirement equality at endpoints whose allocator births differ `1` versus `2`.                                                         | Passing examples and failure cases do not prove universal properties or TypeScript interoperability. The universal claims remain the Lean theorem types above. |
| [`Cordis/NegativeTests.lean`](../Cordis/NegativeTests.lean)                                                                    | **Checked static rejection tests:** guarded elaboration errors reject malformed deletion and the attempt to upgrade birth-erased allocator endpoints to current stricter `RuleRelated`.                                                                 | These are finite attacks against the current API. They do not establish completeness of the type-level invariants or replace the universal theorems.           |
| [`Cordis/AxiomAudit.lean`](../Cordis/AxiomAudit.lean)                                                                          | **Checked audit command set:** selected entries cover relation Setoids/bridges, bidirectional orchestration matches, retained assignments, finite erased-shadow replay, allocator/outside/lifecycle countermodels, and lower rewrite/deletion surfaces. | It reports dependencies for those declarations only. It is not a proof about every declaration, the compiler/runtime, or external systems.                     |
| [`.github/workflows/ci.yml`](../.github/workflows/ci.yml), [`scripts/check_lean_hygiene.py`](../scripts/check_lean_hygiene.py) | **Checked automation:** the pinned workflow verifies the toolchain, strict/default builds, static and runtime tests, demo, lexical source policy, and the selected axiom allow-list.                                                                    | Hosted runners, actions, the script, and its lexical policy remain trusted automation. Passing CI is not a semantic proof of every source declaration.         |
| [`Main.lean`](../Main.lean)                                                                                                    | **Checked demonstration:** executes the pure counter Harness and prints its model, protocol, record outcomes, per-record policy dispatch counts, and whether a dependent result was encoded.                                                            | Console output is a demo observation, not a theorem or production Harness implementation.                                                                      |

The streamed heterogeneous schema continuation is mapped by
`Cordis.DeepSeekSchemaStreamConversation`: it proves the typed `stream: true` request, validates a
complete process-backed SSE body through the existing rich-stream/session certificate, dispatches
entry-specific calls, and keeps a caller-fueled history separate from a text terminal or
exhaustion. Incremental transport, provider-complete assembly, and deployed Harness equivalence
remain unmapped claims.

`Cordis.DeepSeekHarnessProcessSchema` adds process provenance to that boundary. The exact
registry-certified streaming plan, processed body, schema step, and dependent runner endpoint
are retained together; request, process/status, and registry-execution errors remain distinct.

`Cordis.DeepSeekHarnessProcessSchemaPrefix` carries the same exact plan through the line-oriented
prefix process result, retaining fuel-exhausted and cancelled stops as well as the completed
`[DONE]` branch that carries the dependent schema step. Byte framing and deployed cancellation
semantics remain unmapped.

`Cordis.DeepSeekHarnessProcessSchemaPrefixConversation` carries the same plan through the
caller-fueled prefix loop. Each completed tool-round witness retains its prepared plan and
accepted prefix, while round exhaustion and cancellation retain the attempted plan. The loop is
still a local line-oriented adapter; deployed interruption and persistence remain unmapped.

`Cordis.DeepSeekSchemaStreamPrefixConversation` adds the line-oriented companion: it retains an
accepted prefix and typed budget/cancellation stop, then unlocks the same dependent registry only
after strict `[DONE]` rich/session validation. Byte framing, blocked-read interruption, and
deployed Harness equivalence remain unmapped.

`Cordis.DeepSeekSchemaStreamErrors` maps the opt-in heterogeneous provider-error continuation. Its
failed-entry certificate keeps the selected schema binding, parsed/admitted call, policy proof,
exact provider message, and unchanged model; conversion to the existing `ProviderFailedTool`
surface gives an `isError` result, and the executable fixture carries that result into a later
streamed terminal. Retries, cancellation, persistence, external effects, and deployed error
semantics remain unmapped.

`Cordis.DeepSeekStreamFailure` maps the separate wire-only provider terminal-failure subset. A
complete strict SSE body ending in `content_filter` or `insufficient_system_resource` retains its
leading frames, terminal raw `DataFrame`, singleton choice/reason, and optional usage; ordinary
rich finishes and unmodeled `error`/`aborted` envelopes remain outside the certificate.

`Cordis.DeepSeekTerminalOutcome` maps the adjacent sum of that failure subset and the four
successful rich-stream languages. Failure classification runs first, and a successful branch
retains its existing wire/projection/rich certificate rather than being normalized into a weaker
common record. This is a complete-body dispatcher only; it does not establish session semantics or
deployed provider behavior.

| `DeepSeekHarnessExtensionPersistence.ValidatedJson`, `validate`, `validateText`, `validateBytes`, `readValidated`, `RestoredRunner` | **Checked/proved/exercised extension persistence composition:** a session header plus required extension suffix retains exact JSONL/text/UTF-8/backend representations, rejects known core and ignorable rows, restores the indexed extension runner, and links a schema-certified request to the persisted endpoint; memory, temporary-file, append, and invalid-UTF8 fixtures exercise the executable surfaces. | Extension-only persistence bridge above the schema-owned runner/request path. | Mixed current-Harness replay, packed-row persistence, fsync/crash repair, concurrent-writer safety, provider compatibility, transport trust, and deployed persistence equivalence remain external. |
| `DeepSeekHarnessMixedPersistence.MixedRows`, `validate`, `MixedCertificate` | **Checked/proved/exercised mixed source partition:** an explicit row schedule reconstructs one lossless complete archive while independent core and dependent-extension certificates retain exact projection ASTs and indexed endpoints. | Honest composition boundary for mixed persistence inputs. | Combined arbitrary-schema replay, extension surface integration, global sequence normalization, packed-row durability, provider compatibility, transport trust, and deployed persistence equivalence remain external. |
| `DeepSeekHarnessSchemaLift.liftSession`, `SchemaLiftCertificate`, `certify` | **Checked/proved/exercised core schema transport:** dependent core events, payloads, intents, surface transitions, sequence/header state, and protocol projection lift from `Session.noExtensions` into any `ExtensionSchema`; the custom-schema fixture retains the complete endpoint certificate. | Exact type-index transport for the validated core, adjacent to but independent from mixed-row partitioning. | Custom extension-row decoding, mixed interleaving, extension surface integration, provider compatibility, transport trust, and deployed persistence equivalence remain external. |
| `DeepSeekHarnessMixedReplay.Row`, `MixedReplay`, `ValidatedMixedLog`, `validateLog` | **Checked/proved/exercised bounded mixed replay:** a tagged schedule interleaves stateful core validation with custom log-only extension rows, synchronizes a phantom shadow clock, and retains exact final sequence, surface/header, protocol-projection, and typed rejection witnesses. | Global sequence normalization for the restricted source-honest log-only extension subset. | Custom surface integration, extension encodings of core kinds, malformed-row/provider/persistence/transport compatibility, and deployed Harness equivalence remain external. |
| `DeepSeekHarnessTransportContract.TransportRound`, `executePrepared`, `executeSource` | **Checked/proved/exercised injected transport composition:** a prepared request crosses an injected transport, 2xx status and one response decoder are retained, `acceptValidated` admits the same dependent response, and the exact runner append endpoint is certified; local tool-call success and 503 rejection fixtures run. | Pure local transport-to-session contract above the existing request and session-language bridges. | Live network/credential/provider/TLS/retry behavior, persistence, external tool execution, and deployed Harness equivalence remain external. |
| `DeepSeekHarnessTransportToolRound.ToolTransportRound`, `executeSource`, `Example.round` | **Checked/proved/exercised certified tool continuation:** the retained single-decoder transport certificate feeds dependent tool execution without reparsing, and the final session-runner endpoint carries exact assistant-plus-tool-result sequence and tool-count equations; local success and typed 503 fixtures run. | Local injected-transport plus typed-tool execution composition above `TransportRound`. | External tool effects, provider obedience, retry/cancellation, persistence, credentials, live network behavior, and deployed Harness equivalence remain external. |

## Paper claims not implemented by the mapped modules

The following are intentionally not presented as completed formalization work.

1. **Effect-context tower beyond the finite indexed run:** `Cordis.EffectContext` now represents
   Definitions 1–3, 6, and 8–12 and proves Theorems 4–5, 7, and 10–16, including the exact
   lifted-inverse/recovery formula and finite reverse-order context recovery. No theorem here
   turns that indexed run into arbitrary interleaving or the later transformation-monoid
   independence claims.
2. **Beyond finite exact removal:** Definitions 17–19, Theorem 20, and Corollary 21 are now
   mechanized for finite exact pairwise-independent effects. Their observational-quotient,
   infinite, asynchronous, and external-effect interpretations are not. The separate Schedule
   theorem reorders whole effects and is not used as a substitute for inverse permutation.
3. **Recursive unified-context fixed point:** Definitions 22–31 now have bounded direct models,
   but Definition 32's equirecursive fixed point is represented only by exact finite unfoldings.
   The negative occurrence in `Gamma -> Gamma` is not hidden behind an invalid inductive type.
4. **Full mediated independence:** the finite-context relation of Definition 33, the
   fixed-generator interpretation of Definition 34/Lemma 35, Definitions 36–37, the finite
   Lemma 38 core, full total Definition 39, finite partial distinct-key Theorem 40, and Definition
   41 are proved. Same-word tests do not imply paired-inverse coherence, and forward commutation
   does not imply inverse stability. Realized paths and the observational closure conclusion are
   typed, and a counterexample separates it from exact representative closure. The older partial
   closure is also refuted because it compares individual rather than composite domains. A
   corrected theorem swaps arbitrary finite outcome-selected trees with exact whole-run
   successor/undo equality and conditional inverse stability. `PartialTransformation` further
   proves full closure commutation and yield stability for the partial forward map and every
   actually yielded totalized inverse. A countermodel proves whole-run equality is strictly
   weaker. `ObservationalPartialTransformation` additionally proves every current computation
   generator respects the finite context relation and descends the full exact closure theorem.
   Its `RespectGap` counterexample shows exact commutation alone cannot justify that descent. The
   theorem still assumes exact pairwise overlap, so the paper's literal unrestricted
   total/quotient setting remains unproved. `TotalQuotientIndependence` supplies a separate
   finite specialization when success at every context is carried as a certificate.
5. **Full component calculus:** code-only component/fiber/phase/global-registry data, provider
   uniqueness, target/quiescence, birth-ranked parents, and O-Insert/O-Retire/O-Remove are now
   mechanized. External ordinary/registration iterator-code semantics, explicit write/read
   obligations, recovery/WF witnesses, and a fueled Definition 52 substrate are also implemented.
   The seven lifecycle rule names, exact non-reliance guard, phase transitions, explicit inertia,
   and rule-by-rule preservation are mechanized. A combined ten-rule-name relation now retains
   exact endpoints, acted-on names, state-map/edit projections, and an empty-registry-origin trace.
6. **Full preservation metatheory:** strengthened registry well-formedness and preservation by
   the three orchestration rules, all seven lifecycle rule names, and their combined finite traces
   are proved. Actual installed status changes only at L-Begin/L-Unload. General L-Unload
   preservation consumes `RecoveryAdmission`. Bounded Lemma 54 facts are now proved: exact
   non-unload foreign preservation, conditional unload confinement, committed/static continuity,
   aligned name-specific episode boundaries, and accumulator-map uniqueness. A kernel
   countermodel shows bare admission can mutate a foreign table. `GlobalRelations` now defines
   separate rule/effect observation candidates and proves their setoid and incomparability facts.
   `GlobalRuleInvariance` proves exact bidirectional matching for the three orchestration rules
   over well-formed related sources, including unequal but observationally related private values.
   An ambient-sensitive abort policy kernel-refutes full lifecycle invariance from `RuleRelated`
   alone. `GlobalRuleObservations` nevertheless transports every assumption-free provider, target,
   resolution, reliance, phase, quiescence, and structural-guard observation. A Finish landing
   must still relate newly active tables that were private while reloading; iterator/read,
   registration, landing, recovery, and inertia-respect laws remain absent from the base API.
   `GlobalLifecycleBisimulation` names those laws noncircularly and proves all eight lifecycle plus
   all ten unified cases conditionally. Its Finish countermodel proves the related-table clause
   independently necessary; the result is not an unconditional paper Lemma 55 theorem.
   `GlobalNameAction` replaces the old opaque placeholder with actual invertible actions on every
   stored payload, proves state/WF inversion and the three orchestration cases, and kernel-refutes
   the old record with a constant noninjective name map. `GlobalNameLifecycle` assumes only exact
   run, external-undo, dynamics-equivalence, inertia, and catalog-entry action laws, derives
   admission/oracle/execute-one/landing/recovery actions, and proves every lifecycle/unified case
   bidirectionally. This remains a conditional well-formed fixed-catalog Lemma 56 analogue.
   `GlobalVestigial` proves the effect-equivalence sentence and corrected orchestration-only
   simulation. Kernel models show the pinned forward clause misses O-Insert adopting the
   vestigial parent and the backward clause misses O-Remove of the vestigial fiber's parent; the
   latter is a third exception beyond the paper's two insert cases. Rule bisimulation, name
   equivariance, lifecycle/iterator vestigial simulation, new-entry/retirement provenance, the
   actual full Lemmas 55–57, and full Theorem 59 remain absent.
7. **Temporal composability:** a parameterized finite recovery algebra is now proved.
   `GlobalTemporal` reifies partial off-source step maps and composes explicitly supplied
   totalizations and per-step recovery squares. `GlobalIteratorIndependence` supplies an
   oracle-specific finite partial/Kleisli Definition 60 analogue: reachable forward and actually
   yielded inverse maps generate each program closure, exact `Pairwise` is occurrence-indexed, and
   owner/foreign `EffectEquiv`-observational independence plus generated provenance/membership
   evidence yields `PerStepCommutes`. Given `Independent`, `GlobalTransposition` further constructs
   the exact raw two-forward execution diamond and the exact square for two maps whose totality and
   closure membership are supplied. Given `ObservationalIndependent`, those maps commute under the
   selected effect relation; this certificate retains separately supplied `ProgramRespects`. The
   module also proves distinct phase edits commute and
   kernel-refutes promotion from semantic inverse equality to exact stored undo-code equality.
   `GlobalForeignPhase` then derives compatibility from explicit readability, ordinary
   exact-successor, and same-child oracle frames, and constructs an exact two-sided framed raw
   diamond with retained post-raw fiber lookups. Separate kernel models prove full independence
   does not imply compatibility, readability does not imply the ordinary exact frame, and an
   identical readable registration request does not stabilize its state-dependent oracle.
   `GlobalLandingTransposition` adds exact cross-forward `UndoCode` stability, fixed-program
   landing provenance, and structurally derived positive-target preservation, then constructs all
   four L-Iter/L-Finish common-source lifecycle orders with one exact final state. A separate raw
   forward model proves semantic independence still does not imply syntactic yield stability;
   provider-Finish and oracle models prove common applicability and program provenance necessary.
   `GlobalActivationTransposition` adds root-aligned Begin, fixed-program endpoint/rule
   determinism, and a branch-minimal exact diamond for all nine Begin/Iter/Finish pairs. Its
   paper-shaped wrapper accepts an actual normal-order second activation and derives the swapped
   actual lifecycle transition at the same endpoint. The subsequent
   `GlobalActivationOrchestrationTransposition` kernel-refutes the literal clause-(2) premise by
   parent enablement and birth-order models, then proves a corrected exact exchange under
   orchestration-sensitive registration safety and one supplied occurrence frame. It excludes
   registering activation/O-Insert. `GlobalTraceRewrite` now locates those exact pairs inside an
   intrinsic stored trace, consumes fixed-program assignments for the actual steps, splices the
   exact replacement through retained context, and rebuilds the assignment ledger. These theorems
   still do not identify effect observation with rule relation `≃`, construct `TotalStepMap`,
   derive branch laws for arbitrary occurrences, simulate a suffix from a merely related
   birth-erased endpoint, provide episode-level program assignment, or establish a normalization
   certificate. `GlobalDeletion` now constructs generic intrinsic keep/drop replay and exactly
   replays safe foreign orchestration suffixes after entries are already vestigial, but its parent,
   allocator, and redraw countermodels block promotion to closing-episode Lemma 72.
   `GlobalPaperRelation` now supplies the allocator-erased full/outside/deletion observations,
   proves actual bidirectional orchestration replay, and from a well-formed source carrying
   `VestigialNames` packages directional safe erased-shadow replay. Its clock-sensitive
   L-DivertAbort countermodel refutes unconditional lifecycle
   simulation. Canonical unrestricted relation identification, the paper's literal total/quotient Lemma 71, arbitrary Theorem
   61, and Corollary 62 therefore remain absent. `GlobalProgress` separately kernel-refutes raw
   no-deadlock under exhausted finite names and configured progress under oracle rejection, then
   proves a corrected state-local no-deadlock theorem from explicit precedence rank,
   landing-or-raise, recovery, and committed-provider authorities. It does not prove Theorem 66's
   quantitative or maximal-termination conclusions. `GlobalSupport` then kernel-refutes the
   printed Lemma 68 union-order argument, requires combined support well-foundedness directly, and
   defines the unique recursive support predicate. Its corrected quiescent theorem also requires
   active provision totality, no failure, and active-parent closure; separate models prove both
   combined order and parent closure are independent of current static/reachability facts.
8. **Spatial composability:** finite fragments are now proved. L-Begin target evidence supplies
   every dependency; an explicit `NestedEpisodes` master-trace decomposition gives strict
   provider-before-consumer opening and consumer-before-provider closing; boundary-free committed
   resolution blocks provider unload; and per-record confinement yields table-value constancy.
   `reloading_target_dichotomy` classifies one reloading step. The API still lacks maximal episode
   extraction, unconditional same-owner table constancy, an initial-interval theorem, eventual
   close, and the Corollary 62 recovery conclusion, so full Theorems 63–64 remain absent.
9. **Progress:** Definition 65's precedence relation and the conditional state-local no-deadlock
   fragment is implemented; `GlobalProgressRun` adds a finite runner under supplied authority
   and potential, and `GlobalProgressAssignment` reconstructs a dependent assignment when the
   caller supplies one for each lifecycle occurrence. The printed quantitative/maximal Theorem
   66 termination and inferred-program result remain absent.
10. **Confluence:** A generic decreasing/local-diamond Newman kernel now proves global joins and
    unique irreducible endpoints. The CORDIS-specific local diamonds, measure, canonical form,
    and Theorem 73 confluence result remain absent.
11. **Loader and HMR:** Definition 74 entry fields, stable-ID configuration reconciliation,
    Algorithm 8 accepted/declined fixed-point classification, Algorithm 9 declined-boundary
    stale detection, and Algorithm 10 transactional reload/rollback are now modeled by the
    bounded `Cordis.LoaderHMR` slice. Dynamic imports, filesystem watches, real fiber lifetimes,
    cache mutation, and deployed loader equivalence remain outside.
12. **System-boundary enforcement:** §6 discusses resource boundaries, service
    multiplexing, access control, sandboxing, language independence, and dependency
    granularity. The mapped modules do not prove OS isolation, credential confinement,
    network/filesystem policy, or correctness of irreversible external effects.

`Cordis.DeepSeekHarnessEventProcessOutcome` now composes the current-Harness event text/byte
refinement with the complete process-backed rich-outcome adapter. A successful value retains the
prepared streaming request, process/response certificate, dependent tool outcome, final runner,
raw event archive, restored-session equality, and exact protocol projection. The request source
is caller-supplied; provider/schema/credential authenticity, blocked-read cancellation,
persistence, and deployed Harness equivalence remain outside this composition.
The same composition now also launches the existing fuel-bounded complete-body streamed
conversation from the restored endpoint. Its result retains the prepared first request, typed
round history, final runner/model, and explicit completion-or-exhaustion stop; it does not add
blocked-read cancellation, persistence, or deployed-runtime equivalence.

`Cordis.DeepSeekHarnessEventProcessSchema` closes the parallel heterogeneous-schema attachment.
Its indexed round keeps the restored archive/session certificate together with the registry-derived
streaming plan, complete body, schema step, dependent runner endpoint, exact raw-line equality,
and protocol projection. The executable weather/clock fixture reaches the `8 -> 11` endpoint;
provider obedience, credentials, byte framing, persistence, cancellation, external effects, and
deployed Harness equivalence remain unmapped.

`Cordis.LoaderHMR` is the corresponding source-grounded loader boundary. Its executable model
keeps Definition 74 entries keyed by stable IDs, classifies pending modules to a fixed point with
an explicit unresolved-cycle fallback, walks dependencies only across non-declined boundaries,
and stages Algorithm 10's invalidate/import/dispose/install transaction. The failure branch
proves exact restoration of the pre-reload cache/fiber state. This is a finite pure model rather
than a filesystem watcher, dynamic-import evaluator, fiber runtime, or deployed HMR theorem.

## Harness claims not established by this map

Even though the local wire, stream, policy, and Harness modules execute deterministic reference
paths, the mapped Lean modules do not establish:

- source or binary compatibility with the pinned TypeScript packages;
- equivalence of Lean JSON schemas/decoders to Harness JSON Schema validation;
- scoped registration, shadowing, restrictions, approval routing, or the complete tool
  execution pipeline;
- the complete TypeScript parallel-dispatch behavior, including wall-clock overlap, promise
  races, cleanup, retries, and persistence. `Cordis.ParallelHarness` proves only the bounded
  pure certificate slice above, while `Cordis.ParallelSchedule` composes that slice over a finite
  sequence: model-ordered commits, exclusive barriers, exact recovery, and a no-effect synthetic
  drain;
- `Cordis.AsyncHarness` adds only a bounded pure fiber state machine on top: completion-order
  races and typed cancellation/failure are certified in finite traces, but live task handles,
  fairness, cleanup, and cancellation delivery remain unmodeled;
- a filesystem or database persistence refinement beyond the narrow executable `DurableIO`
  adapter. `Cordis.DurableSettlement` proves the typed crash-prefix/resume boundary above, and
  `DurableIO` tests host memory/file calls plus suffix-aware recovery, but neither proves fsync,
  stable media, crash atomicity, authentication, or external-effect exactly-once behavior;
- the complete merge-extensible session vocabulary, surface replacement rules, persistence,
  checkpoint durability, or crash recovery; or
- correctness of real tool I/O, host callbacks, JavaScript promises, an OS sandbox, or a
  network/storage backend.

These are adapter, runtime, and systems claims. They require separate refinement theorems or
integration tests; they cannot be inferred from similarly named Lean declarations.

`Cordis.DeepSeekStreamByteFraming` now fills the pure byte-ingress gap below the
line-oriented prefix machine. Arbitrary `ByteArray` chunks are split at LF boundaries with an
exact canonical reconstruction; complete lines decode as UTF-8 before typed prefix parsing;
invalid UTF-8 and incomplete final lines remain typed errors; and `finish` bridges the
reconstructed text to strict SSE validation. This is not a process-level byte reader and does
not prove blocked-read interruption, backpressure, cancellation, reconnect, provider-complete
assembly, or deployed stream equivalence.
`Cordis.DeepSeekCurlByteFraming` connects that pure seam to bounded piped-process stdout. It
retains the observed `ByteArray` chunks, raw UTF-8 output, private status/body parse equation, and
the exact body-chunk framing certificate before strict SSE validation. This is finite process
evidence only: network, credential, executable-trust, blocked-read, backpressure, cancellation,
reconnect, provider-complete assembly, and deployed stream equivalence remain external.
`Cordis.DeepSeekStreamHarnessByte` carries that dependent byte witness through the complete-body
streamed Harness continuation. Its one-round and fuel-bounded fixtures retain the raw/framed/status
certificate while dispatching the decoded body through rich tool execution, certified result append,
and text-terminal completion. Byte-level cancellation, blocked-read interruption, backpressure,
reconnect, and deployed Harness equivalence remain outside.
`Cordis.DeepSeekCurlBytePrefix` now adds the process-byte prefix seam below that continuation:
bounded stdout reads retain raw chunks and incomplete fragments, complete body lines feed the typed
prefix state before the next read, and status-trailer bytes stay outside the SSE body. This is
finite process/framing evidence; blocked-read interruption, backpressure, cancellation, reconnect,
provider-complete assembly, and deployed Harness equivalence remain unproved.
`Cordis.DeepSeekStreamHarnessBytePrefix` composes that live process-byte prefix with the
existing rich/tool/session runner. Completed prefixes retain the exact body/framing/status witness
beside the assistant/tool endpoint, while read-budget prefix stops become explicit nonterminal
Harness stops. This remains finite process evidence rather than deployed or blocked-read semantics.
`Cordis.DeepSeekCurlBytePrefixTimeout` adds the checked timer-driven byte-read boundary below that
continuation. A real `Std.Async.Sleep` races each synchronous stdout-byte read; a configured-child
timeout retains the accepted prefix state, raw chunks, pending fragment, stderr, exit observation,
and timeout line, with blocked, delayed-prefix, and fast-completion fixtures. This remains local
configured-process evidence, not arbitrary cleanup, fairness, backpressure, authenticity,
durability, reconnect, or deployed Harness semantics.
`Cordis.DeepSeekStreamHarnessBytePrefixTimeout` composes completed timed prefixes with the existing
dependent streamed conversation runner and a fuel-bounded multi-round endpoint trace. It retains
the prefix witness beside finish, tool execution, and session indexing, while timeout/fuel/
cancellation stops remain explicit nonterminal errors. In-flight provider behavior and deployed
asynchronous equivalence remain unproved.
`Cordis.DeepSeekHarnessLiveStreamProbe` is the credential/runtime handoff above that byte-prefix
seam: it classifies a caller-named environment credential without logging it, proves a typed
`stream: true` request, and feeds configured curl byte chunks into the dependent conversation
runner. Its fixture retains one streamed round, raw-prefix evidence, the model/session endpoint,
and explicit fuel exhaustion. Credential validity, remote reachability, provider obedience,
process trust, blocked-read cancellation, backpressure, retries, persistence, and deployed
Harness equivalence remain external.

The indexed follow-up `Cordis.GlobalPaperTraceScopedConfluence` refines the conditional
confluence bridge over an explicit finite package family. Its selected links carry exact
source/target equations, and the nonempty activation/orchestration fixture supplies executable
endpoint, rule, and actor projections. This remains a caller-supplied finite certificate, not a
derived lifecycle strategy, Lemma 72, or Theorem 73.

[paper-tree]: https://github.com/cordiverse/paper/tree/948a07b369c62adb3b12e102458be5c18dfb69b9
[paper-pdf]: https://raw.githubusercontent.com/cordiverse/paper/948a07b369c62adb3b12e102458be5c18dfb69b9/paper.pdf
[cordis-tree]: https://github.com/cordiverse/cordis/tree/8cc9e33fab69e2d0476d126baaf2acb24e6a6ab4
[cordis-context]: https://github.com/cordiverse/cordis/blob/8cc9e33fab69e2d0476d126baaf2acb24e6a6ab4/packages/core/src/context.ts#L21-L77
[cordis-fiber]: https://github.com/cordiverse/cordis/blob/8cc9e33fab69e2d0476d126baaf2acb24e6a6ab4/packages/core/src/fiber.ts#L78-L485
[cordis-reflect]: https://github.com/cordiverse/cordis/blob/8cc9e33fab69e2d0476d126baaf2acb24e6a6ab4/packages/core/src/reflect.ts#L61-L227
[harness-tree]: https://github.com/deepseek-ai/deepseek-harness/tree/47f943859bef60e4160492346772ded9b24f765a
[harness-vendor]: https://github.com/deepseek-ai/deepseek-harness/blob/47f943859bef60e4160492346772ded9b24f765a/vendor/README.md#L9-L49
[harness-cordis-pin]: https://github.com/cordiverse/cordis/tree/56b3d4f725681cf4556c1a8695a709cc3b6eed74
[harness-plugins-pin]: https://github.com/deepseek-ai/deepseek-harness/blob/47f943859bef60e4160492346772ded9b24f765a/vendor/README.md#L20-L27
[harness-vendor-fiber]: https://github.com/deepseek-ai/deepseek-harness/blob/47f943859bef60e4160492346772ded9b24f765a/vendor/cordis/src/fiber.ts#L64-L689
[harness-tools]: https://github.com/deepseek-ai/deepseek-harness/blob/47f943859bef60e4160492346772ded9b24f765a/packages/core/tools/src/index.ts#L211-L269
[harness-tools-policy]: https://github.com/deepseek-ai/deepseek-harness/blob/47f943859bef60e4160492346772ded9b24f765a/packages/core/tools/src/index.ts
[harness-tools-view]: https://github.com/deepseek-ai/deepseek-harness/blob/47f943859bef60e4160492346772ded9b24f765a/packages/core/tools/src/index.ts#L1031-L1284
[harness-tools-execute]: https://github.com/deepseek-ai/deepseek-harness/blob/47f943859bef60e4160492346772ded9b24f765a/packages/core/tools/src/index.ts#L1328-L1530
[harness-llm-stream]: https://github.com/deepseek-ai/deepseek-harness/blob/47f943859bef60e4160492346772ded9b24f765a/packages/llm/llm/src/types.ts#L283-L303
[harness-current-stream-types]: https://github.com/deepseek-ai/deepseek-harness/blob/99f6f02fecdb7dff40c3fbc9470f5907c29f74ca/packages/llm/llm/src/types.ts
[harness-current-session-types]: https://github.com/deepseek-ai/deepseek-harness/blob/99f6f02fecdb7dff40c3fbc9470f5907c29f74ca/packages/core/session/src/types.ts
[harness-jsonl-format]: https://github.com/deepseek-ai/deepseek-harness/blob/99f6f02fecdb7dff40c3fbc9470f5907c29f74ca/packages/session/session-persistence-jsonl/src/format.ts
[harness-jsonl-chunks]: https://github.com/deepseek-ai/deepseek-harness/blob/99f6f02fecdb7dff40c3fbc9470f5907c29f74ca/packages/core/session/src/chunk-rows.ts
[harness-session-types]: https://github.com/deepseek-ai/deepseek-harness/blob/47f943859bef60e4160492346772ded9b24f765a/packages/core/session/src/types.ts#L230-L297
[harness-session-invariant]: https://github.com/deepseek-ai/deepseek-harness/blob/47f943859bef60e4160492346772ded9b24f765a/packages/core/session/src/invariant.ts#L1-L250
[harness-session-append]: https://github.com/deepseek-ai/deepseek-harness/blob/47f943859bef60e4160492346772ded9b24f765a/packages/core/session/src/index.ts#L564-L655
[harness-tool-scheduler]: https://github.com/deepseek-ai/deepseek-harness/blob/47f943859bef60e4160492346772ded9b24f765a/packages/core/agent-loop/src/tool-calls.ts

`DeepSeekAsyncStreamHarnessTimeout` is the timed streamed-race refinement. It composes
`ContextAsync.race` with the arbitrary-byte deadline reader, preserving the dependent runner/model
trace for completion and the exact accepted prefix for timeout. This remains configured local
process evidence, not a theorem of task-cancellation delivery, fairness, cleanup, reconnects,
authenticity, or deployed asynchronous Harness behavior.
`DeepSeekExternalToolProcess` is the adjacent external-tool observation seam. It retains the
configured command, stdout/stderr, exit code, parsed JSON, and typed decoded result; only an
explicit `ToolSpec.post` proof can construct `AcceptedResult.certified` and a
`ToolSpec.CertifiedOutcome`. This is local process evidence, not command identity, sandboxing,
authentication, exactly-once effects, cleanup, or deployed Harness equivalence.
`DeepSeekExternalToolRound` is the local session bridge after acceptance: it constructs the
log-only tool-call plus cited surface tool-result append and proves the exact sequence, message,
and protocol projections. Rendering is supplied by the caller; no provider-specific deployment
claim is made.
